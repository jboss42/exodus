/**
 *  \mainpage 
 *  
 *  \section sec_1 COM API
 *  The COM APIs to Exodus are documented for plugin devleopers. 
 *  Plugin developers are free to use any language supporting COM.
 *
 *  \section sec_2 Delphi API
 *  Exodus itself is written in Delphi.  Class heirarchy is presented, but
 *  extended documentation is still a work in progress.
 * 
 *  \section sec_3 Event Documentation
 *  Exodus is an event driven application. Click 
 *  <a href='..\events.html'>here</a> for a descriptive listing of 
 *  Exodus events.
 * 
 *  \section sec_4 How to write a plugin
 *  Plugins can be written in any language that supports COM.  Click
 *  <a href='..\plugin.html'>here</a> to read the how-to for writing plugins.
**/
[
  uuid(37C1EF21-E4CD-4FF0-B6A5-3F0A649431C8), 
  version(1.0), 
  helpstring("Exodus COM Plugin interfaces")
    
]
library Exodus
{

  importlib("stdole2.tlb");

  [
    uuid(E89B1EBA-8CF8-4A00-B15D-18149A0FA830), 
    version(1.0),
    helpstring("ExodusController Object")
  ]
  coclass ExodusController
  {
    [default] interface IExodusController;
  };

  [
    uuid(9AE59046-2197-429A-A62D-8EE61122B04E), 
    version(1.0)
  ]
  typedef enum tagChatParts
  {
    HWND_MsgInput = 0, 
    Ptr_MsgInput = 1, 
    HWND_MsgOutput = 2, 
    Ptr_MsgOutput = 3
  } ChatParts;
  
  [
    uuid(2ABB30A9-94E3-4085-BED5-4561F62E36EF), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
        \interface IExodusMenuListener
        \brief Defines method firing event when menu item is selected.
  **/
   interface IExodusMenuListener: IDispatch
  {
    /**
        \brief This method is called when a registered menu item is clicked.

        This method is called when a registered menu item is clicked.

        \param menuID The id of the menu item that has been clicked.
        \param xml The XML associated with the event.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000FF), 
    helpstring("Event fired when a registered menu item is selected")
    ]
    HRESULT _stdcall OnMenuItemClick([in] BSTR menuID, [in] BSTR xml );
  };

  [
    uuid(BDD5493D-440F-4376-802B-070B5A4ABFF3), 
    version(1.0), 
    helpstring("Dispatch interface for ExodusRosterItem Object"), 
    dual, 
    oleautomation
  ]
   /**  \interface IExodusRosterItem
        \brief Methods for Roster Items.
    
        Encapsulates Roster Items.
   **/

   interface IExodusRosterItem: IDispatch
  {
  	/**
    	\brief This method is the getter for the roster item's JID.
        
        This method allows access to the JID of this roster item.

        \param value Receives the roster item's full JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000001)
    ]
    HRESULT _stdcall JabberID([out, retval] BSTR * value );
    /**
       \brief This method is a getter for the subscription member.

       This methods returns the value of subscription for this roster item.  The value returned should be a valid subscription state or blank:
       \li To
       \li From
       \li Both
       \li '' (blank if not set)

       \param value Receives the value representing the roster item's subscription.
       \return \c HRESULT either \c S_OK or an error value.
     **/
    [
    propget,
    id(0x00000002)
    ]
    ;!
    HRESULT _stdcall Subscription([out, retval] BSTR * value );
    /**
        \brief This method is the setter for the subscription value.

        This method sets the subscription state for this roster item.  
        \n\b Note: Call Update to send new roster item to the server.

        \param value The new value of the subscription.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000002)
    ]
    HRESULT _stdcall Subscription([in] BSTR value );
    /**
        \brief This method is the getter for the ask value of this roster item.

        This method returns the value of the ask attribute (subscribe or blank if not set).

        \param value Receives the value of the roster item's ask attribute.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000004)
    ]
    HRESULT _stdcall Ask([out, retval] BSTR * value );
    /**
        \brief This method counts the number of groups this roster item is in.
        
        This method counts the number of groups this roster item is in.

        \param value Receives the number of groups this item is in.
        \return \c HRESULT either \c S_OK or an error value.                   
    **/
    [
    propget,
    id(0x00000005)
    ]
    HRESULT _stdcall GroupCount([out, retval] long * value );
    /**
        \brief This method finds the group name at the specified index.

        This method will return the group name at the specified index from the list of groups this item belongs to.

        \param index The index of the group.
        \param value Receives the group name at the specified index.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000006)
    ]
    HRESULT _stdcall Group([in] long index, [out, retval] BSTR * value );
    /**
        \brief This method creates an XML description of this Roster Item.
        
        This method creates an XML description of this Roster Item.

        \param xml Receives the XML representation of this roster item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000007)
    ]
    HRESULT _stdcall xml([out, retval] BSTR * xml );
    /**
        \brief This method removes this roster item from the roster.

        This method removes the item from the roster and fires a /roster/remove event with a remove element payload.  The same as setting Subscription and calling Update.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000008)
    ]
    HRESULT _stdcall Remove( void );
    /**
        \brief This method updates the roster item.

        This method fires a /roster/item event with a update element payload.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000009)
    ]
    HRESULT _stdcall Update( void );
    /**
        \brief This method accesses the nickname of this roster item.
        
        This method accesses the nickname of this roster item.

        \param value Receives the roster item's nickname.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x0000000A)
    ]
    HRESULT _stdcall nickname([out, retval] BSTR * value );
    /**
        \brief This method sets the nickname of this roster item.

        This method sets the nickname of this roster item.
        \b Note: Call Update to send new roster item to the server.

        \param value The new nickname.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x0000000A)
    ]
    HRESULT _stdcall nickname([in] BSTR value );
    /**
        \brief This method gets the nickname of this roster item.

        This method gets the nickname of this roster item.
        
        \param value Receives the new nickname.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x0000000B)
    ]
    HRESULT _stdcall RawNickname([out, retval] BSTR * value );
    /**
        \brief This method gets the menu id associated with this roster item.

        This method gets the menu id associated with this roster item.  This 
        method may return empty string if no context menu is associated with 
        this item.

        \param value Receives the value of menu id.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000C9)
    ]
    HRESULT _stdcall ContextMenuID([out, retval] BSTR * value );
    /**
        \brief This method sets the value of the menu id.

        This method not only sets the member menu id, but sets the 
        \c TJabberRosterItem's custom context menu.

        \param value The new id of the desired menu item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000C9)
    ]
    HRESULT _stdcall ContextMenuID([in] BSTR value );
    /**
        \brief This method accesses the status of this roster item.

        This is the value of the staus element of the entities' presence.

        \param value Receives the item's presence.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000CA)
    ]
    HRESULT _stdcall status([out, retval] BSTR * value );
    /**
        \brief This method sets the status of this roster item.

        This method sets the status of this roster item.
        \b Note this doesn't send protocol, only changes the value on this object.

        \param  value The new status.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000CA)
    ]
    HRESULT _stdcall status([in] BSTR value );
    /**
        \brief This method returns the tooltip associated with this roster item.

        This method returns the tooltip associated with this roster item.
        
        \param value Receives the tooltip text.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000CB)
    ]
    HRESULT _stdcall Tooltip([out, retval] BSTR * value );
    /**
        \brief This method sets the tooltip associated with this roster item.

        This method sets the tooltip associated with this roster item.
        
        \param value The new tooltip.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000CB)
    ]
    HRESULT _stdcall Tooltip([in] BSTR value );
    /**
        \brief This method access the value of the double-click action.

        The value of action is the name of the event fired when this roster
        item is double clicked. Typically the value is '/session/gui/contact'.

        \param value Receives the value of action.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000CC)
    ]
    HRESULT _stdcall action([out, retval] BSTR * value );
    /**
        \brief Sets the double click action event name.

        The value of action should be a valid event name. Typically it is '/session/gui/contact'.

        \param value The new value for action.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000CC)
    ]
    HRESULT _stdcall action([in] BSTR value );
    /**
        \brief This method accesses the roster item's image index.

        The image index determines the image displayed by this item in the roster. 
        The image index is determined from the value of the show element of this 
        item's presence.  The IExodusRosterImages class is responsible for maintaining 
        mappings between the images and the show values - see IExodusRosterImages::Find.

        \param value Receives the value of the image's index.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000CD)
    ]
    HRESULT _stdcall imageIndex([out, retval] long * value );
    /**
        \brief This method sets the roster item's image index.

        The image index determines the image displayed by this item in the roster.
        Normally this item is determined from mappings of show element values to 
        images kept by the IExodusRosterImages class - see IExodusRosterImages::Find.

        \param value The new image index to be set.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000CD)
    ]
    HRESULT _stdcall imageIndex([in] long value );
    /**
        \brief Allows the caller to determine if the roster item's caption is editable.

        A true value indicates the caption is editable.

        \param value Recieves the value of the editable property.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000CE)
    ]
    HRESULT _stdcall InlineEdit([out, retval] VARIANT_BOOL * value );
    /**
        \brief Allows caller to mark the roster item's caption as readonly or writable.

        A true value indicates the caption is editable.

        \param value The new value for the property.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000CE)
    ]
    HRESULT _stdcall InlineEdit([in] VARIANT_BOOL value );
    /**
        \brief Fires a /roster/item event signalling a change to the roster item.

        This method should be called whenever a change is made to the roster
        item that requires a UI update.
    
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CF)
    ]
    HRESULT _stdcall FireChange( void );
    /**
        \brief  Indicates if this roster item is a contact.

        Not all roster items are contacts. Use this method to tell.

        \param value Receives the properties' value.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000D0)
    ]
    HRESULT _stdcall IsContact([out, retval] VARIANT_BOOL * value );
    /**
        \brief Set to indicate if this roster item is a contact.

        Set to indicate if this roster item is a contact.
        
        \param value The new value to set.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000D0)
    ]
    HRESULT _stdcall IsContact([in] VARIANT_BOOL value );
    /**
        \brief Adds this roster item to the new group.

        This method adds this item to the specified group.  Update should
        be called after this method to notify the UI of the change.

        \param grp The group to add this item to.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D2)
    ]
    HRESULT _stdcall AddGroup([in] BSTR grp );
    /**
        \brief Removes this roster item from the named group.

        Removes this roster item from the named group.
        \b Note: Update should be called after this method to notify the UI the change.

        \param grp The group to remove this item from.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D3)
    ]
    HRESULT _stdcall RemoveGroup([in] BSTR grp );
    /**
        \brief Updates any pending changes the groups this roster item belongs to.
        
        This applies updates to the internal roster item's data store. It does
        not cause a UI update.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D4)
    ]
    HRESULT _stdcall SetCleanGroups( void );
    /**
        \brief Accesses the imageprefix of this roster item.

        The image prefix is prepended to the name of the show value set from 
        the item's presence to determine which image is selected for display.
        See IExodusRosterImages.

        \param value Receives the value of the image prefix.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000D1)
    ]
    HRESULT _stdcall ImagePrefix([out, retval] BSTR * value );
    /**
        \brief Sets the image prefix for this roster item.

        The image prefix is prepended to the name of the show value set from 
        the item's presence to determine which image is selected for display.
        See IExodusRosterImages.

        \param value The new value for image prefix.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000D1)
    ]
    HRESULT _stdcall ImagePrefix([in] BSTR value );
    /**
        \brief Sets the IsNative property - reserved for future use.
       
        This is reserved for future use.

        \param value Receives the current value of this property.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000D5)
    ]
    HRESULT _stdcall IsNative([out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets the IsNative property - reserved for future use.

        This is reserved for future use.

        \param value The new value for IsNative.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000D5)
    ]
    HRESULT _stdcall IsNative([in] VARIANT_BOOL value );
    /**
        \brief Indicates if this roster item can receive offline messages.

        Indicates if this roster item can receive offline messages.
        True indicates the contact can receive offline messages.

        \param value Receives the current property value.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000D6)
    ]
    HRESULT _stdcall CanOffline([out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets the property indicating this contact can receive offline messages.

        Sets the property indicating this contact can receive offline messages.
        
        \param value The new value of this property.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x000000D6)
    ]
    HRESULT _stdcall CanOffline([in] VARIANT_BOOL value );
  };

  [
    uuid(FF4EFE7E-35AC-48B5-ACDB-6753C402F0DB), 
    version(1.0), 
    helpstring("Dispatch interface for ExodusPresence Object"), 
    dual, 
    oleautomation
  ]
   /**\interface IExodusPresence
    \brief Encapsulates a presence packet.
   
    This interface has methods for accessing and setting fields on a presence 
    packet.
   **/

   interface IExodusPresence: IDispatch
  {
    /**
        \brief Gets the presence type for this presence.

        This method gets the presence type for this presence. This is the type 
        attribute on the presence packet.  Per RFC 3921 valid values may be:
        \li unavailable
        \li subscribe
        \li subscribed
        \li unsubscribe
        \li unsubscribed
        \li probe
        \li error

        \param value Recieves the type of this presence.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000001)
    ]
    HRESULT _stdcall PresType([out, retval] BSTR * value );
    /**
        \brief Sets the presence type of this presence.

        This method sets the type attribue of the presence packet. 
        See PresType(BSTR*) for more information.

        \param value The value of the new type attribute.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000001)
    ]
    HRESULT _stdcall PresType([in] BSTR value );
    /**
        \brief Retreives the status of this presence.

        This method retrieves the value of this presence's status element.  
        Status is a natural language description of the show value (see Show). 
        The status may be localized or custom (i.e. it is not standarized) and 
        \e should not be nil - but this is NOT prevented.

        \param value Recieves the status, blank, or possibly nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000002)
    ]
    HRESULT _stdcall status([out, retval] BSTR * value );
    /**
        \brief Sets the status of this presence.

        Sets the status element's value. The value is a natural language 
        description of the show value (see Show).  The value is free form but 
        the best practice is to not set it to nil.

        \param value The new value of the presence's status.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000002)
    ]
    HRESULT _stdcall status([in] BSTR value );
    /**
        \brief Retrieves the value of the presence's show element.

        The show element is optional and indicates the JID's availability in a
        number of preset states (per RFC 3921):
        \li away -- The JID is temporarily away.
        \li chat -- The JID is actively interested in chatting.
        \li dnd  -- The JID is busy ("Do Not Disturb").
        \li xa   -- The JID is away for an extended period ("eXtended Away").

        This value \e should not be nil, but the setter does NOT prevent this.

        \param value away, chat, dnd, xa, '' if none, or possibly nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000003)
    ]
    HRESULT _stdcall show([out, retval] BSTR * value );
    /**
        \brief Sets the value of the presence's show element.

        The show element is optional and indicates the JID's availability in a
        number of preset states (per RFC 3921):
        \li away -- The JID is temporarily away.
        \li chat -- The JID is actively interested in chatting.
        \li dnd  -- The JID is busy ("Do Not Disturb").
        \li xa   -- The JID is away for an extended period ("eXtended Away").

        Best practices should be to not set this to nil (use blank instead), 
        but this is not prevented.

        \param value The new value of show (away, chat, dnd, xa).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000003)
    ]
    HRESULT _stdcall show([in] BSTR value );
    /**
        \brief Retrieves the priority of this presence.

        Priority is an optional element and may be an integer between 
        -128 and +127.  Higher numbers indicate servers give more priority
        to this JID when routing stanzas.  The default is 0.

        \param value Receives the priorty of this presence (-128 to +127).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000004)
    ]
    HRESULT _stdcall priority([out, retval] long * value );
    /**
        \brief Sets the priorty of this presence.

        Priority is an optional element and may be an integer between 
        -128 and +127.  Higher numbers indicate servers give more priority
        to this JID when routing stanzas.  The default is 0.

        \param value the new priorty value (-128 to +127).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000004)
    ]
    HRESULT _stdcall priority([in] long value );
    /**
        \brief Retrieves the error code for this presence.

        If the presence packet contained an error code this method retrieves it.
        value \e should not be nil, but ErrorString(BSTR) does not prevent this.

        \n Error stanzas and codes are defined in RFC 3920 and JEP-0086.
        
        \n \b Note: Exodus will also fire events when presence packets with an error 
        stanza are received:
            \li Error codes 500, 502, 504 - Exodus assumes this is an invalid 
            roster item and the following events are fired:
            '/session/error/presence' and '/presence/error'
           
           \li Error code 400, only with a blank from attribute, is considered 
           invisible packet bouncing and MainSession presence set to Available.

           \li All Other Error codes result in Exodus firing a '/presence/error' 
           event.

        \param value Receives the error code or blank if none.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000005)
    ]
    HRESULT _stdcall ErrorString([out, retval] BSTR * value );
    /**
        \brief Sets an error code for this presence.

        The value should be as defined in RFC 3920 and JEP-0086, but are 
        essentially HTTP codes.

        \param value The value to set the code to.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000005)
    ]
    HRESULT _stdcall ErrorString([in] BSTR value );
    /**
        \brief Creates and XML representation of this presence.

        This method returns the XML representation of this presence.

        \param xml Receives the XML for this presence packet.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000006)
    ]
    HRESULT _stdcall xml([out, retval] BSTR * xml );
    /**
        \brief Determines if this presence packet affects subscription state.

        This method will return True if the type (see PresType(BSTR*)) is one of:
        \li subscribe
        \li subscribed
        \li unsubscribe
        \li unsubscribed

        \param subscription True if this presence deals with subscription state.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000007)
    ]
    HRESULT _stdcall IsSubscription([out, retval] VARIANT_BOOL * subscription );
    /**
        \brief Returns the JID from the presence packet's to attribute.

        This method returns the JID from presence packet's to attribute. This 
        \e should not return nil, but the setter does NOT prevent this 
        (see toJID(BSTR*)).

        \param value Receives the JID this presence was directed to.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000008)
    ]
    HRESULT _stdcall ToJid([out, retval] BSTR * value );
    /**
        \brief Sets the JID this presence should be directed to.

        This method sets the to attribute of this presence packet. It determines
        to whom the packet is sent.  This value should NOT be blank or set to nil.

        \param value The JID that should receive this presence: should not be blank or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000008)
    ]
    HRESULT _stdcall ToJid([in] BSTR value );
    /**
        \brief Retrieves the JID that sent this presence packet.

        This method retrieves the JID that sent the presence packet. Determined
        by examining the from attribute of the presence packet.  This should
        not be null or blank, but the setter does not prevent this (see fromJid(BSTR*)).

        \param value Receives the JID sending this presence: should not be blank or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000009)
    ]
    HRESULT _stdcall FromJid([out, retval] BSTR * value );
    /**
        \brief Sets the from attribute of this presence.

        This method sets the from attribute of the presence packet.  Best 
        practice is to never set this to blank or NIL, but this is allowed.
        \n\b Note: Servers may ignore or modify this attribute to avoid spoofing.

        \param value The JID to whom this presence belongs.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x00000009)
    ]
    HRESULT _stdcall FromJid([in] BSTR value );
  };

  [
    uuid(1E010B88-4A61-4185-A243-A6283A26C80E),
    version(1.0)
  ]
  typedef enum tagActiveItem
  {
    RosterItem = 0,
    Bookmark = 1,
    Group = 2
  } ActiveItem;

  [
    uuid(BFE1905C-3620-4C9D-B0C2-27EB456EF73B), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusAuth
    \brief Interface for Authentication Plugins.
    
    This is the interface for plugins that handle different kinds of authentication.
  **/

   interface IExodusAuth: IDispatch
  {
    /**
        \brief Starting Authentication.

        Starting the Authentication Process.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000001)
    ]
    HRESULT _stdcall StartAuth( void );
    /**
        \brief Cancel an in-process authentication.

        Cancel an in-process authentication.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000002)
    ]
    HRESULT _stdcall CancelAuth( void );
    /**
        \brief Start the registration process.

        Start the registration process.

        \param regSupported True if registration is supported, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000003)
    ]
    HRESULT _stdcall StartRegistration([out, retval] VARIANT_BOOL * regSupported );
    /**
        \brief Cancel an in-process registration.

        Cancel an in-process registration.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000004)
    ]
    HRESULT _stdcall CancelRegistration( void );
  };

  [
    uuid(F4AAF511-D144-42E7-B108-8A196D4BD115), 
    version(1.0), 
    helpstring("Dispatch interface for ExodusRosterImages Object"), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusRosterImages
    \brief This interface defines how to work with images used by roster items.

    This interface allows plugins to add, remove, and find roster item images.
    All images are identified by an id from which they can be found. Typically
    this id is the show element's value taken from the presence packet.
  **/

   interface IExodusRosterImages: IDispatch
  {
    /**
        \brief Loads a roster image from the filesystem.

        This method will load a roster image from the filesystem.  This method
        will not replace an existing id so use Remove first to override existing ids.
        The return value is the image's index in roster's image store.

        \param id The identifier for this image - usually one of away, chat, dnd, xa.
        \param filename The path and filename to the image being loaded.
        \param index The image index assigned by the internal data store or -1 for failure.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall AddImageFilename([in] BSTR id, [in] BSTR filename, [out, retval] long * index );
    /**
        \brief Loads a roster image from a base64 encoded string.

        This method will decoded a base64 encoded image. This method will not replace
        and existing id so user Remove first to override existing ids.

        \param id The identifier for this image - usually one of away, chat, dnd, xa.
        \param base64 The base64 encoded image data to load.
        \param index The image index assigned by the internal data store or -1 for failure.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall AddImageBase64([in] BSTR id, [in] BSTR base64, [out, retval] long * index );
    /**
        \brief Loads a roster image from a resource (such as a DLL).

        This method will attempt to peform a LoadLibrary call on the specified 
        resource.  If successful, an attempt to load the named resource will be
        made. This method will not replace and existing id so user Remove first 
        to override existing ids.

        \param id The identifier for this image - usually one of away, chat, dnd, xa.
        \param libName The name (optionally including path) of the resource to load the image from.
        \param resName The name of the resource specific to it's library.
        \param index The image index assigned by the internal data store or -1 for failure.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall AddImageResource([in] BSTR id, [in] BSTR libName, [in] BSTR resName, [out, retval] long * index );
    /**
        \brief Removes the specified roster image.

        The roster image with the specified id is removed from the roster's 
        internal image store.

        \param id The id of the image to remove - usually one of away, chat, dnd, xa.
        \return \c HRESULT either \c S_OK or an error value.
   **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall Remove([in] BSTR id );
    /**
        \brief Finds a roster image by id.

        This method returns the index of of the specified image in the roster's
        internal image store or -1 if the id wasn't found.

        \param id A \c BSTR representing the ide of the image to find.
        \param index The index of the image or -1.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CD)
    ]
    HRESULT _stdcall Find([in] BSTR id, [out, retval] long * index );
  };

  [
    uuid(D8297D0C-A316-4E9D-A89C-095CFAE51141),
    version(1.0),
    dual,
    oleautomation
  ]
    /**
        \interface IExodusControlFont
        \brief <b>UNSUPPORTED</b> - Font associated with an IExodusControl

        <b>UNSUPPORTED</b>

        This interface details a font used within IExodusControl controls.
        Methods are defined to accesses and modify the character set, color,
        height, pitch, size and font name (family) of the font.

        \since 0.9.1.1
    **/
   interface IExodusControlFont: IExodusControl
  {
    /**
        \brief Get the font's character set.

        Retrieve the font's character set.
        
        See Charset(long value) for a detailed explanation.

        \param value The current character set identifier.
        \return S_OK if successful, or an error value otherwise.
        \see #Name
    **/
    [
    propget,
    id(0x00000001)
    ]
    HRESULT _stdcall Charset([out, retval] long * value );
    /**
        \brief Set the font's character set.

        Specify the font's character set.

        Each typeface (specified by Name(BSTR value)) supports one or more
        character sets.\n
        Check the information supplied by the font vendor to
        determine what possible values of the character set might be.

        \param value The new character set identifier.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x00000001)
    ]
    HRESULT _stdcall Charset([in] long value );
    [
    propget,
    id(0x00000002)
    ]
    /**
        \brief Get the font's color.

        Retrieve the font's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The font's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput,
    id(0x00000002)
    ]
    /**
        \brief Set the font's color.

        Specify the font's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the font's color will remain unchanged.

        \param value The font's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    /**
        \brief Get the font's height.

        Retrieve the height of the font in pixels.

        If \p value is
        negative, it only specifys the font height and does not include
        the internal leading that appears at the top of each line of text.
        If \p value is positive, it includes the height of the characters
        plus the internal leading.

        \param value The font's current height.
        \return S_OK if successful, or an error value otherwise.
        \see #Size
    **/
    [
    propget,
    id(0x00000003)
    ]
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput,
    id(0x00000003)
    ]
    /**
        \brief Set the font's height.

        Specify the height of the font in pixels.

        If \p value is
        negative, it will only specify the font height and the internal leading
        that appears at the top of each line of text is not measured.
        If \p value is positive, it will specify the height of the characters
        plus the internal leading.

        To specify the size of the font in points, use Size(long value).
        Users usually specify font size in points, while applications may be
        concerned with the actual size of the font in pixels when displaying
        the font on the screen.

        \param value The font's new height.
        \return S_OK if successful, or an error value otherwise.
        \see #Size
    **/
    HRESULT _stdcall Height([in] long value );
    /**
        \brief Get the font family.

        Retrieve the font family.

        \p value should have at least 256 bytes allocated.

        \param value The current font family.
        \return S_OK if successful, or an error value otherwise.
        \see #Charset, #Pitch, #Size
    **/
    [
    propget,
    id(0x00000004)
    ]
    HRESULT _stdcall name([out, retval] BSTR * value );
    /**
        \brief Set the font family.

        Specify the font family (typeface) of the font.

        If the font family
        described by \p value includes multiple character sets, be sure to set the
        Charset property as well.\n
        If the combination of font family (typeface) and attributes
        (such as bold or italic) specifies a font that is not available on
        the system, the system substitutes a different font.

        \param value The new font family.
        \return S_OK if successful, or an error value otherwise.
        \see #Charset, #Pitch, #Size
    **/
    [
    propput,
    id(0x00000004)
    ]
    HRESULT _stdcall name([in] BSTR value );
    /**
        \brief Get the font's pitch.

        Retrieve the font's pitch.

        Each typeface has an associated pitch. The characters in fixed-pitch
        fonts are all the same width. The characters in variable-pitch fonts
        differ in width. Fixed pitch fonts are generally used for representing
        source code, because they allow the indentation to line up properly.
        Variable pitch fonts are generally used to give text a more natural
        appearance.

        \p value will be one of the following:
        \li 0 default pitch of the font family
        \li 1 variable-pitch
        \li 2 fixed-pitch

        \param value The font's current pitch.
        \return S_OK if successful, or an error value otherwise.
        \see Name
    **/
    [
    propget,
    id(0x00000005)
    ]
    HRESULT _stdcall Pitch([out, retval] long * value );
    /**
        \brief Set the font's pitch.

        Specify the font's pitch.

        See Pitch(long * value) for additonal information.

        Setting the Pitch of a fixed-pitch font to variable or a
        variable-pitch font to fixed will cause window system to find what it
        considers the best match to all of the font characteristics that have
        been specified. This might have no effect on the appearance of a font,
        or might cause the window system to substitute a different font with the
        appropriate pitch. For example, setting the pitch of Serif
        (a variable-pitch font) to fixed causes Courier to be displayed.

        Setting \p value to anything other than 0 (default), 1 (variable) or 2 (fixed)
        will be ignored and the font's pitch will remain unchanged..

        \param value The font's new pitch.
        \return S_OK if successful, or an error value otherwise.
        \see Name
    **/
    [
    propput,
    id(0x00000005)
    ]
    HRESULT _stdcall Pitch([in] long value );
    /**
        \brief Get the font's size.

        Retrieve the font's size in points.

        See Size(long value) for a detailed explanation.

        \param value The font's current size.
        \return S_OK if successful, or an error value otherwise.
        \see Height
    **/
    [
    propget,
    id(0x00000006)
    ]
    HRESULT _stdcall Size([out, retval] long * value );
    /**
        \brief Set the font's size.

        Specify the point size of the font.

        If \p value is negative,
        the internal leading that appears at the top of each line of text is
        included. If the \p value is positive, \p value represents the height of the
        characters but not the internal leading.

        \param value The font's new size.
        \return S_OK if successful, or an error value otherwise.
        \see Height
    **/
    [
    propput,
    id(0x00000006)
    ]
    HRESULT _stdcall Size([in] long value );
  };

  [
    uuid(EFBC071A-460A-4E1B-89EC-25B23460BA93),
    version(1.0),
    dual,
    oleautomation
  ]
  /**
    \interface IExodusControlMenuItem
    \brief <b>UNSUPPORTED</b> - Describes the properties of an item in a menu.

    <b>UNSUPPORTED</b>

    Use this interface to specify the appearance and behavior of an
    item in a menu. Each IExodusControlMainMenu or IExodusControlPopupMenu
    object can contain multiple menu items

    \since 0.9.1.1
  **/

   interface IExodusControlMenuItem: IExodusControl
  {
    [
    propget,
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall name([out, retval] BSTR * value );
    [
    propput,
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.
         
        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall name([in] BSTR value );
    [
    propget,
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput,
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget,
    id(0x00000003)
    ]
    /**
        \brief Get auto check property.

        Retrieve the auto check property.

        \p value will either be 0 (FALSE) or 1(TRUE).
        See AutoCheck(long value) for additional information.

        \param value The current auto check property.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoCheck([out, retval] long * value );
    [
    propput,
    id(0x00000003)
    ]
    /**
        \brief Set the state of auto check functionality

        Specify the auto checked property.

        \c value must be either 0 (FALSE) or 1(TRUE). All other values will
        be ignored and the auto check functionality will be unchanged.

        When \p value is true, then every time the menu item is clicked, the
        value of the Checked property toggles automatically before any events
        are fired. When \p value is false, the application must explicitly set
        the value of the Checked property.

        \param value The new auto checked property.
        \return S_OK if successful, or an error value otherwise.
        \see Checked
    **/
    HRESULT _stdcall AutoCheck([in] long value );
    [
    propget,
    id(0x00000004)
    ]
    /**
        \brief Get whether the accelerator keys for menu items can be reset automatically.

        Retrieve whether the accelerator keys for menu items can be reset automatically.

				Use AutoHotkeys to indicate whether the menu should automatically ensure that accelerator keys for its items are reset if necessary so that every menu item has an accelerator key and no two items have the same accelerator key. This ensures that menu items added dynamically at runtime can have accelerator keys that do not introduce conflicts. TMenu tries to preserve all existing accelerator key mappings when adjusting the accelerator keys, but may be forced to change some in order to ensure that every item has a unique accelerator.
				 
				Before the menu is displayed, the menu checks and adjusts the accelerator key mappings if AutoHotkeys is maAutomatic. When AutoHotkeys is maManual, the menu does not alter the top-level menu items, but may adjust the accelerator keys for submenus, depending on the AutoHotkeys property of the menu item that is the parent of the submenu.
				 
        \p value can be:
        \li 0 - Automatic
        \li 1 - Manual
        
        \param value Receives the value of AutoHotKeys.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoHotkeys([out, retval] long * value );
    [
    propput,
    id(0x00000004)
    ]
    /**
        \brief Set whether the accelerator keys for menu items can be reset automatically.

        Specifies whether the accelerator keys for menu items can be reset automatically.

				Use AutoHotkeys to indicate whether the menu should automatically ensure that accelerator keys for its items are reset if necessary so that every menu item has an accelerator key and no two items have the same accelerator key. This ensures that menu items added dynamically at runtime can have accelerator keys that do not introduce conflicts. TMenu tries to preserve all existing accelerator key mappings when adjusting the accelerator keys, but may be forced to change some in order to ensure that every item has a unique accelerator.
				 
				Before the menu is displayed, the menu checks and adjusts the accelerator key mappings if AutoHotkeys is maAutomatic. When AutoHotkeys is maManual, the menu does not alter the top-level menu items, but may adjust the accelerator keys for submenus, depending on the AutoHotkeys property of the menu item that is the parent of the submenu.
				 
        \p value can be:
        \li 0 - Automatic
        \li 1 - Manual
        
        \param value Sets the value of AutoHotKeys.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoHotkeys([in] long value );
    [
    propget,
    id(0x00000005)
    ]
    /**
        \brief Get whether redundant separator bars are automatically removed from the menu.

        Retrieve whether redundant separator bars are automatically removed from the menu.

		Use AutoLineReduction to indicate whether the menu should automatically ensure that the menu does not start or end with a separator bar and that two separator bars do not appear next to each other. This ensures that menus built dynamically at runtime do not have separator bars in undesired positions.
 
		Before the menu is displayed, it removes superfluous separator bars when AutoLineReduction is maAutomatic. When AutoLineReduction is maManual, the menu does not remove separators from the top-level menu items, but may remove superfluous separators from submenus, depending on the AutoLineReduction property of the menu item that is the parent of the submenu.				
        
        \p value can be:
        \li 0 - automatic
        \li 1 - manual
        
        \param value Receives the value of AutoLineReduction.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoLineReduction([out, retval] long * value );
    [
    propput,
    id(0x00000005)
    ]
    /**
        \brief Set whether redundant separator bars are automatically removed from the menu.

        Specifies whether redundant separator bars are automatically removed from the menu.

		Use AutoLineReduction to indicate whether the menu should automatically ensure that the menu does not start or end with a separator bar and that two separator bars do not appear next to each other. This ensures that menus built dynamically at runtime do not have separator bars in undesired positions.
 
		Before the menu is displayed, it removes superfluous separator bars when AutoLineReduction is maAutomatic. When AutoLineReduction is maManual, the menu does not remove separators from the top-level menu items, but may remove superfluous separators from submenus, depending on the AutoLineReduction property of the menu item that is the parent of the submenu.				
        
        \p value can be:
        \li 0 - automatic
        \li 1 - manual
        
        \param value Sets the value of AutoLineReduction.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoLineReduction([in] long value );
    [
    propget,
    id(0x00000006)
    ]
    /**
        \brief Get whether the menu item starts a new column in the menu.

        Retreive whether the menu item starts a new column in the menu.

				Set Break to break a long menu into columns at the item described by this menu item. The value of Break indicates whether the menu is broken into columns, and if so, whether a bar appears between the columns of the parent menu.
        
        \p value can be:
        \li 0 - none
        \li 1 - break
        \li 2 - bar break
        
        \param value Receives the value of Break (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Break([out, retval] long * value );
    [
    propput,
    id(0x00000006)
    ]
    /**
        \brief Set whether the menu item starts a new column in the menu.

        Specifies whether the menu item starts a new column in the menu.

				Set Break to break a long menu into columns at the item described by this menu item. The value of Break indicates whether the menu is broken into columns, and if so, whether a bar appears between the columns of the parent menu.
        
        \p value can be:
        \li 0 - none
        \li 1 - break
        \li 2 - bar break
        
        \param value Sets the value of Break (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Break([in] long value );
    [
    propget,
    id(0x00000007)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall caption([out, retval] BSTR * value );
    [
    propput,
    id(0x00000007)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall caption([in] BSTR value );
    [
    propget,
    id(0x00000008)
    ]
    /**
        \brief Get whether a check mark should appear beside the Caption.

        Retreive whether a check mark should appear beside the Caption.

				Use Checked to allow a menu item to work like a check box. If Checked is true, the menu item appears checked. If Checked is false, the menu item remains unchecked.
        
        \p value is 1 (TRUE) or 0 (FALSE)
        
        \param value Receives the value of Checked.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Checked([out, retval] long * value );
    [
    propput,
    id(0x00000008)
    ]
    /**
        \brief Set whether a check mark should appear beside the Caption.

        Specified whether a check mark should appear beside the Caption.

				Use Checked to allow a menu item to work like a check box. If Checked is true, the menu item appears checked. If Checked is false, the menu item remains unchecked.
        
        \p value is 1 (TRUE) or 0 (FALSE)
        
        \param value Sets the value of Checked.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Checked([in] long value );
    [
    propget,
    id(0x00000009)
    ]
    /**
        \brief Get whether the menu item is invoked when the parent item is double clicked.

        Retreive whether the menu item is invoked when the parent item is double clicked.

				Set Default to specify whether a menu item is the default item in a submenu. Default menu items are executed when the parent item is double-clicked, allowing users to avoid navigating the submenus that contain them. 

				The default item appears in boldface. A submenu can have only one default item. If the Items list of the parent menu item already contains a default menu item, setting the Default property to true sets the Default property of the previous default item to false.        
				
        \p value is 1 (TRUE) or 0 (FALSE)
        
        \param value Receives the value of Default.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Default([out, retval] long * value );
    [
    propput,
    id(0x00000009)
    ]
    /**
        \brief Set whether the menu item is invoked when the parent item is double clicked.

        Specifies whether the menu item is invoked when the parent item is double clicked.

				Set Default to specify whether a menu item is the default item in a submenu. Default menu items are executed when the parent item is double-clicked, allowing users to avoid navigating the submenus that contain them. 

				The default item appears in boldface. A submenu can have only one default item. If the Items list of the parent menu item already contains a default menu item, setting the Default property to true sets the Default property of the previous default item to false.        
				
        \p value is 1 (TRUE) or 0 (FALSE)
        
        \param value Sets the value of Default.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Default([in] long value );
    [
    propget,
    id(0x0000000A)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput,
    id(0x0000000A)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget,
    id(0x0000000B)
    ]
    /**
        \brief Get the Identity of  the logical group to which the menu item belongs.

        Retreive the Identity of  the logical group to which the menu item belongs.

				Use GroupIndex to control how menus are merged. In applications that contain multiple forms, items from the menus of secondary forms can be merged into the application's main menu as those forms become active. MDI applications always merge the menus of child windows with the main menu of the parent window. 
				
        \param value Receives the value of the group index.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall GroupIndex([out, retval] long * value );
    [
    propput,
    id(0x0000000B)
    ]
    /**
        \brief Set the Identity of  the logical group to which the menu item belongs.

        Specifies the Identity of  the logical group to which the menu item belongs.

				Use GroupIndex to control how menus are merged. In applications that contain multiple forms, items from the menus of secondary forms can be merged into the application's main menu as those forms become active. MDI applications always merge the menus of child windows with the main menu of the parent window. 
				
        \param value Sets the value of the group index.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall GroupIndex([in] long value );
    [
    propget,
    id(0x0000000C)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * Vvlue );
    [
    propput,
    id(0x0000000C)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget,
    id(0x0000000D)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput,
    id(0x0000000D)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget,
    id(0x0000000E)
    ]
    HRESULT _stdcall imageIndex([out, retval] long * value );
    /**
        \brief Get which image maintained by the parent menu appears next to the menu item.
        
        Receives which image maintained by the parent menu appears next to the menu item.

        Set ImageIndex to designate an image that should appear next to the caption when the menu item is drawn. On top-level menu items, ImageIndex specifies a zero-offset index into the Images property of the parent menu (TMenu or TPopupMenu). On submenu items, ImageIndex is the zero-offset index into the parent item's SubMenuImages property.

        \param value Receives the image index.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall imageIndex([out, retval] long * value );
    [
    propput,
    id(0x0000000E)
    ]
    HRESULT _stdcall imageIndex([in] long value );
    /**
        \brief Set which image maintained by the parent menu appears next to the menu item.
        
        Specifies which image maintained by the parent menu appears next to the menu item.

        Set ImageIndex to designate an image that should appear next to the caption when the menu item is drawn. On top-level menu items, ImageIndex specifies a zero-offset index into the Images property of the parent menu (TMenu or TPopupMenu). On submenu items, ImageIndex is the zero-offset index into the parent item's SubMenuImages property.

        \param value Setsthe image index.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall imageIndex([in] long value );
    [
    propget,
    id(0x0000000F)
    ]
    /**
        \brief Get whether the menu item is mutually exclusive with other menu items in its group.
        
        Receives whether the menu item is mutually exclusive with other menu items in its group.

        Use RadioItem to make the menu item behave like a radio button with a group of other menu items. The group of menu items is the set of all menu items in a pop-up or drop-down menu that have the same value of GroupIndex. When RadioItem is true, only one menu item in the group that contains the menu item can be checked at a time. The single selected menu item in the group is indicated by a round dot next to the Caption.

        \param value Receives the RadioItem state.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall RadioItem([out, retval] long * value );
    [
    propput,
    id(0x0000000F)
    ]
    /**
        \brief Set whether the menu item is mutually exclusive with other menu items in its group.
        
        Specifies whether the menu item is mutually exclusive with other menu items in its group.

        Use RadioItem to make the menu item behave like a radio button with a group of other menu items. The group of menu items is the set of all menu items in a pop-up or drop-down menu that have the same value of GroupIndex. When RadioItem is true, only one menu item in the group that contains the menu item can be checked at a time. The single selected menu item in the group is indicated by a round dot next to the Caption.

        \param value Sets the RadioItem state.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall RadioItem([in] long value );
    [
    propget,
    id(0x00000010)
    ]
    /**
        \brief Get the key combination users can type to quickly access the menu item.
        
        Receives the key combination users can type to quickly access the menu item.

        Use ShortCut to allow the user to type a key combination instead of selecting the item using the menu. This key combination appears to the right of the menu item in the menu.

        \param value Receives the ShortCut.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShortCut([out, retval] long * value );
    [
    propput,
    id(0x00000010)
    ]
    /**
        \brief Set the key combination users can type to quickly access the menu item.
        
        Specifies the key combination users can type to quickly access the menu item.

        Use ShortCut to allow the user to type a key combination instead of selecting the item using the menu. This key combination appears to the right of the menu item in the menu.

        \param value Sets the ShortCut.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShortCut([in] long value );
    [
    propget,
    id(0x00000011)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput,
    id(0x00000011)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
  };

  [
    uuid(31485408-E6E7-437D-A0FE-C176DF0D2CD8),
    version(1.0)
  ]
  /**
    \brief Enumeration of available IExodusControl types
  **/
  enum tagExodusControlTypes
  {
    ExodusControlButton = 0,
    ExodusControlCheckBox = 1,
    ExodusControlComboBox = 2,
    ExodusControlEdit = 3,
    ExodusControlFont = 4,
    ExodusControlLabel = 5,
    ExodusControlListBox = 6,
    ExodusControlMenuItem = 7,
    ExodusControlPanel = 8,
    ExodusControlPopupMenu = 9,
    ExodusControlRadioButton = 10,
    ExodusControlRichEdit = 11,
    ExodusControlBitBtn = 12,
    ExodusControlMainMenu = 13,
    ExodusControlMemo = 14,
    ExodusControlPageControl = 15,
    ExodusControlSpeedButton = 16,
    ExodusControlForm = 17
    //todo missing toolbar and toolbar buttons?
  } ExodusControlTypes;

  [
    uuid(57DFE494-4509-4972-A93B-6C7E6A9D6A59),
    version(1.0),
    dual,
    oleautomation
  ]
  /**\interface IExodusIQListener
    \brief A Listening inteface specifically for tracking IQ requests.

    A Listening inteface specifically for tracking IQ requests.
  **/

   interface IExodusIQListener: IDispatch
  {
    /**
        \brief IQ request has returned, needs processing.

        IQ request has returned, needs processing.

        \param handle Handle to IQ request.
        \param xml XML of the IQ request response.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall ProcessIQ([in] BSTR handle, [in] BSTR xml );
    /**
        \brief IQ request has timed out.

        IQ request has timed out.

        \param handle Handle of timed out IQ request.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall TimeoutIQ([in] BSTR handle );
  };

  [
    uuid(C9FEB6AF-32BE-4B47-984C-9DA11B4DF7A6),
    version(1.0),
    noncreatable,
    hidden
  ]
  coclass ExodusChat
  {
    [default] interface IExodusChat;
  };

  [
    uuid(027E1B53-59A9-4FA4-9610-AC6CA2561248),
    version(1.0),
    hidden
  ]
  coclass ExodusRoster
  {
    [default] interface IExodusRoster;
  };

  [
    uuid(9ED8C497-1121-4C9E-B586-C7DFDB35B581),
    version(1.0),
    hidden
  ]
  coclass ExodusPPDB
  {
    [default] interface IExodusPPDB;
  };

  [
    uuid(B39343ED-2E2D-4C91-AE4F-E0153BA347DA),
    version(1.0),
    hidden
  ]
  coclass ExodusRosterItem
  {
    [default] interface IExodusRosterItem;
  };

  [
    uuid(8B7DF610-B49C-4A90-9B98-CB0CB27D8827),
    version(1.0),
    hidden
  ]
  coclass ExodusPresence
  {
    [default] interface IExodusPresence;
  };

  [
    uuid(05237BC3-3093-4541-941D-A38FAFB78D89),
    version(1.0),
    hidden
  ]
  coclass ExodusRosterGroup
  {
    [default] interface IExodusRosterGroup;
  };

  [
    uuid(F0EA9081-9352-496D-94BA-E96605166527),
    version(1.0),
    hidden
  ]
  coclass ExodusRosterImages
  {
    [default] interface IExodusRosterImages;
  };

  [
    uuid(B777EA4A-A2A4-4597-87E2-E1B9800BFDC2),
    version(1.0),
    hidden
  ]
  coclass ExodusEntityCache
  {
    [default] interface IExodusEntityCache;
  };

  [
    uuid(F7D97ED8-C6BA-470F-8D63-7A6D70894AB3),
    version(1.0),
    noncreatable,
    hidden
  ]
  coclass ExodusEntity
  {
    [default] interface IExodusEntity;
  };

  [
    uuid(28132170-54E2-4BDD-A37D-BE115E68F044),
    version(1.0),
    dual, 
    oleautomation
  ]
  /**\interface IExodusListener
    \brief A basic listener to process events.
    
    A basic listener to process events.  
    Should be registered with IExodusController.
  **/

   interface IExodusListener: IDispatch
  {
    /**
        \brief Process an event for which this interface was registered.

        Process an event for which this interface was registered.

        \param event The event that is to be processed.
        \param xml XML associated with the event.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall ProcessEvent([in] BSTR event, [in] BSTR xml );
  };

  [
    uuid(D4749AC4-6EBE-493B-844C-0455FF0A4A77),
    version(1.0),
    dual,
    oleautomation
  ]
  /**\interface IExodusToolbarButton
    \brief Interface to the Exodus toolbar buttons.

    Interface to the Exodus toolbar buttons.
  **/

   interface IExodusToolbarButton: IDispatch
  {
    /**
        \brief Is the button visible.

        Is the button visible.

        \param value True if button is visible, false if not.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x000000C9)
    ]
    HRESULT _stdcall Visible([out, retval] VARIANT_BOOL * value );
    /**
        \brief Set visibility of button.

        Set visibility of button.

        \param value True to make button visible, False to make button invisible.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x000000C9)
    ]
    HRESULT _stdcall Visible([in] VARIANT_BOOL value );
    /**
        \brief Retreive the button tooltip.

        Retreive the button tooltip.

        \param value Receives the current button tooltip.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x000000CA)
    ]
    HRESULT _stdcall Tooltip([out, retval] BSTR * value );
    /**
        \brief Set the button tooltip.

        Set the button tooltip.

        \param value Tooltip to set for the button.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x000000CA)
    ]
    HRESULT _stdcall Tooltip([in] BSTR value );
    /**
        \brief Retreive the button ID.

        Retreive the button ID.

        \param value Receives the button ID.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x000000CB)
    ]
    HRESULT _stdcall imageID([out, retval] BSTR * value );
    /**
        \brief Set the image for the button.

        Set the image for the button.

        \param value ID value for the button image.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x000000CB)
    ]
    HRESULT _stdcall imageID([in] BSTR value );
    /**
        \brief Is the button enabled.

        Is the button enabled.

        \param value True if button is enabled, False if not.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x000000CC)
    ]
    HRESULT _stdcall Enabled([out, retval] VARIANT_BOOL * value );
    /**
        \brief Enable or disable button.

        Enable or disable button.

        \param value True to enable button, False to disable button.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x000000CC)
    ]
    HRESULT _stdcall Enabled([in] VARIANT_BOOL value );
  };

  [
    uuid(E12A4659-336B-4921-AC6A-771B1DCA5AF8),
    version(1.0),
    helpstring("ExodusToolbar Object")
  ]
  coclass ExodusToolbar
  {
    [default] interface IExodusToolbar;
  };

  [
    uuid(D29EB98A-994F-4E67-A12F-652733E7E5DD),
    version(1.0),
    helpstring("ExodusToolbarButton Object")
  ]
  coclass ExodusToolbarButton
  {
    [default] interface IExodusToolbarButton;
  };

  [
    uuid(2E945876-C2E5-4A24-98B4-0E38BD65D431),
    version(1.0),
    helpstring("Dispatch interface for ExodusLogMsg Object"),
    dual,
    oleautomation
  ]
  /**\interface IExodusLogMsg
    \brief A data structure for logging messages to persistant storage.

    A data structure containing all the data related to a message to be stored to persistant storage.
    Used by IExodusLogger.
  **/

   interface IExodusLogMsg: IDispatch
  {
    /**
        \brief The 'to' JID of the message to be logged.

        The 'to' JID of the message to be logged.

        \param value 'To' JID value
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000C9)
    ]
    HRESULT _stdcall ToJid([out, retval] BSTR * value );
    /**
        \brief The 'from' JID of the message to be logged.

        The 'from' JID of the message to be logged.

        \param value 'From' JID value
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CA)
    ]
    HRESULT _stdcall FromJid([out, retval] BSTR * value );
    /**
        \brief Gets the message's type.

        Gets the message's type.

        \param value Receives the message's type attribute.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CB)
    ]
    HRESULT _stdcall MsgType([out, retval] BSTR * value );
    /**
        \brief Message ID

        Message ID

        \param value Message ID
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CC)
    ]
    HRESULT _stdcall id([out, retval] BSTR * value );
    /**
        \brief Nickname associated with message to be logged.

        Nickname associated with message to be logged.

        \param value Nickname
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CD)
    ]
    HRESULT _stdcall Nick([out, retval] BSTR * value );
    /**
        \brief The body of the message to be logged.

        The body of the message to be logged.

        \param value Body data to be logged
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CE)
    ]
    HRESULT _stdcall body([out, retval] BSTR * value );
    /**
        \brief Conversation thread identifier.

        Conversation thread identifier.

        \param value Conversation thread ID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CF)
    ]
    HRESULT _stdcall thread([out, retval] BSTR * value );
    /**
        \brief Subject of the conversation for which message is to be logged.

        Subject of the conversation for which message is to be logged.

        \param value Subject
        \return \c HRESULT either \c S_OK or an error value.
    **/    
    [
    propget,
    id(0x000000D0)
    ]
    HRESULT _stdcall subject([out, retval] BSTR * value );
    /**
        \brief Timestamp for message to be logged.

        Timestamp for message to be logged.

        \param value Timestamp
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D1)
    ]
    HRESULT _stdcall Timestamp([out, retval] BSTR * value );
    /**
        \brief Was message sent to or from client.

        Was message sent to or from client.

        \param value Direction
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D2)
    ]
    HRESULT _stdcall Direction([out, retval] BSTR * value );
    /**
        \brief Raw XML of message to be logged.

        Raw XML of message to be logged.

        \param value XML
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D3)
    ]
    HRESULT _stdcall xml([out, retval] BSTR * value );
  };

  [
    uuid(740743C0-7BEF-48E8-BD05-1470047F03CA),
    version(1.0),
    helpstring("ExodusLogMsg Object")
  ]
  coclass ExodusLogMsg
  {
    [default] interface IExodusLogMsg;
  };

  [
    uuid(6D58A577-6BC4-4B1C-B5F8-759B94136B0A),
    version(1.0),
    helpstring("Dispatch interface for ExodusLogListener Object"),
    dual,
    oleautomation
  ]
  /**\interface IExodusLogListener
    \brief <b>UNSUPPORTED</b> - Return path for getting logged messages by date.

    <b>UNSUPPORTED INTERFACE</b>
    
    This interface is used by IExodusLogger as a return path for the
    GetDays and GetMessages calls.
  **/

   interface IExodusLogListener: IDispatch
  {
    /**
        \brief <b>UNSUPPORTED</b> - Process a chunk of archived messages.

        <b>UNSUPPORTED</b> - Process a chunk of archived messages.

        \param count Number of messages in array.
        \param messages Array of returned messages
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall ProcessMessages([in] long count, [in] SAFEARRAY(long) messages );
    /**
        \brief <b>UNSUPPORTED</b> - End of returning messages for a specific date.

        <b>UNSUPPORTED</b> - End of returning messages for a specific date.

        \param day Day of Messages
        \param month Month of Messages
        \param year Year of Messages
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall EndMessages([in] long day, [in] long month, [in] long year );
    /**
        \brief <b>UNSUPPORTED</b> Error in retrieving messages.

        <b>UNSUPPORTED</b> - Error in retrieving messages for specific date.

        \param day Day of Messages
        \param month Month of Messages
        \param year Year of Messages
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall Error([in] long day, [in] long month, [in] long year );
    /**
        \brief <b>UNSUPPORTED</b> - A list of dates with archived messages available.

        <b>UNSUPPORTED</b> - A list of dates with archived messages available.
        This is a list of dates within a specific Month and Year.

        \param count Number of dates in array.
        \param dates Array with valid dates for the queried month and year.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall ProcessDates([in] long count, [in] SAFEARRAY(long) dates );
  };

  [
    uuid(98ED888A-0569-4E5B-8933-36EBF08812B4),
    version(1.0),
    helpstring("ExodusLogListener Object")
  ]
  coclass ExodusLogListener
  {
    [default] interface IExodusLogListener;
  };


  typedef BSTR * PWideString1;

  [
    uuid(0BB5F3EB-D671-420F-93BA-61049B046912), 
    version(1.0),
    helpstring("Dispatch interface for ExodusController Object"),
    dual,
    oleautomation
  ]
  /**
    \interface IExodusController
    \brief Provides interface to control Exodus.

    "Where all the good stuff happens."
  **/
   interface IExodusController: IDispatch
  {
    /**
        \brief Indicates if the session is connected.

        This method indicates if the session is connected to the server.

        \param value True if connected.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000001)
    ]
    HRESULT _stdcall Connected([out, retval] VARIANT_BOOL * value );
    /**
        \brief Retreives the username portion of the profile's JID.

        This method gets the username portion of the current profile's JID. Under 
        normal conditions, this return value is never nil or blank (as long as 
        the profile is valid).

        \param value The username portion of the JID - never nil or blank.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000002)
    ]
    HRESULT _stdcall Username([out, retval] BSTR * value );
    /**
        \brief Retrieves the hostname portion of the profile's JID.

        This method gets the hostname portion of the current profile's JID.

        \param value Recieves the hostname portion of the profile's JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000003)
    ]
    HRESULT _stdcall Server([out, retval] BSTR * value );
    /**
        \brief Registers an event callback for a plugin.

        Register your plugin to receive packets which match a specific xpath-like 
        expression. The following are SAMPLES of some of the expressions you may use:
        \n /session - Catch all session related events.  In these cases there will be no stanza returned in the event.
        \n /packet/message[@type="headline"] - Catch all headline messages.
        \n /packet/iq[@id="my_id"] - Catch all iq packets which have this ID attribute.

        Note that exodus does not include a COMPLETE xpath parser. You may use 
        any variations of the samples show above. Note that you should use 
        /packet whenever attempting to catch incoming packets from the socket. 
        All of the "top level" strings which you may use are as follows:
            \li /session - Session and connection related details. Events will occur like /session/connected, /session/prefs, etc..
            \li /packet - Catch any generic packet.
            \li /roster - Catch new roster item pushes, and full roster results.
            \li /presence - Catch presence packets coming into the system.
            \li /windows - Catch windows events related to a specific window.
       
        \param xpat The xpath expression to use for this callback.
        \param callback the IExodusPlugin object which will receive events.
        \param ID The ID of the registered callback - save for use in UnRegisterCallback.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000004)
    ]
    HRESULT _stdcall RegisterCallback([in] BSTR xpath, [in] IExodusPlugin * callback /*Warning: unable to validate structure name: */, [out, retval] long * ID );
    /**
        \brief Unregisters a previously registered stream callback.

        This method unregisters a previously registered callback (see RegisterCallback).

        \param id The id returned when registering the callback.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000005)
    ]
    HRESULT _stdcall UnRegisterCallback([in] long id);
    /**
        \brief Sends the provided xml to the server.

        This method sends the xml directly to the server.

        \b Note: The xml must be well formed as Exodus does not enforce this. 
        If the xml is invalid the server will disconnect the session.

        \param xml The xml to send to the server.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000006)
    ]
    HRESULT _stdcall Send([in] BSTR xml );
    /**
        \brief Searches for the JID in the roster.

        This method will search the roster for the given JID. Will return True
        if the JID is found.

        \param jid The JID to find in the roster.
        \param value True if the JID is in the roster, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000007)
    ]
    HRESULT _stdcall IsRosterJID([in] BSTR jid, [out, retval] VARIANT_BOOL * value );
    /**
        \brief Checks the subscription state of the supplied JID.

        The method will return True if the JID is found and has a subscription 
        state of 'to' or 'both'.

        \param jid The JID to check in the roster.
        \param value True if the JID is found and subscription is 'to' or 'both'.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000008)
    ]
    HRESULT _stdcall IsSubscribed([in] BSTR jid, [out, retval] VARIANT_BOOL * value );
    /**
        \brief Changes the presence of the session.

        This method changes the presence of the session.

        \param show  The contents of the show element to use. MUST be either: blank (empty string, ""), "chat", "away", "xa", or "dnd".  
        \param status  The extended attribute of the presence. Can be any string but is typically kept relatively short. Like: "I'm in a meeting".  
        \param priority  The new priority of this client. Do not specify to keep the existing priority.  
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000000B)
    ]
    HRESULT _stdcall ChangePresence([in] BSTR show, [in] BSTR status, [in] long priority );
    /**
        \brief Finds or starts a new chat with the given JID and resource.

        This method finds or starts a chat with the given JID and specific resource.  
        The nickname is used as an alternate display for the JID in the UI.

        \param jid The JID with whom to start a chat.
        \param resource The specific resource to chat with (can be blank but not nil).
        \param nickname The alternate name for this JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000000C)
    ]
    HRESULT _stdcall StartChat([in] BSTR jid, [in] BSTR resource, [in] BSTR nickname );
    /**
        \brief Displays the profile windows for this JID.

        This method causes the profile to be fetched and opens the profile 
        window for the given JID.

        \param jid The JID whose profile will be dipslayed.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000000D)
    ]
    HRESULT _stdcall GetProfile([in] BSTR jid );
    /**
        \brief <b>UNSUPPORTED</b> - Creates an instance of a dockable window.

        <b>UNSUPPORTED</b>

        This method creates an instance of TfrmDockContainer (a dockable window)
        using the given caption. The method returns the window's handle. 

        \param caption The desired caption on the window.
        \param hwnd The window handle of the created window.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000010)
    ]
    HRESULT _stdcall CreateDockableWindow([in] BSTR caption, [out, retval] long * hwnd );
    /**
        \brief Adds a menu item to the Plugins-menu.

        A menu item is added to the Exodus Plugins-menu using the given caption. 
        The method returns an ID which is used to by removePluginMenu.

        \param caption The name of the menu item to be added.
        \param menuListener The object that will be notified when menu item is clicked (IExodusMenuListener.OnMenuItemClick).
        \param menuID The id generated by Exodus - save to remove the menu item (see removePluginMenu).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000000E)
    ]
    HRESULT _stdcall AddPluginMenu([in] BSTR caption, [in] IExodusMenuListener * menuListener, [out, retval] BSTR * menuID );
    /**
        \brief Removes a menu item from the Plugins-menu.

        This method uses the ID returned when calling addPluginMenu to find and
        remove the menu item.  If the ID is not found no action occurs.

        \param menuID The id of the menu item to remove.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000000F)
    ]
    HRESULT _stdcall RemovePluginMenu([in] BSTR menuID );
    /**
        \brief Monitors this JID to determine if Registration needs to be done.

        This method will watch for a presence packet type='error' and a code of
        407 for this JID. If received, a registration from will be displayed 
        (see TfrmRegister).

        \param jabberID The JID to be monitored.
        \param fullJID Flag indicating if this is a full JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000011)
    ]
    HRESULT _stdcall MonitorImplicitRegJID([in] BSTR jabberID, [in] VARIANT_BOOL fullJID );
    /**
        \brief Searches for the service on the given server.

        This method searches the given server for the named item and if found
        returns it's JID.

        \param server the server to search.
        \param service the named feature to find.
        \param jabberID the JID of the item or '' if not found.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000013)
    ]
    HRESULT _stdcall GetAgentService([in] BSTR server, [in] BSTR service, [out, retval] BSTR * jabberID );
    /**
        \brief Generates an id unique to the application.

        This method generates a unique id. This id is typically used for 
        setting the id on IQ packets.

        \param id Recieves the unique id.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000014)
    ]
    HRESULT _stdcall GenerateID([out, retval] BSTR * id );
    /**
        \brief Determines if the JID should be blocked.

        This method will return True if the given JID is found in the blocked
        list or if the user has selected to block non-roster contacts and this 
        JID is not in the user's roster.

        \param jabberID The JID which may need blocking.
        \param blocked - True if this JID should be blocked.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000015)
    ]
    HRESULT _stdcall IsBlocked([in] BSTR jabberID, [out, retval] VARIANT_BOOL * blocked );
    /**
        \brief Adds this JID to the block list.

        This method will add the given JID to the block list.

        \param jabberID The JID to block.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000016)
    ]
    HRESULT _stdcall Block([in] BSTR jabberID );
    /**
        \brief Removes a JID from the block list.

        This method removes a JID from the block list.

        \param jabberID The JID to remove from the block list.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000017)
    ]
    HRESULT _stdcall UnBlock([in] BSTR jabberID );
    /**
        \brief Returns the resource of the session JID.

        This method returns the resource associated with the current session JID.

        \param value Recieves the resource of the session JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000018)
    ]
    HRESULT _stdcall Resource([out, retval] BSTR * value );
    /**
        \brief Gets the port used to connect to the Jabber server.

        This method gets the port used to connect to the Jabber server. This
        is set from the current profile.

        \param value The connection port number.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000019)
    ]
    HRESULT _stdcall Port([out, retval] long * value );
    /**
        \brief Gets the priority of the session's presence.

        This method returns the priority of the main session's presence.

        \param value The session's priority:  -128 to +127.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x0000001A)
    ]
    HRESULT _stdcall Priority([out, retval] long * value );
    /**
        \brief Get's the status of the session's presence.

        This method returns the status of the main session's presence.

        \param value A natural language description of the show value.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x0000001C)
    ]
    HRESULT _stdcall PresenceStatus([out, retval] BSTR * value );
    /**
        \brief Get's the show value of the session's presence.

        This method returns the show value of the main session's presence.
        This is a machine readable value (see IExodusPresence.Show).

        \param value - The show value one of: show, away, dnd, xa
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x0000001D)
    ]
    HRESULT _stdcall PresenceShow([out, retval] BSTR * value );
    /**
        \brief Determines if the session is paused.

        This method returns True if the session is paused. Messages
        received while in paused mode will be queued for later delivery.

        \param value True if the session is paused.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x0000001E)
    ]
    HRESULT _stdcall IsPaused([out, retval] VARIANT_BOOL * value );
    /**
        \brief Determine if the session's presence is set to invisble.

        This method returns True if the main session's presence has been
        set to invisible.

        \param value True if session's presence is set to invisible.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x0000001F)
    ]
    HRESULT _stdcall IsInvisible([out, retval] VARIANT_BOOL * value );
    /**
        \brief Attempts to connect to the server.

        This method attempts to connect to the server.  Exodus will only attempt
        to connect if the main session is not already connected.  If no profile
        has been selected an exception will be raised.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000020)
    ]
    HRESULT _stdcall Connect( void );
    /**
        \brief Disconnects from the server.

        This method disconnects the active session from the server.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000021)
    ]
    HRESULT _stdcall Disconnect( void );
    /**
        \brief Retrieves a preference value as a string.

        This method will return the value of the given preference as a String.

        \param key Preference to get.  Should not be nil.
        \param value The string value of the preference or blank if not found.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000022)
    ]
    HRESULT _stdcall GetPrefAsString([in] BSTR key, [out, retval] BSTR * value );
    /**
        \brief Retrieves a preference value as an int.

        This method will return the value of the given preference as a int.

        \param key The preference to get.  Should not be nil and a valid XML element name.
        \param value The int value of the preference or 0 if not found.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000023)
    ]
    HRESULT _stdcall GetPrefAsInt([in] BSTR key, [out, retval] long * value );
    /**
        \brief Retrieves a preference value as a boolean.

        This method will return the value of the given preference as a boolean.

        \param key The preference to get.  Should not be nil and a valid XML element name.
        \param value The boolean value of the preference.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000024)
    ]
    HRESULT _stdcall GetPrefAsBool([in] BSTR key, [out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets the given preference using a string.

        This method sets the given preference using the given string value.

        \param key The preference to get.  Should not be nil and a valid XML element name.
        \param value The new value of the preference.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000025)
    ]
    HRESULT _stdcall SetPrefAsString([in] BSTR key, [in] BSTR value );
    /**
        \brief Sets the given preference using a integer.

        This method sets the given preference using the given integer value.

        \param key The preference to set. Should not be nil.
        \param value The new value of the preference.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000026)
    ]
    HRESULT _stdcall SetPrefAsInt([in] BSTR key, [in] long value );
    /**
        \brief Sets the given preference using a boolean

        This method sets the given preference using the given boolean value.

        \param key The preference to set. Should not be nil.
        \param value The new value of the preference.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000027)
    ]
    HRESULT _stdcall SetPrefAsBool([in] BSTR key, [in] VARIANT_BOOL value );
    /**
        \brief Finds or creates a chat session for the given JID/resource.

        This method searches existing chat sessions for the given JID/resource 
        (if not found it creates a new chat session) and returns the window handle to the window. If no resource is specified then a best fit is applied to find a suitable chat session.


        \param jabberID The JID whose chat session is desired.
        \param resource The specific resource of the JID (if any).
        \param handle The window handle of the existing chat window or nil if not found.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000028)
    ]
    HRESULT _stdcall FindChat([in] BSTR jabberID, [in] BSTR resource, [out, retval] long * handle );
    /**
        \brief Opens the JUD search window to the specified search agent JID.

        This method opens the JUD search window using the supplied JID as the search agent.

        \param searchJID The JID of search agent to use.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000029)
    ]
    HRESULT _stdcall StartSearch([in] BSTR searchJID );
    /**
        \brief Creates or enters or shows a room window.

        This method uses the supplied roomjid, nickname, password (optional) to 
        enter or create a room and find or show a room window. 
        
        The send presence parameter determines if the room should be created if 
        it doesn't exist.  If a new room is is created, plugins are notified via 
        the IExodusPlugin.NewRoom callback.
       
        If a password is required but not supplied, then a popup dialog will prompt the user.
        
        \b Note: If text conferencing is branded off nothing happens (brand_muc pref).

        \param roomJID The JID of the room to enter.
        \param nickname The nickname to use in the room. Should not be nil. If blank, the default_nick pref is used.
        \param password The password (optional). Should not be nil use blank.
        \param sendPresence True if Exodus should create the room if it doesn't exist.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000002A)
    ]
    HRESULT _stdcall StartRoom([in] BSTR roomJID, [in] BSTR nickname, [in] BSTR password, [in] VARIANT_BOOL sendPresence );
    /**
        \brief Creates an instant message window for normal type messages.

        This method creates an instant message window for sending normal type 
        messages to the given JID. This method creates the window but doesn't 
        show it. Exodus notifies plugins the chat window is being created by 
        using the IExodusPlugin.NewOutgoingIM callback.

        \param jabberID The JID with whom to chat.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000002B)
    ]
    HRESULT _stdcall StartInstantMsg([in] BSTR jabberID );
    /**
        \brief Opens the Jabber Browser window.

        This method opens the Jabber browser window with the selected JID or
        the session JID if none supplied.

        \param browseJID The JID to browse.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000002C)
    ]
    HRESULT _stdcall StartBrowser([in] BSTR browseJID );
    /**
        \brief Creates the join room window.

        This method shows the join room window prepopulated with the information 
        provided. If text conferencing is branded off this does nothing (brand_muc pref).

        \param roomJID The jid of the room to join (must not be nil).
        \param nickname The nickname to use in the room (use blank instead of nil).
        \param password The password to use on entering the room (use blank instead of nil).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000002D)
    ]
    HRESULT _stdcall ShowJoinRoom([in] BSTR roomJID, [in] BSTR nickname, [in] BSTR password );
    /**
        \brief Shows the preferences window.

        This method causes the preferences window to display.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000002E)
    ]
    HRESULT _stdcall ShowPrefs( void );
    /**
        \brief Shows the the custom presence dialog.

        This method causes the custom presence dialog to display.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000002F)
    ]
    HRESULT _stdcall ShowCustomPresDialog( void );
    /**
        \brief Shows the debug XML window.

        This method shows the debug XML window.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000030)
    ]
    HRESULT _stdcall ShowDebug( void );
    /**
        \brief Shows the login window.

        This method shows the login window.
        \b Note: You should check the connected property before call this method 
        (see IExodusController.Connected).

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000031)
    ]
    HRESULT _stdcall ShowLogin( void );
    /**
        \brief Shows a toast pop-up window.

        This method shows a toast pop-up window using the given message text and image.
        The plugin can (optionally) supply it's own window handle to focus when the toast 
        link is clicked on.

        \param message The text to display in the toast.
        \param wndHandle The window handle to focus when toast clicked (can be 0).
        \param imageIndex The index of the image to show on the toast window.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000032)
    ]
    HRESULT _stdcall ShowToast([in] BSTR message, [in] long wndHandle, [in] long imageIndex );
    /**
        \brief Changes the presence of the session JID.

        This method changes the presence of this session JID.

        \param show The new show value describing the availability (show, away, dnd, xa).
        \param status The new status value (natural language description of the show state).
        \param priority The new priority (-128 to +127).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000033)
    ]
    HRESULT _stdcall SetPresence([in] BSTR show, [in] BSTR status, [in] long priority );
    /**
        \brief Returns the Roster interface.

        This method returns the roster interface.

        \param value The IExodusRoster interface.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000036)
    ]
    HRESULT _stdcall Roster([out, retval] IExodusRoster ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Returns the presence database interface.

        This method returns the presence database interface.

        \param value The IExodusPPDB interface.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000037)
    ]
    HRESULT _stdcall PPDB([out, retval] IExodusPPDB ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Adds a plugin's disco item to those sent by Exodus.

        This method adds the given disco item to Exodus' own. Exodus returns an 
        ID that should be kept and used when removing the item (see removeDiscoItem).

        \param jabberID The JID of the new item.
        \param name The name attribute of the disco item.
        \param id The id used when removing this item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000000A)
    ]
    HRESULT _stdcall RegisterDiscoItem([in] BSTR jabberID, [in] BSTR name, [out, retval] BSTR * id );
    /**
        \brief Removes a plugin's disco item from Exodus.

        This method removes a disco item from Exodus using the id returned when
        registering it (see registerDiscoItem).

        \param id The id returned when registering this item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000035)
    ]
    HRESULT _stdcall RemoveDiscoItem([in] BSTR id );
    /**
        \brief Allows plugins to append static XML to each presence packet sent by Exodus.

        This method allows plugins to append static XML to each presence packet 
        sent by Exodus. The XML \b must be well formed.

        Calling this method will return an ID which plugins should use to 
        remove this XML (see removePresenceXML).

        \param xml The well-formed XML to append to presence packets.
        \param id The ID assigned to this XML by Exodus - used for removing it.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000039)
    ]
    HRESULT _stdcall RegisterPresenceXML([in] BSTR xml, [out, retval] BSTR * id );
    /**
        \brief Allows plugins to remove static XML being appened to presence packets.

        This method allows plugins to remove static XML being appened to 
        presence packets using it's registration id.

        \param id The id obtained when the XML was added using (registerPresenceXML).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000003A)
    ]
    HRESULT _stdcall RemovePresenceXML([in] BSTR id );
    /**
        \brief Causes the given Windows message to generate Exodus events.
        \depcreated This method is deprecated after v.0.9.1.

        This causes Exodus to track this Windows message id. When the message 
        is received Exodus fires a '/windows/msg' event containing the relevant
        windows message information: the message id, \c hwnd, \c lparam, \c wparam.

        \param message The id of the Windows Message to track.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000003B)
    ]
    HRESULT _stdcall TrackWindowsMsg([in] long message );
    /**
        \brief Adds a context menu to the roster.

        This method adds a context menu to the roster which is available 
        for all contacts.  The method returns an ID used to identify this item
        in onMenu event processig and to remove the menu (see removeContactMenu).

        \param caption The caption of the menu item.
         \param menuListener The object that will be notified when menu item is clicked (IExodusMenuListener.OnMenuItemClick).
        \param menuID The id of the menu item used to identify it or remove it.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000003C)
    ]
    HRESULT _stdcall AddContactMenu([in] BSTR Caption, [in] IExodusMenuListener * menuListener, [out, retval] BSTR * menuID );
    /**
        \brief Removes a context menu from the roster.

        This method removes the given menu from the roster using the id returned
        from addContactMenu.

        \param menuID The id of the menu to be removed.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000003D)
    ]
    HRESULT _stdcall RemoveContactMenu([in] BSTR menuID );
    /**
        \brief Returns the JID of the currently selected roster item.

        This method returns the JID of the currently selected roster item. If the 
        current item isn't a contact this returns blank.

        \param jid The JID of the selected roster item or blank if none selected.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000003E)
    ]
    HRESULT _stdcall GetActiveContact([out, retval] BSTR * jid );
    /**
        \brief Returns the name of the currently selected roster group.

        This method returns the name of the currently selected roster group.  If
        the current item isn't a group this returns blank.

        \param name The name of the selected group or blank if item isn't a group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x0000003F)
    ]
    HRESULT _stdcall GetActiveGroup([out, retval] BSTR * name );
    /**
        \brief Returns a list of currently selected contacts.

        This method returns the list of currently selected contacts in the roster.
        The method allows you to specify if only online contacts are returned.

        \param online Set to True if you only want online contacts.
        \param jids A list of JIDs (can be empty but not nil).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000041)
    ]
    HRESULT _stdcall GetActiveContacts([in] VARIANT_BOOL online, [out, retval] VARIANT * jids );
    /**
        \brief Gets the IP address of the client.

        Gets the IP address of the client.

        \param value The IP address of the client.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000040)
    ]
    HRESULT _stdcall LocalIP([out, retval] BSTR * value );
    /**
        \brief Register plugin as an authentication mechanmism.

        This method allows plugins providing authentiation mechanisms to register 
        with the session object. This method \b must be called while the session 
        is disconnected. 

        \param authAgent The object which implements IExodusAuth interface.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000042)
    ]
    HRESULT _stdcall SetPluginAuth([in] IExodusAuth * authAgent /*Warning: unable to validate structure name: */ );
    /**
        \brief Use to inform Exodus of the results of the plugin's authentication results.

        Calling this method informs Exodus the plugin has or has not authenticated the session.

        \param authed True if the authentication succeeded.
        \param xml - reseverd for future use.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000043)
    ]
    HRESULT _stdcall SetAuthenticated([in] VARIANT_BOOL authed, [in] BSTR xml );
    /**
        \brief This method informs Exodus of the authenticated session JID.

        If an authentication plugin doesn't use username/password authentication 
        or otherwise modifies the JID during authentication, use this method to
        inform Exodus of the session JID.

        \param username The username portion of the authenticated entity.
        \param host The hostname portion of the authenticated entity.
        \param resource The resource of the authenticated entity.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000044)
    ]
    HRESULT _stdcall SetAuthJID([in] BSTR username, [in] BSTR host, [in] BSTR resource );
    /**
        \brief Adds a menu item to "normal" message window context menus.

        This method adds a menu item to "normal" message window (i.e. instant 
        message) context menus.  The ID of the menu item is used to identify
        events raised from this menu item or for it's removal.

        \param caption The name of the menu item displayed in the UI.
        \param menuListener The object that will be notified when menu item is clicked (IExodusMenuListener.OnMenuItemClick).
        \param menuID The Exodus assigned id for this menu item. 
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall AddMessageMenu([in] BSTR caption, [in] IExodusMenuListener * menuListener, [out, retval] BSTR * menuID );
    /**
        \brief Adds a menu item to the context menu for roster groups.

        This method adds a menu item to the context menu for roster groups. The
        menu id returned should be used to identify events from this menu item 
        or to remove the item (see removeGroupMenu).

        \param caption The name of the menu item.
        \param menuListener The object that will be notified when menu item is clicked (IExodusMenuListener.OnMenuItemClick).
        \param menuID The Exodus assigned id for this menu item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall AddGroupMenu([in] BSTR Caption, [in] IExodusMenuListener * menuListener, [out, retval] BSTR * ID );
    /**
        \brief Removes a context menu item from roster groups.

        This method removes a context menu item from the roster's group 
        specific context menu.

        \param menuID The id of the menu item to remove (see addGroupMenu).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall RemoveGroupMenu([in] BSTR menuID );
    /**
        \brief Shows the service registration wizard.

        This method causes the registration wizard to show. This method 
        should only be used after authentication.

        \param jabberID The service to register with.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall RegisterWithService([in] BSTR jabberID );
    /**
        \brief Shows the add contact dialog.

        This method causes the show contact dialog to show using the given
        JID as the contact to add.

        \param jid The jid to be added (can be nil).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CD),
    helpstring("Show the \"add contact\" form")
    ]
    HRESULT _stdcall ShowAddContact([in] BSTR jid );
    /**
        \brief Registers an Caps extension with Exodus.

        Registers a JEP-115 (Capabilities) extension feature with Exodus.
        This extension will be added to the presence packets sent by Exodus.
        To add multiple features for this extension, this method may be called
        multiple times.

        \param ext The extension to add.
        \param feature The feature to add to this extension.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CE),
    helpstring("Register a JEP-115 extension.  Call multiple times with the same ext to set multiple features.")
    ]
    HRESULT _stdcall RegisterCapExtension([in] BSTR ext, [in] BSTR feature );
    /**
        \brief Unregisters a Caps extension with Exodus.

        This method unregisters all features associated with the given JEP-115 
        (Capabilities) extension.

        \param ext The caps extension to remove.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CF),
    helpstring("Unregister all of the features for a given ext.")
    ]
    HRESULT _stdcall UnregisterCapExtension([in] BSTR ext );
    /**
        \brief Gets the roster images COM interface.

        This method returns the IExodusRosterImages COM interface.  This 
        interface allows plugins to add, load or find images in the roster's
        image cache.

        \param value A IExodusRosterImages COM inteface.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D0)
    ]
    HRESULT _stdcall RosterImages([out, retval] IExodusRosterImages ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the disco entity cache COM interface.

        This method returns the IExodusEntityCache interface. This inteface
        allows plugins to find information in the disco cache or retrieve 
        disco information from entities.

        \param value An IExodusEntityCache COM interface.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D1)
    ]
    HRESULT _stdcall EntityCache([out, retval] IExodusEntityCache ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Writes a message the debug window.

        This method allows plugins to write messages to the debug window.

        \param value the message to write to the debug window (should not be nil).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D2)
    ]
    HRESULT _stdcall Debug([in] BSTR value );
    /**
        \brief Call to be notified of the result of an IQ get/set.

        The plugin can register to track a given IQ get/set.  Exodus will call
        back the supplied listener using IExodusIQListener.ProcessIQ.

        \param xml The IQ to track.
        \param listener The IExodusIQListener to callback.
        \param timeout The timeout value for the listener (in seconds).
        \param ret The id of the IQ being tracked. 
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D3)
    ]
    HRESULT _stdcall TrackIQ([in] BSTR xml, [in] IExodusIQListener * listener /*Warning: unable to validate structure name: */, [in] long timeout, [out, retval] BSTR * ret );
    /**
        \brief Fires an event.

        This method fires the given event with the optional argument. The xml
        element is required and should not be nil.  The argument (Arg) is 
        optional and can be blank but not nil.  The argument Arg is interpreted 
        according to the type of event being fired:
        \li /roster 
          Arg is expected to be a roster JID, the xml param is overwritten with 
          the roster item's xml representation.
        \li /presence
          Arg is expected to be a valid JID whose presence is located in the 
          presence database.
        \li /data
          Arg is passed through directly.
        \li any other value
          Arg is ignored.

        \param event The name of the event (i.e. /roster/remove).
        \param xml The XML that is passed to event listeners.
        \param arg Either a JID or data to be passed to the event handlers.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D4)
    ]
    HRESULT _stdcall FireEvent([in] BSTR event, [in] BSTR xml, [in] BSTR arg );
    /**
        \brief Allows plugins to register a listener on a signal chain.

        This method will allow the plugin to be notified of events on the 
        signal chain described by the xpath parameter:
            Recognized chains are:
                /roster
                /data
                /presence
                /filter
                /pre
                /packet
                /post
        The returned id is used to unregister this listener (see UnRegisterCallback).

        \param xpath The xpath to match.
        \param listener The listener to notify when the xpath is matched.
        \param id The id of the registered listener - use to remove (see UnRegisterCallback)
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D5)
    ]
    HRESULT _stdcall RegisterListener([in] BSTR xpath, [in] IExodusListener * listener /*Warning: unable to validate structure name: */, [out, retval] long * id );
    /**
        \brief Allows plugins to access the Exodus toolbar.

        This method returns the IExodusToolbar COM interface.

        \param value The IExodusToolbar COM interface.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D6)
    ]
    HRESULT _stdcall Toolbar([out, retval] IExodusToolbar ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the contact message logger.

        This method gets the IExodusLogger contact logger for use by plugins.
        The contact logger is responsible for logging messages for this contact.

        \param value An instance of IExodusLogger used for logging messages. 
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D7)
    ]
    HRESULT _stdcall ContactLogger([out, retval] IExodusLogger ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Sets the logger used for contact messages.

        This method sets the logger used to log contact messages with the given
        instance of IExodusLogger.  Calling this method causes an 
        '/session/logger' event to fire.

        \param value The IExodusLogger that will log contact messages.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000D7)
    ]
    HRESULT _stdcall ContactLogger([in] IExodusLogger * value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the room logger.

        This method gets the IExodusLogger interface used for room logging.

        \param value An instance of IExodusLogger used for room logging.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D8)
    ]
    HRESULT _stdcall RoomLogger([out, retval] IExodusLogger ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Sets the logger used for room logging.

        This method sets the room logger using the given IExodusLogger instance. 
        This method causes a '/session/room-logger' event to fire.

        \param value The IExodusLogger interface to be used for room logging.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000D8)
    ]
    HRESULT _stdcall RoomLogger([in] IExodusLogger * value /*Warning: unable to validate structure name: */ );
    /**
        \brief Adds the value as one string in a list to the given preference key.

        Adds the value as one string in a list to the given preference key.

        \param key The preference key to which a value will be added.
        \param value A new value to be added to this preference key.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D9)
    ]
    HRESULT _stdcall AddStringlistValue([in] BSTR key, [in] BSTR value );
    /**
        \brief Removes an item from the message context menu.
        
        Removes an item from the message context menu.

        \param menuID The id of the menu item to remove.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000DA), 
    helpstring("Remove an item from the message context menu")
    ]
    HRESULT _stdcall RemoveMessageMenu([in] BSTR menuID );
  };

  [
    uuid(D2639B6C-A7BB-4CCC-BD73-8C1EB197F9D3), 
    version(1.0),
    helpstring("Dispatch interface for ExodusChat Object"),
    dual,
    oleautomation
  ]
  /**\interface IExodusChat
    \brief Interface for triggering events in a chat window.

    This interface allows a plugin to trigger events and query properties of a chat window.
  **/

   interface IExodusChat: IDispatch
  {
    /**
        \brief JID of remote user or room. 

        This is the JID of the room or user with which the conversation is being held.

        \param value JID of user or room.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000001)
    ]
    HRESULT _stdcall jid([out, retval] BSTR * value );
    /**
        \brief Add a menu item to the window's context menu.

        Add a menu item to the window's context menu.

        \param caption String for this menu item.
        \param menuListener The object that will be notified when menu item is clicked (IExodusMenuListener.OnMenuItemClick).
        \param menuID ID of this menu item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000002)
    ]
    HRESULT _stdcall AddContextMenu([in] BSTR caption, [in] IExodusMenuListener * menuListener /*Warning: unable to validate structure name: */, [out, retval] BSTR * menuID );
    /**
        \brief Gets the text in the message out input box.

        Gets the text in the message out input box.

        \param value Text currently in the message out input box.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000004)
    ]
    HRESULT _stdcall MsgOutText([out, retval] BSTR * value );
    /**
        \brief Registers a Plugin with the chat window.

        Registers a plugin (IExodusChatPlugin) with the chat window.

        \param plugin The Plugin interface to be registered.
        \param id The ID assigned to this plugin.  Used for unregistering plugin.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000003)
    ]
    HRESULT _stdcall RegisterPlugin([in] IExodusChatPlugin * plugin /*Warning: unable to validate structure name: */, [out, retval] long * id );
    /**
        \brief Unregisters a plugin with the chat window.

        Unregisters a plugin (IExodusChatPlugin) with the chat window.

        \param id ID returned by RegisterPlugin.
        \param success True if unregistration was successful, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000005)
    ]
    HRESULT _stdcall UnRegisterPlugin([in] long id, [out, retval] VARIANT_BOOL * success );
    /**
        \brief <b>DEPRECATED</b> - Get an integer handle to the various elements in a chat.

        <b>DEPRECATED</b> - This method gives a way to get the memory location (Handle) for various parts of the chat.
        Returned value is integer and must be cast to correct type before use.\n
        Valid values for parameter Part:\n
        HWND_MsgInput = 0\n
        HWND_MsgOutput = 1\n
        Ptr_MsgInput = 2\n
        Ptr_MsgOutput = 3\n

        \param part What handle/pointer to return.
        \param value Returned hanle/pointer as integer.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000006)
    ]
    HRESULT _stdcall GetMagicInt([in] ChatParts part /*Warning: unable to validate structure name: */, [out, retval] long * value );
    /**
        \brief Remove a menu item from the context menu.

        Remove a menu item from the context menu.

        \param menuID ID of the menu item to be removed.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000007)
    ]
    HRESULT _stdcall RemoveContextMenu([in] BSTR menuID );
    /**
        \brief Add text to message out input box.

        Add text to message out input box.

        \param value Text to add to message out input box.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall AddMsgOut([in] BSTR value );
    /**
        \brief Add a menu item to the message out input box menu.

        Add a menu item to the message out input box menu.

        \param caption String for this menu item.
        \param menuListener The object that will be notified when menu item is clicked (IExodusMenuListener.OnMenuItemClick).
        \param menuID Id for this menu item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall AddMsgOutMenu([in] BSTR caption, [in] IExodusMenuListener * menuListener /*Warning: unable to validate structure name: */, [out, retval] BSTR * menuID );
    /**
        \brief Remove a menu item from the message out input box.

        Remove a menu item from the message out input box.

        \param menuID ID of the menu item to be removed.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall RemoveMsgOutMenu([in] BSTR menuID );
    /**
        \brief Send a message to the chat.

        This method sends a message to the chat room from the current user.

        \param body Body of the message to be sent.
        \param subject Subject of the message to be sent.
        \param xml The xml of the message to be sent.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall SendMessage([in] BSTR * body, [in] BSTR * subject, [in] BSTR * xml );
    /**
        \brief Get the current thread ID.

        Get the current thread ID.

        \param thread The thread ID for the current conversation thread.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CD)
    ]
    HRESULT _stdcall CurrentThreadID([out, retval] BSTR * thread );
    /**
        \brief Display a message in the chat or room history.

        This method will generate and display a message in the chat or room history.  
        Message is not actually sent to remote user or room.

        \param body Body of message to display.
        \param subject Subject of message to display.
        \param from The From JID of the message to display.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CE)
    ]
    HRESULT _stdcall DisplayMessage([in] BSTR body, [in] BSTR subject, [in] BSTR from );
    /**
        \brief Add a user to a room.

        Add a user to a room.

        \param jid JID of the user to be added to room.
        \param nickname Nickname of the user to be added to the room.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CF)
    ]
    HRESULT _stdcall AddRoomUser([in] BSTR jid, [in] BSTR Nickname );
    /**
        \brief Remove a user from a room.

        Remove a user from a room.

        \param jid JID of the user to be removed from the room.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D0)
    ]
    HRESULT _stdcall RemoveRoomUser([in] BSTR jid );
    /**
        \brief Get the user's current nickname.

        Get the user's current nickname.

        \param value Current user's nickname.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D1)
    ]
    HRESULT _stdcall CurrentNick([out, retval] BSTR * value );
    /**
        \brief Get a handle to the interface for a chat window control.

        This method will set the passed in pointer to a reference of the control named.

        \param name Name of the control desired.
        \param value Pointer to the control.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D2)
    ]
    HRESULT _stdcall GetControl([in] BSTR name, [out, retval] IExodusControl ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Get the caption for the chat.

        Get the caption for the chat.

        \paramr value Pointer to Caption value.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D3)
    ]
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    /**
        \brief Get the caption for the chat.

        Get the caption for the chat.

        \param value Caption Value.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000D3)
    ]
    HRESULT _stdcall Caption([in] BSTR value );
  };

  [
    uuid(DCDFAD67-6CB2-4202-B29C-50D633C02596),
    version(1.0),
    helpstring("Dispatch interface for ExodusPlugin Object"),
    dual,
    oleautomation
  ]
  /**\interface IExodusPlugin
    \brief Basic inteface for plugins.

    IExodusPlugin is the core interface that is used by exodus, and MUST be implemented by all plugins.
  **/

   interface IExodusPlugin: IDispatch
  {
    /**
        \brief First method called when client starts the plugin.

        First method called when client starts the plugin.
        Any plugin startup and initialization code should be done here.

        \param exodusController The COM Object which represents the Exodus application. Plugins will most likely want to cache this object in order to make other calls back to the exodus application itself.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x00000001),
    helpstring("Exodus is starting the plugin")
    ]
    HRESULT _stdcall Startup([in] IExodusController * exodusController /*Warning: unable to validate structure name: */ );
    /**
        \brief Last method called when the client shuts down the plugin.

        This method is called whenever the plugin is being shutdown. Possible reasons for this may be that the application is shutting down (very common) or the user has selected to disable this particular plugin. You should make sure that all refcounts are decremented such that the exodus process can be completely freed if the application is exiting.

        Note this method is not called if the plugin is running outside the Exodus process space.

        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x00000002),
    helpstring("Exodus is shutting down the plugin")
    ]
    HRESULT _stdcall Shutdown( void );
    /**
        \brief Plugin is receiving a packet to process.

        When a plugin registers to receive notifcations (by calling RegisterCallback), exodus will call this method for every packet or event which matches the provided xpath statement.

        \param xpath The string matching the xpath expression the plugin previously registered for.
        \param event Event will almost always be the string "xml" or possibly "timeout".
        \param xml A BSTR containing the raw serialized XML. Plugins are responsible for re-parsing the serialized XML if a DOM object is required.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x00000003),
    helpstring("Plugin is receiving a packet")
    ]
    HRESULT _stdcall Process([in] BSTR xpath, [in] BSTR event, [in] BSTR xml );
    /**
        \brief A new chat window is starting.

        When a new chat window is started by either the user, or an incoming message element, this method is called. If a plugin wishes to add functionality to this chat window, it should register with the IExodusChat object passed as the second parameter. Typically, plugins which are interested in chat windows will wish to instantiate some other COM object for each window. The main plugin should use this event to do this, and register the new object instance with the chat object passed in.

        \param jid JID of the remote user of the chat.
        \param chat A reference to the IExodusChat interface for this chat.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x00000004),
    helpstring("A new chat window is starting")
    ]
    HRESULT _stdcall NewChat([in] BSTR jid, [in] IExodusChat * chat /*Warning: unable to validate structure name: */ );
    /**
        \brief A new room is starting.

        Almost identical to the NewChat event, but fired when new conference rooms are started. The same IExodusChat controller is used to manipulate the room object.

        \param jid JID of the room.
        \param room a reference to the IExodusChat interface for this room.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x00000005),
    helpstring("A new room is starting")
    ]
    HRESULT _stdcall NewRoom([in] BSTR jid, [in] IExodusChat * room /*Warning: unable to validate structure name: */ );
    /**
        \brief A new IM is about to go out.

        This event is fired whenever an IM message (not a chat or groupchat) is being sent from the system. This includes messages like room invites, and roster items being sent. Plugins are allowed to change the value of the Body and Subject paramaters if necessary.
        
        Plugins should return WELL-FORMED XML as a return value if that plugin needs to append additional elements into the message stanza. For example, a plugin could return: <x xmlns="foo">Some CDATA</x> and have that x tag be inserted into the outgoing message.

        \param jid The jabber ID of where the message is being sent to.
        \param body The current body of the message. Plugins may modify this parameter.
        \param subject The current subject of the message. Plugins may modify this parameter.
        \param xTags The current string representing all xtags being appending to the outgoing message.
        \param addXML XML to be added to the IM by the plugin.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x00000008),
    helpstring("A New IM is about to go out.")
    ]
    HRESULT _stdcall NewIM([in] BSTR jid, [in] BSTR * body, [in] BSTR * subject, [in] BSTR xTags, [out, retval] BSTR * addXML );
    /**
        \brief A request to configure the plugin has been made.

				Exodus calls this method from the plugin options window where the user can configure a specific plugin. Plugins should display some kind of configuration dialog and allow the user to specific options. In order to persist options (preferences), plugins can use the appropriate IExodusController methods (such as getPrefAsString and setPrefAsString) to persist options directly to the exodus.xml file in the user's profile. 

        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x0000000C),
    helpstring("The plugin should configure itself")
    ]
    HRESULT _stdcall Configure( void );
    /**
        \brief A new IM is being created.

        Almost identical to the NewChat event, but fired when a new instant message window is opened. The same IExodusChat controller is used to get events and manipulate the IM window.

        \param jid The jabber ID of the recipient.
        \param The COM object which can be used to manipulate the message window.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall NewOutgoingIM([in] BSTR jid, [in] IExodusChat * InstantMsg /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(E28E487A-7258-4B32-AD1C-F23A808F0460), 
    version(1.0),
    helpstring("Dispatch interface for ExodusChatPlugin Object"),
    dual,
    oleautomation
  ]
/**\interface IExodusChatPlugin
    \brief Interface for messages to a Chat Plugin

    Interface for a chat window to notify a Chat plugin of an event in the chat window.
    Should be registered with IExodusChat interface.
   **/

   interface IExodusChatPlugin: IDispatch
  {
    /**
        \brief The user has entered text to send to a Room or Contact

        The client is about to send this message to the Room or Contact.
        The plugin may evaluate the message and either modify the message by changing the body, or 
        prevent the message from being sent by returning false (0) in the Allow paramerter.

        \param body Body of the message.
        \param allow Should this message be allowed to be sent.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000001)
    ]
    HRESULT _stdcall OnBeforeMessage([in] BSTR * body, [out, retval] VARIANT_BOOL * allow );
    /**
        \brief A message is being sent to the Room or Contact.

        A message is being sent to the Room or Contact.
        The plugin may add additional XML to the outgoing message.

        \param body Body of the message
        \param additionalXML Additional XML to be added to the outgoing message.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000002)
    ]
    HRESULT _stdcall OnAfterMessage([in] BSTR * body, [out, retval] BSTR * additionalXML );
    /**
        \brief The chat is closing.

        The chat is closing.
        Please, do any required cleanup, including unregistering the plugin here.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000006)
    ]
    HRESULT _stdcall OnClose( void );
    /**
        \brief <b>UNSUPPORTED</b> - A new chat window is being created.

        <b>UNSUPPORTED</b> - A new chat window is being created.

        \param hwnd Handle to new chat window.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall OnNewWindow([in] long hwnd);
    /**
        \brief IExdodusController notifies plugins a new message is about to be received via this method.
        
        The plugin is notified of new messages are about to be processed by IExodusController via this method.  The plugin can prevent other plugins from being notified by setting allow to False.

        \param body The body of the new message.
        \param xml  The full message packet.
        \param allow Indicates to IExodusController if other plugins should revceive this notification.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall OnBeforeRecvMessage([in] BSTR body, [in] BSTR xml, [out, retval] VARIANT_BOOL * allow );
    /**
        \brief IExodusController uses this method to notify plugins a new message has been received and processed.
        
        The plugin is notified via this method that a new message has been received and processed by IExodusController.

        \param body The body of the new message.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall OnAfterRecvMessage([in] BSTR * body );
    /**
        \brief Called to notify the plugin a key has returned to the up state.
        
        Called to notify the plugin a key has returned to the up state.  The shiftState indcates if any key modifiers are also pressed. The handled parameter can be set to true (1) to indicate the event has been handeled.

        Values for shiftState can be:
        \li 0 - shift key
        \li 1 - alt key
        \li 2 - control key
        \li 3 - left mouse button
        \li 4 - right mouse button
        \li 5 - middle mouse button
        \li 6 - mouse double click

        \param key The value of the key pressed.
        \param shiftState Indicates if any key modifers are in effect (shift/control/alt).
        \param handled Set to True (1) to prevent other handling of this event.
    **/
    [
    id(0x0000012D)
    ]
    HRESULT _stdcall OnKeyUp([in] long key, [in] long shiftState, [out, retval] VARIANT_BOOL * handled );
    /**
        \brief Called to notify the plugin a key has been entered the depressed state.
        
        Called to notify the plugin a key has entered to the down state.  The shiftState indcates if any key modifiers are also pressed. The handled parameter can be set to true (1) to indicate the event has been handeled.

        Values for shiftState can be:
        \li 0 - shift key
        \li 1 - alt key
        \li 2 - control key
        \li 3 - left mouse button
        \li 4 - right mouse button
        \li 5 - middle mouse button
        \li 6 - mouse double click

        \param key The value of the key pressed.
        \param shiftState Indicates if any key modifers are in effect (shift/control/alt).
        \param handled Set to True (1) to prevent other handling of this event.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall OnKeyDown([in] long key, [in] long shiftState, [out, retval] VARIANT_BOOL * handled ); 
  };

  [
    uuid(29B1C26F-2F13-47D8-91C4-A4A5AC43F4A9),
    version(1.0),
    helpstring("Dispatch interface for ExodusRoster Object"),
    dual,
    oleautomation
  ]
  /**
    \interface IExodusRoster
    \brief IExodusRoster encapsulates the roster and related activities.
     
     Common roster mamangement functions are defined in this interface.
  **/

   interface IExodusRoster: IDispatch
  {
    /**
        \brief Calling this method fetches the user's roster.

        Calling this method fetches the user's roster.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000001)
    ]
    HRESULT _stdcall Fetch( void );
    /**
        \brief Adds an item to the roster w/o optional subscription.

        This method adds an item to the roster including sending an update event to the
        UI (i.e. the same as calling IExodusRosterItem.Update).  Depending on the value of
        the Subscribe parameter a subscription request is sent to the contact.

        \param jabberID The JID of the roster item to be added.
        \param nickname The name displayed by the UI for this JID.
        \param group The name of the group to add this roster item to.
        \param subscribe True if a subscription should be sent to the contact.
        \param Item The roster item that was added or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000003)
    ]
    HRESULT _stdcall Subscribe([in] BSTR jabberID, [in] BSTR nickname, [in] BSTR group, [in] VARIANT_BOOL subscribe, [out, retval] IExodusRosterItem ** item /*Warning: unable to validate structure name: */ );
    /**
        \brief Finds the JID in the roster.
        
        Finds the JID in the roster.

        \param jabberID The JID of the contact to find.
        \param item The roster item that was found or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000006)
    ]
    HRESULT _stdcall Find([in] BSTR jabberID, [out, retval] IExodusRosterItem ** item /*Warning: unable to validate structure name: */ );
    /**
        \brief Finds a roster item by index.

        This method returns the roster item at the specified index or nil.

        \param index The index of the roster item to retreive.
        \param item The desired IExodusRosterItem or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000007)
    ]
    HRESULT _stdcall Item([in] long index, [out, retval] IExodusRosterItem ** item /*Warning: unable to validate structure name: */ );
    /**
        \brief Returns the number of items in the roster.
        
        Returns the number of items in the roster.

        \param itemCount the number of items in the roster
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000008)
    ]
    HRESULT _stdcall Count([out, retval] long * itemCount );
    /**
        \brief Removes an item from the roster.

        This method doesn't update the server just removes the item from internal
        roster representation.

        \param item the item to remove.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall RemoveItem([in] IExodusRosterItem * item /*Warning: unable to validate structure name: */ );
    /**
        \brief Adds the specified group to the roster.

        This method does not cause an update on the UI.

        \param grp The name of the group to add.
        \param ret The \c IExodusRosterGroup that was created.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall AddGroup([in] BSTR grp, [out, retval] IExodusRosterGroup ** ret /*Warning: unable to validate structure name: */ );
    /**
        \brief Finds a roster group by name.

        This method finds the named group in the roster or returns nil if the 
        group doesn't exist.

       \param grp The name of the group to find.
       \param ret This is the \c IExodusRosterGroup that was found or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall GetGroup([in] BSTR grp, [out, retval] IExodusRosterGroup ** ret /*Warning: unable to validate structure name: */ );
    /**
        \brief Removes a group from the roster.

        This method does not update the server or the UI.

        \param grp The \c IExodusRosterGroup to remove.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall RemoveGroup([in] IExodusRosterGroup * grp /*Warning: unable to validate structure name: */ );
    /**
        \brief Returns the number of groups in the roster.
        
        Returns the number of groups in the roster.

        \param value the number of groups in the roster.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CD)
    ]
    HRESULT _stdcall GroupsCount([out, retval] long * value );
    /**
        \brief Finds a group by index in the roster.

        This method will can return nil if the index is greater than the actual
        number of roster groups. If the index is negative behavior is undefined.

        \param index The index of the desired group.
        \param ret The desired \c IExodusRosterGroup or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CE)
    ]
    HRESULT _stdcall Groups([in] long index, [out, retval] IExodusRosterGroup ** ret /*Warning: unable to validate structure name: */ );
    /**
        \brief Adds a context menu to the roster.

        This method adds a context menu to the roster using the given id. 
        If a menu with this id exists a duplicate is not added.  
        
        \b Note: If a context menu is added with this method, it should be 
        removed using RemoveContextMenu.

        \param id The id of the menu to add.
        \param ret True if the menu item was added.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D0)
    ]
    HRESULT _stdcall AddContextMenu([in] BSTR id, [out, retval] VARIANT_BOOL * ret );
    /**
        \brief Removes the context menu from the Roster.

        Removes the menu identified by the given ID from the roster.

        \param id The id of the menu to remove.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D1)
    ]
    HRESULT _stdcall RemoveContextMenu([in] BSTR id );
    /**
        \brief Adds a context menu item to a context menu.

        Adds a context menu item to the specified menu. The specified action 
        should be a handled event name. The unique id of the menu item which
        is returned should be tracked to be able to remove the menu item later 
        or to identify events from this context menu.

        \b Note: IF a context menu item is added using this method, it should 
        be removed using removeContextMenuItem.

        \param menuID The id of the menu to add this item to.
        \param caption The human readable name of this menu item exposed to the UI.
        \param menuListener The object that will be notified when menu item is clicked (IExodusMenuListener.OnMenuItemClick).
        \param itemID The id of the menu item (used to remove it).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D2)
    ]
    HRESULT _stdcall AddContextMenuItem([in] BSTR menuID, [in] BSTR caption, [in] IExodusMenuListener * menuListener /*Warning: unable to validate structure name: */, [out, retval] BSTR * itemID );
    /**
        \brief Removes a context menu item from a menu.

        The specified menu item id is removed from the specified menu.  The
        menu item id used should be the one returned by addContextmenuItem.

        \param menuID The id of the menu to add the item to.
        \param itemID The id of menu item to remove.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D3)
    ]
    HRESULT _stdcall RemoveContextMenuItem([in] BSTR menuID, [in] BSTR itemID );
    /**
        \brief Adds a new plugin-roster item to the roster.

        These roster items are uniquely identified in their xml representation
        by the namespace 'exodus:plugin-item'.

        \param jabberID The JID of the contact to be added.
        \param ret the \c IExodusRosterItem that was created.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D4)
    ]
    HRESULT _stdcall AddItem([in] BSTR jabberID, [out, retval] IExodusRosterItem ** ret /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(284E49F2-2006-4E48-B0E0-233867A78E54),
    version(1.0),
    helpstring("Dispatch interface for ExodusPPDB Object"),
    dual,
    oleautomation
  ]
  /** \interface IExodusPPDB
  	\brief Methods for accessing the Exodus Presence Database.

    Methods for managing presence by for JIDs. 
  **/
   interface IExodusPPDB: IDispatch
  {
    /**
        \brief Finds presence for the given JID & resource.

        This method retrieves the presence for this full JID.  If resource is 
        omitted, then the highest priority presence for the bare JID is returned.

        \param jabberID The JID whose presence we want.
        \param resource The specific resource - should be blank & not nil.
        \param presence The presence that was found or nil if not found or not yet received.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000001)
    ]
    HRESULT _stdcall Find([in] BSTR jabberID, [in] BSTR resource, [out, retval] IExodusPresence ** presence /*Warning: unable to validate structure name: */ );
    /**
        \brief Retrieves the next presence packet retrieved for the given JID.

        This method retrieves the next presence packet based on the supplied jid and resource.

        \param jabberID The JID whose next presence we want.
        \param resource The specific resource - should be blank & not nil.
        \param value The next presence that was found - nil if not found or none received.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x00000002)
    ]
    HRESULT _stdcall Next([in] BSTR jabberID, [in] BSTR resource, [out, retval] IExodusPresence ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Counts the presence packets in the database.

        This method determines the total number of presence packets being held
        by the Exodus presence database.

        \param value The number of packets in the database.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000003)
    ]
    HRESULT _stdcall Count([out, retval] long * value );
    /**
        \brief Gets the last presence recieved.

        Retrieves the last presence packet received or nil if none yet received.

        \param value The last presence received or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x00000004)
    ]
    HRESULT _stdcall LastPresence([out, retval] IExodusPresence ** value /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(FA63024E-3453-4551-8CA0-AFB78B2066AD),
    version(1.0),
    helpstring("Dispatch interface for TExodusRosterGroup Object"),
    dual,
    oleautomation
  ]
  /**
    \interface IExodusRosterGroup
    \brief Encapsulates a roster group and associated actions.

    Methods for managing roster groups.
  **/
   interface IExodusRosterGroup: IDispatch
  {
    /**
        \brief Accesses the value of the double-click action.

        The value of action is the name of the event fired when this roster
        item is double clicked.

        \param value receives the value of action.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000C9)
    ]
    HRESULT _stdcall Action([out, retval] BSTR * value );
    /**
        \brief Sets the double click action event name.

        The value of action should be a valid event name.

        \param value the new event name.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000C9)
    ]
    HRESULT _stdcall Action([in] BSTR value );
    /**
        \brief Accesses property for displaying group when empty.

        When True this property will cause the roster to display this item even if
        it's empty.  This property does not affect the existance of

        \param value True if this group should be shown when empty.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CA)
    ]
    HRESULT _stdcall KeepEmpty([out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets property for displaying group when empty.

        When True this property will cause the roster to display this item even if
        it's empty.

        \param value True if this group should be shown when empty.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000CA)
    ]
    HRESULT _stdcall KeepEmpty([in] VARIANT_BOOL value );
    /**
        \brief Gets the relative sort priority for this roster group.

        This value affects the placement of the group in the roster.
        "Unknown" and "Offline" groups have a value of 500 and "My Resources"
        group is set at 750.

        \param value The group's current sort priority.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CB)
    ]
    HRESULT _stdcall SortPriority([out, retval] long * value );
    /**
        \brief Sets the relative sort priority for this roster group.

        This value affects the placement of the group in the roster.
        "Unknown" and "Offline" groups have a value of 500 and "My Resources"
        group is set at 750.

        \param value a relative (postive) numberic value.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000CB)
    ]
    HRESULT _stdcall SortPriority([in] long value );
    /**
        \brief Indicates if this group shows presence of it's members.

        Set to True if this group's items should reflect presence.

        \param value true if this group's items show presence icons.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CC)
    ]
    HRESULT _stdcall ShowPresence([out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets a property indicating if group shows presence icons.

        Set to True if this group's items should reflect presence.

        \param value true if this group's items show presence icons.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000CC)
    ]
    HRESULT _stdcall ShowPresence([in] VARIANT_BOOL value );
    /**
        \brief Accessor for the property indicating if the group is a drag target.
        
        True if the roster group is an allowable drag target.

        \param value True if this is a drop target.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CD)
    ]
    HRESULT _stdcall DragTarget([out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets the property indicating if this is a valid drap traget.

        Set to True to allow the UI to send drop events to this group.

        \param value new value for the drap target property.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000CD)
    ]
    HRESULT _stdcall DragTarget([in] VARIANT_BOOL value );
    /**
        \brief Accessor for property indicating if this is a drag source.

        True if the UI allows this group to be a source of drag events.

        \param value receives the current value of the property.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CE)
    ]
    HRESULT _stdcall DragSource([out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets the property indicating if this is a drag source.

        Set to True to tell the UI to allow this group to be the source
        of drag events.

        \param value the new value for this property.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000CE)
    ]
    HRESULT _stdcall DragSource([in] VARIANT_BOOL value );
    /**
        \brief Flag indicating group should auto-expand in roster.

        A True value indcates this group should be auto-expanded in the roster
        tree view.

        \param value Set to True to auto-expand the group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CF)
    ]
    HRESULT _stdcall AutoExpand([out, retval] VARIANT_BOOL * value );
    /**
        \brief Sets property indicating this group should auto-expand in roster.

        Set to True to cause group to auto-expand in the roster tree view.

        \param value True if group is set to auto-expand.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput,
    id(0x000000CF)
    ]
    HRESULT _stdcall AutoExpand([in] VARIANT_BOOL value );
    /**
        \brief Accesses the name of the group.

        This accesses the name of the group dispalyed in the roster.
        
        \param text receives the name of the group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D0)
    ]
    HRESULT _stdcall GetText([out, retval] BSTR * text );
    /**
        \brief Adds a JID to this group.

        This method adds a contact to the group by JID.  To save the change 
        use IExodusRoster.Update afterwards.

        \param jid The JID to be added to the group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D1)
    ]
    HRESULT _stdcall AddJid([in] BSTR jid );
    /**
        \brief Removes a JID from this group.

        This method removes a contact from the group by JID. To save the change
        use IExodusRoster.Update afterwards.

        \param jid The JID to be removed from the group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D2)
    ]
    HRESULT _stdcall RemoveJid([in] BSTR jid );
    /**
        \brief Determines if the JID is in this group.

        A True result indicates the JID is in this group.

        \param jid The search JID.
        \param ret True if the JID is in the group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D3)
    ]
    HRESULT _stdcall InGroup([in] BSTR jid, [out, retval] VARIANT_BOOL * ret );
    /**
        \brief Determines if the group has no items/sub-groups.

        A True result indcates this group has no JIDs or sub-groups.

        \param ret True if group has no items/sub-groups.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D4)
    ]
    HRESULT _stdcall IsEmpty([out, retval] VARIANT_BOOL * ret );
    /**
        \brief Gets a group by it's fullname.

        This method retreives a group by name (see FullName) from the roster. 
        It may return nil if the group name isn't found.

        \param group_name The name of the group to get.
        \param ret The specified group or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D5)
    ]
    HRESULT _stdcall GetGroup([in] BSTR group_name, [out, retval] IExodusRosterGroup ** ret /*Warning: unable to validate structure name: */ );
    /**
        \brief Adds a sub-group to this group.

        This method adds a sub-group to the roster by fullname (see FullName). 
        Roster.Update should be called to save changes to the server.
        \n\b Note: This method will replace an existing group with the same name.

        \param child The group to be added.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D6)
    ]
    HRESULT _stdcall AddGroup([in] IExodusRosterGroup * child /*Warning: unable to validate structure name: */ );
    /**
        \brief Removes a sub-group.

        Removes a sub-group from this group.

        \param child The sub-group to remove. Should not be nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D7)
    ]
    HRESULT _stdcall RemoveGroup([in] IExodusRosterGroup * child /*Warning: unable to validate structure name: */ );
    /**
        \brief Returns roster items in the group and sub-groups.

        The method will return contacts from the group and all sub-goups 
        filtering based on the value of the Online parameter.

        \param online Set to true to return only online contacts.
        \param items The list of items returned.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000D8)
    ]
    HRESULT _stdcall GetRosterItems([in] VARIANT_BOOL online, [out, retval] VARIANT * items );
    /**
        \brief Determines the nested level of this group.

        The nest level of the group is relative from the root of the roster's tree.

        \param value a zero based index
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D9)
    ]
    HRESULT _stdcall NestLevel([out, retval] long * value );
    /**
        \brief Determines the number of online contacts in the group.

        Determines the number of online contacts for this group and all 
        sub-groups.

        \param value Receives the number of online users.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000DA)
    ]
    HRESULT _stdcall Online([out, retval] long * value );
    /**
        \brief Counts the total number of contacts in the group.

        This method counts the total number of contacts in this group and all 
        sub-groups.

        \param value The total number of contacts in this group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000DB)
    ]
    HRESULT _stdcall Total([out, retval] long * value );
    /**
        \brief Retrieves the fullname of this group including parent groups.

        The fullname contains the name of any parent groups the current group 
        may have.  Top level roster groups have no beginning separator. For
        example:
        \li 'My Group' is a top level folder
        \li In 'My Group/My Sub-group', 'My Sub-group' is at the first nest level.

        \param value Receives the full name of this group.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000DC)
    ]
    HRESULT _stdcall FullName([out, retval] BSTR * value );
    /**
        \brief Retrieves the parent of this group.

        This method retrieves the parent group of this group or nil if the
        group has no parent.

        \param value The parent group or nil.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000DD)
    ]
    HRESULT _stdcall Parent([out, retval] IExodusRosterGroup ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Retrieves the parent group at the given index.

        This method retrieves the parent group at the given index.  The index
        is zero based starting at the furthest ancestor group.  If the index
        exceeds the nest level blank is returned. No lower bounds checking is
        performed.

        \param index The index representing the desired nest level.
        \param value The group name (not fullname) found at index.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000DE)
    ]
    HRESULT _stdcall Parts([in] long index, [out, retval] BSTR * value );
    /**
        \brief Notifies the UI that the group has changed.

        This method notifies the UI that the group has changed and needs to be
        re-rendered.  The event fired is '/roster/group'.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000DF)
    ]
    HRESULT _stdcall FireChange( void );
  };

  [
    uuid(6759BFE4-C72D-42E3-86A3-1F343E848933),
    version(1.0),
    helpstring("Dispatch interface for ExodusDiscoCache Object"),
    dual,
    oleautomation
  ]
  /**\interface IExodusEntityCache
    \brief A cache of all the IExodusEntity items known by the system.

    A cache of all the IExodusEntity items known by the system.
  **/

   interface IExodusEntityCache: IDispatch
  {
    /**
        \brief Get an IExodusEntity by JID.

        Get an IExodusEntity by JID.

        \param jid JID for the requested IExodusEntity.
        \param node The node from which you are requesting information.
        \param ret Receives IExodusEntity for requested JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall GetByJid([in] BSTR jid, [in] BSTR node, [out, retval] IExodusEntity ** ret /*Warning: unable to validate structure name: */ );
    /**
        \brief Fetch disco info from server or cache as needed.

        Fetch disco info from server or cache as needed.
				Fires '/session/entity/info' events against TJabberSession passed in node.

        \param jid JID for the requested IExodusEntity.
        \param node JabberSession object to fire events.
        \param items_limit Limit of the number of items returned from server query.
        \param ret Receives IExodusEntity for requested JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall Fetch([in] BSTR jid, [in] BSTR node, [in] VARIANT_BOOL items_limit, [out, retval] IExodusEntity ** ret /*Warning: unable to validate structure name: */ );
    /**
        \brief Dispatch a disco info query for JID.

        Dispatch a disco info query for JID.
        

        \param jid JID for the requested IExodusEntity.
        \param node JabberSession object associated with this query.
        \param timeout Timeout value (seconds) for query.
        \param ret Receives IExodusEntity for requested JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall DiscoInfo([in] BSTR jid, [in] BSTR node, [in] long timeout, [out, retval] IExodusEntity ** ret /*Warning: unable to validate structure name: */ );
    /**
        \brief Dispatch a disco items query for JID.

        Dispatch a disco items query for JID.

        \param jid JID for the requested IExodusEntity.
        \param node JabberSession object associated with this query.
        \param timeout Timeout value (seconds) for query.
        \param ret Receives IExodusEntity for requested JID.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall DiscoItems([in] BSTR jid, [in] BSTR node, [in] long timeout, [out, retval] IExodusEntity ** ret /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(1F8FF968-CB2A-480C-B8C2-1E34C493EC0F),
    version(1.0),
    helpstring("Dispatch interface for ExodusEntity Object"),
    dual,
    oleautomation
  ]
  /**\interface IExodusEntity
    \brief A representation of the disco info, features, and capabilities for various items in the system.

    A entity is a representation of the capabilities, features and disco info for a specific JID.
    This could be info on the client used by a contact, or a server's capablities, etc.
  **/

   interface IExodusEntity: IDispatch
  {
    /**
        \brief Does the entity have a specific feature.

        Does the entity have a specific feature.

        \param feature The feature being queried.
        \param ret True if entity has the specified feature, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall HasFeature([in] BSTR feature, [out, retval] VARIANT_BOOL * ret );
    /**
        \brief Does this entity have a specific Identity.

        Does this entity have a specific Identity.

        \param category Identity category to query.
        \param discoType Disco type to query.
        \param ret True if has Identity, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall HasIdentity([in] BSTR category, [in] BSTR discoType, [out, retval] VARIANT_BOOL * ret );
    /**
        \brief Are there disco items associated with this entity.

        Are there disco items associated with this entity.

        \param ret True if there are items, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall HasItems([out, retval] VARIANT_BOOL * ret );
    /**
        \brief Is there disco info associated with this entity.

        Is there disco info associated with this entity.

        \param ret True if there is info, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall HasInfo([out, retval] VARIANT_BOOL * ret );
    /**
        \brief Get the JID associated with this entity.

        Get the JID associated with this entity.

        \param value Receives JID associated with this entity.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CD)
    ]
    HRESULT _stdcall jid([out, retval] BSTR * value );
    /**
        \brief Text representation of the node associated with this entity.

        Text representation of the node associated with this entity.

        \param value Receives raw XML of node.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CE)
    ]
    HRESULT _stdcall node([out, retval] BSTR * value );
    /**
        \brief Category of entity.

        Category of entity.

        \param value Receives category for entity.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000CF)
    ]
    HRESULT _stdcall Category([out, retval] BSTR * value );
    /**
        \brief Disco type of entity.

        Disco type of entity.

        \param value Receives Disco type of entity.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D0)
    ]
    HRESULT _stdcall DiscoType([out, retval] BSTR * value );
    /**
        \brief Name of entity.

        Name of entity.

        \param value Receives name of entity.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D1)
    ]
    HRESULT _stdcall Name([out, retval] BSTR * value );
    /**
        \brief A count of the number of features in this entity.

        A count of the number of features in this entity.

        \param value Receives number of features.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D2)
    ]
    HRESULT _stdcall FeatureCount([out, retval] long * value );
    /**
        \brief Retreives feature value by index.

        Retreives feature value by zero based index.

        \param index Zero based index of desired feature.
        \param value Receives requested feature.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D3)
    ]
    HRESULT _stdcall feature([in] long index, [out, retval] BSTR * value );
    /**
        \brief A count of the number of items in this entity.

        A count of the number of items in this entity.

        \param value Receives the number of items.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D4)
    ]
    HRESULT _stdcall ItemsCount([out, retval] long * value );
    /**
        \brief Retrieves an item by index.

        Retrieves an item by zero based index.

        \param index Zero based index of desired item.
        \param value Receives IExodusEntity interface for requested item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget,
    id(0x000000D5)
    ]
    HRESULT _stdcall Item([in] long index, [out, retval] IExodusEntity ** value /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(0B992E91-DAD7-4CDC-9FD6-8007A63700E0),
    version(1.0),
    dual,
    oleautomation
  ]
	/**
  	    \interface IExodusControl
        \brief <b>UNSUPPORTED</b> - Parent interface for Controls

        <b>UNSUPPORTED</b>

        Parent interface for controls. Provides ExodusControlTypes type information
	**/
   interface IExodusControl: IDispatch
  {
    /**
        \brief <b>UNSUPPORTED</b> - Get type for the control

        <b>UNSUPPORTED</b>

        Get the type for an instance of an IExodusControl.

        This method is a getter for the readonly property ControlType.

        \param value The ExodusControlTypes type of this control.
        \return HRESULT
    **/
    [
    propget,
    id(0x000000C9)
    ]
    HRESULT _stdcall ControlType([out, retval] ExodusControlTypes * value /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(896CCC11-8929-4FEC-BC95-C96E5027C1F6),
    version(1.0),
    dual,
    oleautomation
  ]
  /**
    \interface IExodusControlCheckBox
    \brief <b>UNSUPPORTED</b> - A checkable box that can be on (checked) or off (unchecked).

    <b>UNSUPPORTED</b>

    Presents an option to the users. The user can check the box to select the
    option, or uncheck it to deselect the option.

    \since 0.9.1.1
  **/

   interface IExodusControlCheckBox: IExodusControl
  {
    [
    propget,
    id(0x00000001)
    ]
      /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput,
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget,
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput,
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget,
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side position of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput,
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget,
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput,
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget,
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput,
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget,
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput,
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget,
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput,
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget,
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput,
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget,
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput,
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget,
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput,
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget,
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput,
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget,
    id(0x0000000C)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput,
    id(0x0000000C)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    /**
        \brief Gets the property controlling how the text is aligned within the control.

        Gets the property controlling how the text is aligned within the control.

        Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Receives control's alignment (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x0000000D)
    ]
    HRESULT _stdcall Alignment([out, retval] long * value );
    /**
        \brief Set the property controlling how the text is aligned within the control.

        Specify how the text is aligned within the control.

		Use Alignment to change the way the text is formatted by the control.
				 
		\p values can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Sets control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x0000000D)
    ]
    HRESULT _stdcall Alignment([in] long value );
    /**
        \brief Gets property indicating if checkbox can be grayed out.
        
        Gets the AllowGrayed property which indicates if control allowed to be in 
        grayed out state. Should be 1 if allowed 0 if not.

        \param value Indicates if the greyed state is allowed (1) or not (0).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x0000000E)
    ]
    HRESULT _stdcall AllowGrayed([out, retval] long * value );
    /**
        \brief Sets property indicating checkbox can be grayed out.
        
        Sets the AllowGrayed property which indicates if control allowed to be in 
        grayed out state. Should be 1 if allowed 0 if not.

        \param value New value indicating if greyed state is allowed (1) or not (0).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x0000000E)
    ]
    HRESULT _stdcall AllowGrayed([in] long value );
    [
    propget,
    id(0x0000000F)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
        todo: this is incorrect
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput,
    id(0x0000000F)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

        todo: this is incorrect
    **/
    HRESULT _stdcall BiDiMode([in] long value );
    [
    propget,
    id(0x00000010)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput,
    id(0x00000010)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
     /**
        \brief Get whether the checkbox is checked.

        Retrieve whether the checkbox is checked.

        Read Checked to determine whether the checkbox is checked. Set Checked 
        to false to uncheck the checkbox.
				
        \p value is 1 (TRUE) or 0 (FALSE)

        \param value Receives the value of Checked.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x00000011)
    ]
    HRESULT _stdcall Checked([out, retval] long * value );
    /**
        \brief Sets whether the checkbox is checked.

        Sets whether the checkbox is checked.
        \p value is 1 (TRUE) or 0 (FALSE)

        \param value True to check the checkbox, false to uncheck it.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x00000011)
    ]
    HRESULT _stdcall Checked([in] long value );
    [
    propget,
    id(0x00000012)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput,
    id(0x00000012)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget,
    id(0x00000013)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x00000013)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget,
    id(0x00000014)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput,
    id(0x00000014)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget,
    id(0x00000015)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput,
    id(0x00000015)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget,
    id(0x00000016)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput,
    id(0x00000016)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget,
    id(0x00000017)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput,
    id(0x00000017)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget,
    id(0x00000018)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    [
    propget,
    id(0x00000019)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput,
    id(0x00000019)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget,
    id(0x0000001A)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput,
    id(0x0000001A)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget,
    id(0x0000001B)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput,
    id(0x0000001B)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget,
    id(0x0000001C)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput,
    id(0x0000001C)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget,
    id(0x0000001D)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput,
    id(0x0000001D)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget,
    id(0x0000001E)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget,
    id(0x0000001F)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput,
    id(0x0000001F)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    /**
        \brief Determines the state of the checkbox (checked/unchecked/grayed).

        Determines the state of the checkbox (checked/unchecked/grayed).
        Similar to Checked, but detects the grayed state as well. value can be:
        \li 0   Unchecked
        \li 1   Checked
        \li 2   Greyed

        \param value Recieves the state of the checkbox (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x00000020)
    ]
    HRESULT _stdcall State([out, retval] long * value );
    /**
        \brief Sets the state of the checkbox (checked/unchecked/grayed).

        Sets the state of the checkbox (checked/unchecked/grayed).
        Similar to Checked, but sets the checkbox to the grayed state as well.
        value can be:
        \li 0   Unchecked
        \li 1   Checked
        \li 2   Greyed

        \param value The new state of the checkbox (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x00000020)
    ]
    HRESULT _stdcall State([in] long value );
    [
    propget,
    id(0x00000021)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput,
    id(0x00000021)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget,
    id(0x00000022)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput,
    id(0x00000022)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    /**
        \brief Get whether the checkbox text wraps to fit the width of the control.

        Retrieve whether the checkbox text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Receives WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x00000023)
    ]
    HRESULT _stdcall WordWrap([out, retval] long * value );
    /**
        \brief Set whether the checkbox text wraps to fit the width of the control.

        Specifies whether the checkbox text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Sets WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x00000023)
    ]
    HRESULT _stdcall WordWrap([in] long value );
  };

  [
    uuid(16D21C8F-EF88-4E93-87C6-CD8F8C1EE7F7),
    version(1.0),
    dual,
    oleautomation
  ]
  /**
    \interface IExodusControlComboBox
    \brief <b>UNSUPPORTED</b> - Combines an edit box with a scrollable list.

    <b>UNSUPPORTED</b>

    An edit box with a scrollable drop-down list attached to it.
    Users can select an item from the list or type directly into the edit box.

    \since 0.9.1.1
  **/

   interface IExodusControlComboBox: IExodusControl
  {
    [
    propget,
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput,
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget,
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput,
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget,
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput,
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget,
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput,
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget,
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput,
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget,
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput,
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget,
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput,
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget,
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput,
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget,
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput,
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget,
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput,
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget,
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput,
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    /**
        \brief Gets the property indicating if text entered will autocomplete.
        
        Gets the property indicating if text entered will autocomplete.  Text 
        matching items in the combobox will autocomplete if this property is 
        set to 1.

        \param value Recieves the value of this property (1 or 0).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x0000000C)
    ]
    HRESULT _stdcall AutoComplete([out, retval] long * value );
    /**
        \brief Sets the property indicating if text entered will autocomplete.
        
        Sets the property indicating if text entered will autocomplete.  Text 
        matching items in the combobox will autocomplete if this property is 
        set to 1.

        \param value The new value of this property (1 or 0).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x0000000C)
    ]
    HRESULT _stdcall AutoComplete([in] long value );
    /**
        \brief Gets property indicating if dropdown list appears when user types.
        
        Specifies whether the drop-down list drops down automatically in 
        response to user keystrokes. A value of 1 indicates it will drop-down
        automatically. A value of 0 indicates it won't automatically drop-down.

        \param value Recieves the value of the AutoDropdown property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x0000000D)
    ]
    HRESULT _stdcall AutoDropDown([out, retval] long * value );
    /**
        \brief Sets property indicating if dropdown list appears when user types.
        
        Specifies whether the drop-down list drops down automatically in 
        response to user keystrokes. A value of 1 indicates it will drop-down
        automatically. A value of 0 indicates it won't automatically drop-down.

        \param value The new value of the AutoDropdown property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x0000000D)
    ]
    HRESULT _stdcall AutoDropDown([in] long value );
    /**
        \brief Gets property indicating if dropdown list auto-closes when item is selected.
        
        Gets the property indicating if dropdown list auto-closes when user selects 
        an item. A value of 1 indicates the combobox will autoclose, and 0 means it won't.

        \param value Recieves the value of the AutoCloseUp property (1 or 0).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoCloseUp([out, retval] long * value );
    /**
        \brief Sets property indicating if dropdown list auto-closes when item is selected.
        
        Sets the property indicating if dropdown list auto-closes when user selects 
        an item. A value of 1 indicates the combobox will autoclose, and 0 means it won't.

        \param value The new value of the AutoCloseUp property (1 or 0).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoCloseUp([in] long value );
    /**
        \brief Get the cut of the inner bevel.

        Retreive the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. 
        
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x0000000F)
    ]
    HRESULT _stdcall BevelInner([out, retval] long * value );
  /**
        \brief Sets the cut of the inner bevel.

        Specifies the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value are:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
		\p value should be one of 0, 1, 2, 3.
 
        \param value Sets control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x0000000F)
    ]
    HRESULT _stdcall BevelInner([in] long value );
    /**
        \brief Get the control's bevel style.

        Retreive the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Receives control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x00000010)
    ]
    HRESULT _stdcall BevelKind([out, retval] long * value );
    /**
        \brief Set the control's bevel style.

        Specify the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Sets control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x00000010)
    ]
    HRESULT _stdcall BevelKind([in] long value );
    /**
        \brief Get the cut of the outer bevel.

        Retrieve the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
		The outer bevel appears immediately inside the border and outside the inner bevel. 
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x00000011)
    ]
    HRESULT _stdcall BevelOuter([out, retval] long * value );
    /**
        \brief Set the cut of the outer bevel.

        Specifies the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
	    The outer bevel appears immediately inside the border and outside the inner bevel. 
        
        \c value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Sets control's bevel (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x00000011)
    ]
    HRESULT _stdcall BevelOuter([in] long value );
    /**
        \brief Gets the style property of the combo box.

        This method gets the visual style property of the control.
        The property determines the look and feel of the control.
        
        \c value can be:
        \li 0 - Drop down list with all items of the same height
        \li 1 - An edit box over a list box of values
        \li 2 - Drop down list but text box isn't editable
        \li 3 - Drop down list that can combine text/graphics, items are all of ItemHeight height.
        \li 4 - Drop down list that can combine text/graphics, items are of variable height.

        \param value The value of the style property (0, 1, 2, 3, 4).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x00000012)
    ]
    HRESULT _stdcall Style([out, retval] long * value );
    /**
        \brief Sets the style property of the combo box.

        This method sets the visual style property of the control.
        The property determines the look and feel of the control.

        \c value can be:
        \li 0 - Drop down list with all items of the same height
        \li 1 - An edit box over a list box of values
        \li 2 - Drop down list but text box isn't editable
        \li 3 - Drop down list that can combine text/graphics, items are all of ItemHeight height.
        \li 4 - Drop down list that can combine text/graphics, items are of variable height.

        \param value The value of the style property (0, 1, 2, 3, 4).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput,
    id(0x00000012)
    ]
    HRESULT _stdcall Style([in] long value );
    [
    propget,
    id(0x00000013)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput,
    id(0x00000013)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Gets the property indicating control forces text case.

        Use CharCase to force the text contents of the control into either upper 
        or lower case.  Once converted the original case information cannont be
        recovered.

        value are:
        \li ecNormal    (0) - leaves case unaltered
        \li ecUpperCase (1) - forces text to upper case
        \li ecLowerCase (2) - forces text to lower case

        \param The current value of the CharCase property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget,
    id(0x00000014)
    ]
    HRESULT _stdcall CharCase([out, retval] long * value );
    /**
        \brief Sets the property indicating control forces text case.

        Use CharCase to force the text contents of the control into either upper 
        or lower case.  Once converted the original case information cannont be
        recovered.

        value can be:
        \li ecNormal    (0) - leaves case unaltered
        \li ecUpperCase (1) - forces text to upper case
        \li ecLowerCase (2) - forces text to lower case

        \param The new value of the CharCase property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000014)
    ]
    HRESULT _stdcall CharCase([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    /**
        \brief Gets the value of the DropDownCount property.

        Use the DropDownCount property to determine how many items are shown by
        default when the dropdown list opens.  The default value is 8.

        \param value The current value of dropdown list items to show.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001A)
    ]
    HRESULT _stdcall DropDownCount([out, retval] long * value );
    /**
        \brief Sets the value of the DropDownCount property.

        Use the DropDownCount property to determine how many items are shown by
        default when the dropdown list opens.  The default value is 8.

        \param value The new amount of dropdown items to show.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001A)
    ]
    HRESULT _stdcall DropDownCount([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget,
    id(0x0000001C)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the current value of the IME mode for this control.

        This method gets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value Receives the current value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001D)
    ]
    HRESULT _stdcall ImeMode([out, retval] long * value );
    /**
        \brief Sets the current value of the IME mode for this control.

        This method sets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value The new value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001D)
    ]
    HRESULT _stdcall ImeMode([in] long value );
    /**
        \brief Gets the ImeName property of the control.

        Gets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determines which IME tool will be used to convert user 
        input.

        \param value Receives the name of the current IME instance being used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001E)
    ]
    HRESULT _stdcall ImeName([out, retval] BSTR * value );
    /**
        \brief Sets the ImeName property of the control.

        Sets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determine which IME tool will be used to convert user 
        input. If the supplied name isn't recognized the currently selected IME
        instance remains active and no error is thrown.

        \param value The new name of the IME instance to be used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001E)
    ]
    HRESULT _stdcall ImeName([in] BSTR value );
    /**
        \brief Gets the height of combo items in pixels.

        This method gets the height of the combo items in the drop down list 
        in pixels.

        \b Note: The value of this property is only used if, and only if, the 
        Style property is set to 3 or 4 (the owner-draw settings).

        \param value The height in pixels of the combo items.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001F)
    ]
    HRESULT _stdcall ItemHeight([out, retval] long * value );
    /**
        \brief Sets the height of combobox list items in pixels.

        This method sets the height of the combobox list items in pixels.
        
       \b Note: The value of this property is only used if, and only if, the 
        Style property is set to 3 or 4 (the owner-draw settings).

        \param value The height in pixels of the combo items.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001F)
    ]
    HRESULT _stdcall ItemHeight([in] long value );
    /**
        \brief Gets the currently selected item's index.

        This method retrieves the currently selected item's index.  The first
        item in the list is item 0 and if no item is selected the value is -1.

        \param value Receieves the index of the currently selected item or -1 if no selection.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000020)
    ]
    HRESULT _stdcall ItemIndex([out, retval] long * value );
    /**
        \brief Sets the currently selected item by index.

        This method sets the currently selected item programmatically using 
        it's index number.  Indexes are zero based. Use -1 to clear selection.

        \param value The new index of the selected item or -1 for no selection.
        \return S_OK if successful, or an error value otherwise.
    **/
    **/
    [
    propput, 
    id(0x00000020)
    ]
    HRESULT _stdcall ItemIndex([in] long value );
    /**
        \brief Gets the maximum number of characters that can be input in the edit box.

        This method returns the limit to the number of characters that can be 
        input into the edit portion of the combo box.  A value of 0 indicates 
        there is no application limit (the underlying OS widgets may still have 
        a limit).

        \param value Receives the maximum number characters that can be input to the edit box.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000021)
    ]
    HRESULT _stdcall MaxLength([out, retval] long * value );
    /**
        \brief Sets the maximum number of characters that can be input in the edit box.

        This method sets the limit to the number of characters that can be input
        into the edit portion of the combo box.  A value of 0 indicates there is
        no application limit (the underlying OS widgets may still have a limit).

        Setting this value while text is in the input box will not truncate 
        existing text, it will only limit further user input.

        \param value Receives the maximum number characters that can be input to the edit box.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000021)
    ]
    HRESULT _stdcall MaxLength([in] long value );
    [
    propget, 
    id(0x00000022)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000022)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000023)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000023)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000024)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput, 
    id(0x00000024)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget, 
    id(0x00000025)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000025)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000026)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput,
    id(0x00000026)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000027)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000028)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000028)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    /**
        \brief Retrieves property indicating if combo box list items are sorted.

        This method retrieves the property indicating if the combo box list items 
        are sorted alphabetically.
        value can be:
        \li 0 - unsorted
        \li 1 - sorted

        \param value Receives the sorted property (0 unsorted, 1 sorted).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000029)
    ]
    HRESULT _stdcall Sorted([out, retval] long * value );
    /**
        \brief Sets property indicating if combo box list items are sorted.

        This method sets the property indicating if the combo box list items are 
        sorted alphabetically.
        value can be:
        \li 0 - unsorted
        \li 1 - sorted

        Note: Once items are sorted the original order is lost.

        \param value The new value for the sorted property (0 unsorted, 1 sorted).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000029)
    ]
    HRESULT _stdcall Sorted([in] long value );
    [
    propget, 
    id(0x0000002A)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000002A)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x0000002B)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000002B)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    /**
        \brief Gets the text in the edit portion of the combo box.

        This method retrieves the text in the edit portion of the combo box. This
        could return an existing list item's text or user entered text.

        \param value Receives the text in the edit portion of the combo box.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002C)
    ]
    HRESULT _stdcall Text([out, retval] BSTR * value );
    /**
        \brief Sets the text in the edit portion of the combo box.

        This method sets the text in the edit portion of the combo box. This
        could return an existing list item's text or user entered text.

        \param value The new text to place in the edit portion of the combo box.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002C)
    ]
    HRESULT _stdcall Text([in] BSTR value );
    [
    propget, 
    id(0x0000002D)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieves whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000002D)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    /**
        \brief Gets the list item text at the specified index.

        This method returns the list item text at the given index. This method
        does bounds checking on the supplied index.

        \param index The zero based index of the item to retrieve.
        \param value Receives the value of the item (or blank if index invalid).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002E)
    ]
    HRESULT _stdcall Items([in] long index, [out, retval] BSTR * value );
    /**
        \brief Sets the list item text at the specified index.

        This method sets the list item text at the given index. This method does
        bounds checking on the supplied index. It is not possible to add items
        using this method - but the existing values can be modified.

        \param index The zero based index of the item to set.
        \param value The new value of the list item (do not use nil - blank is allowed).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002E)
    ]
    HRESULT _stdcall Items([in] long index, [in] BSTR value );
  };

  [
    uuid(A7B8A353-FF1E-4933-9A01-BD7B0FDC6F02), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlEdit
    \brief  <b>UNSUPPORTED</b> - A wrapper for a Windows single-line edit control.

    <b>UNSUPPORTED</b>

    Edit controls are used to retrieve text that users type.
    Edit controls can also display text to the user.

    \since 0.9.1.1
  **/

   interface IExodusControlEdit: IExodusControl
  {
    [
    propget,
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    /**
        \brief Gets property indicating if text should be selected when control is focused.

        This method gets property indicating if text should be selected when control
        is focused.

        value can be:
        \li 0  - false (text not selected)
        \li 1  - true  (text will be selected)

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000D)
    ]
    HRESULT _stdcall AutoSelect([out, retval] long * value );
    /**
        \brief Sets property indicating if text should be selected when control is focused.

        This method sets a property indicating if text should be selected when 
        control is focused.

        value can be:
        \li 0  - false (text not selected)
        \li 1  - true  (text will be selected)

        \param value Sets the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000D)
    ]
    HRESULT _stdcall AutoSelect([in] long value );
    /**
        \brief Get whether the control sizes itself automatically to accommodate its contents.

        Retreive whether the control sizes itself automatically to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoSize([out, retval] long * value );
    /**
        \brief Set whether the control sizes itself automatically to accommodate its contents.

        Specifies whether the control sizes itself automatically to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Gets control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoSize([in] long value );
    /**
        \brief Gets the cut of the inner bevel.

        Retreives the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value can be:
       \li bvNone    (0)   No bevel
       \li bvLowered (1)   Lowered bevel
       \li bvRaised  (2)   Raised bevel
       \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000F)
    ]
    HRESULT _stdcall BevelInner([out, retval] long * value );
    /**
        \brief Sets the cut of the inner bevel.

        Specifies the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
		\p value should be one of 0, 1, 2, 3.
 
        \param value Sets control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000F)
    ]
    HRESULT _stdcall BevelInner([in] long value );
    /**
        \brief Get the control's bevel style.

        Retreive the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Receives control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000010)
    ]
    HRESULT _stdcall BevelKind([out, retval] long * value );
    /**
        \brief Sets the control's bevel style.

        Specify the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Sets control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000010)
    ]
    HRESULT _stdcall BevelKind([in] long value );
    /**
        \brief Get the cut of the outer bevel.

        Retrieve the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
		The outer bevel appears immediately inside the border and outside the inner bevel. 
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000011)
    ]
    HRESULT _stdcall BevelOuter([out, retval] long * value );
    /**
        \brief Set the cut of the outer bevel.

        Specifies the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
	    The outer bevel appears immediately inside the border and outside the inner bevel. 
        
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Sets control's bevel (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000011)
    ]
    HRESULT _stdcall BevelOuter([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Get whether the edit control has a single line border around the client area.

        Retreive whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Receives control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000013)
    ]
    HRESULT _stdcall BorderStyle([out, retval] long * value );
    /**
        \brief Set whether the edit control has a single line border around the client area.

        Specify whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Sets control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000013)
    ]
    HRESULT _stdcall BorderStyle([in] long value );
    /**
        \brief Gets the property indicating control forces text case.

        Use CharCase to force the text contents of the control into either upper 
        or lower case.  Once converted the original case information cannont be
        recovered.

        value can be:
        \li ecNormal    (0) - leaves case unaltered
        \li ecUpperCase (1) - forces text to upper case
        \li ecLowerCase (2) - forces text to lower case

        \param The current value of the CharCase property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000014)
    ]
    HRESULT _stdcall CharCase([out, retval] long * value );
    /**
        \brief Sets the property indicating control forces text case.

        Use CharCase to force the text contents of the control into either upper 
        or lower case.  Once converted the original case information cannont be
        recovered.

        value can be:
        \li ecNormal    (0) - leaves case unaltered
        \li ecUpperCase (1) - forces text to upper case
        \li ecLowerCase (2) - forces text to lower case

        \param The new value of the CharCase property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000014)
    ]
    HRESULT _stdcall CharCase([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x00000016)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Get whether the visual indication of the selected text remains when focus shifts to another control.

        Retreive whether the visual indication of the selected text remains when focus shifts to another control.

				Set HideSelection to false when it is important to provide visual feedback of the selected portion of the text even when the edit control does not have focus. Set HideSelection to true to make the visual indication of selection appear only when the edit control has focus. HideSelection does not affect the actual value of the selection, only the visual indication. Always setting HideSelection to false can make forms containing many edit controls look busy.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's HideSelection value.
        \return S_OK if successful, or an error value otherwise.
    **/ 
    [
    propget, 
    id(0x0000001C)
    ]
    HRESULT _stdcall HideSelection([out, retval] long * value );
    /**
        \brief Set whether the visual indication of the selected text remains when focus shifts to another control.

        Specify whether the visual indication of the selected text remains when focus shifts to another control.

				Set HideSelection to false when it is important to provide visual feedback of the selected portion of the text even when the edit control does not have focus. Set HideSelection to true to make the visual indication of selection appear only when the edit control has focus. HideSelection does not affect the actual value of the selection, only the visual indication. Always setting HideSelection to false can make forms containing many edit controls look busy.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's HideSelection value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001C)
    ]
    HRESULT _stdcall HideSelection([in] long value );
    /**
        \brief Gets the current value of the IME mode for this control.

        This method gets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value Receives the current value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001D)
    ]
    HRESULT _stdcall ImeMode([out, retval] long * value );
    /**
        \brief Sets the current value of the IME mode for this control.

        This method sets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value The new value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001D)
    ]
    HRESULT _stdcall ImeMode([in] long value );
    /**
        \brief Gets the ImeName property of the control.

        Gets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determines which IME tool will be used to convert user 
        input.

        \param value Receives the name of the current IME instance being used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001E)
    ]
    HRESULT _stdcall ImeName([out, retval] BSTR * value );
    /**
        \brief Sets the ImeName property of the control.

        Sets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determine which IME tool will be used to convert user 
        input. If the supplied name isn't recognized the currently selected IME
        instance remains active and no error is thrown.

        \param value The new name of the IME instance to be used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001E)
    ]
    HRESULT _stdcall ImeName([in] BSTR value );
   /**
        \brief Get the maximum number of characters the user can enter into the edit control.

        Retreive the maximum number of characters the user can enter into the edit control.

		Use MaxLength to limit the number of characters that can be entered into the edit control. A value of 0 indicates that there is no application-defined limit on the length.
				
		\param value Receives the Maximum Length.
        \return S_OK if successful, or an error value otherwise.
    **/ 
    [
    propget, 
    id(0x0000001F)
    ]
    HRESULT _stdcall MaxLength([out, retval] long * value );
    /**
        \brief Set the maximum number of characters the user can enter into the edit control.

        Specifies the maximum number of characters the user can enter into the edit control.

		Use MaxLength to limit the number of characters that can be entered into the edit control. A value of 0 indicates that there is no application-defined limit on the length.
				
		\param value Set the MaximumLength.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001F)
    ]
    HRESULT _stdcall MaxLength([in] long value );
    /**
        \brief Gets property that determines if characters typed in control will map to OEM.

        This method gets the property that determines if characters typed in control
        are converted from ANSI to OEM back to ANSI. The conversion is to ensure 
        entered characters can be unambiguously mapped to an OEM character.

        Note: If your application supports Unicode this functionality may not be needed.

        value can be:
        \li 0 - no conversion
        \li 1 - perform conversion of text from ANSI to OEM back to ANSI

        \param value The current value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000020)
    ]
    HRESULT _stdcall OEMConvert([out, retval] long * value );
   /**
        \brief Sets property that determines if characters typed in control will map to OEM.

        This method gets the property that determines if characters typed in control
        are converted from ANSI to OEM back to ANSI. The conversion is to ensure 
        entered characters can be unambiguously mapped to an OEM character.

        Note: If your application supports Unicode this functionality may not be needed.

        value can be:
        \li 0 - no conversion
        \li 1 - perform conversion of text from ANSI to OEM back to ANSI

        \param value The current value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/ 
    [
    propput, 
    id(0x00000020)
    ]
    HRESULT _stdcall OEMConvert([in] long value );
    [
    propget, 
    id(0x00000021)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000021)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000022)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000022)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000023)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput, 
    id(0x00000023)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget, 
    id(0x00000024)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000024)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000025)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000025)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    /**
        \brief Gets the character used to display in place of typed characters.

        This method gets the character used to show in place of plain text in
        password fields.  If the value is nil (null) then no display 
        replacement is done.  
        
        Note: The actual value of the Text property is unchanged.

        \param value Receives the character used to replace typed characters (can be nil).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000026)
    ]
    HRESULT _stdcall PasswordChar([out, retval] BSTR * value );
    /**
        \brief Sets the character used to display in place of typed characters.

        This method sets the character used to show in place of plain text in
        password fields.  If the value is nil (null) then no display 
        replacement is done.  
        
        Note: The actual value of the Text property is unchanged.

        \param value The character used to replace typed characters (can be nil).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000026)
    ]
    HRESULT _stdcall PasswordChar([in] BSTR value );
    [
    propget, 
    id(0x00000027)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Get whether the user can change the text of the edit control.

        Retreive whether the user can change the text of the edit control.

		To restrict the edit control to display only, set the ReadOnly property to true. Set ReadOnly to false to allow the contents of the edit control to be edited.
 
		Setting ReadOnly to true ensures that the text is not altered, while still allowing the user to select text. The selected text can then be manipulated by the application, or copied to the Clipboard.
		
		\p value is 1 (TRUE) or 0 (FALSE)
		
		\param value Receives Read Only value.
        \return S_OK if successful, or an error value otherwise.
    **/ 
    [
    propget, 
    id(0x00000028)
    ]
    HRESULT _stdcall ReadOnly([out, retval] long * value );
    /**
        \brief Set whether the user can change the text of the edit control.

        Specifies whether the user can change the text of the edit control.

		To restrict the edit control to display only, set the ReadOnly property to true. Set ReadOnly to false to allow the contents of the edit control to be edited.
 
		Setting ReadOnly to true ensures that the text is not altered, while still allowing the user to select text. The selected text can then be manipulated by the application, or copied to the Clipboard.

		\p value is 1 (TRUE) or 0 (FALSE)
				
		\param value Sets Read Only value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000028)
    ]
    HRESULT _stdcall ReadOnly([in] long value );
    [
    propget, 
    id(0x00000029)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000029)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000002A)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000002A)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    /**
        \brief Gets the text string associated with the control.

        This method retrieves the text string associated with the control.

        \param value Receives the current value of the control's text.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002B)
    ]
    HRESULT _stdcall Text([out, retval] BSTR * value );
    /**
        \brief Sets the text string associated with the control.

        This method sets the text string associated with the control.

        \param value The new value of the control's text.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002B)
    ]
    HRESULT _stdcall Text([in] BSTR value );
    [
    propget, 
    id(0x0000002C)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000002C)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
  };

  [
    uuid(F53704E6-83C2-4021-97A5-169BC58D9E03), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlLabel
    \brief <b>UNSUPPORTED</b> - A nonwindowed control that displays text on a form.

    <b>UNSUPPORTED</b> 

    Use to add text that the user can't edit to a form.
    This text can be used to label another control, and can set focus to
    that control when the user types an accelerator key

    \since 0.9.1.1
  **/

   interface IExodusControlLabel: IExodusControl
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
     /**
        \brief Get how the control aligns within its container (parent control).

        Retreive how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Receives control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000C)
    ]
    HRESULT _stdcall Align([out, retval] long * value );
    /**
        \brief Set how the control aligns within its container (parent control).

        Specify how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Sets control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000C)
    ]
    HRESULT _stdcall Align([in] long value );
    /**
        \brief Gets the property controlling how the text is aligned within the control.

        Gets the property controlling how the text is aligned within the control.

        Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Receives control's alignment (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Alignment([out, retval] long * value );
    /**
        \brief Sets the property determining how the text is aligned within the control.

        Specify how the text is aligned within the control.

        Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Sets control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Alignment([in] long value );
    /**
        \brief Get whether the control sizes itself automatically to accommodate its contents.

        Retreive whether the control sizes itself automatically to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoSize([out, retval] long * value );
    /**
        \brief Set whether the control sizes itself automatically to accommodate its contents.

        Specifies whether the control sizes itself automatically to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Gets control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoSize([in] long value );
    [
    propget, 
    id(0x0000000F)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)
        
        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput,
    id(0x0000000F)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000013)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Determine how an ampersand is displayed in the label.

        This method determines how an ampersand is displayed in the label. When
        set to true, any character preceeded by an ampersand appears underlined.

        Setting this property affects display only to enable accelerator key 
        behavior also see FocusControl.

        value can be:
        \li 0 - false (an & is an &)
        \li 1 - true  (an & indicates an accelerator)
        
        \param value 1 if & indicates an accelerated key (underlined char).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001C)
    ]
    HRESULT _stdcall ShowAccelChar([out, retval] long * value );
    /**
        \brief Sets how an ampersand is displayed in the label.

        This method determines how an ampersand is displayed in the label. When
        set to true, any character preceeded by an ampersand appears underlined.

        Setting this property affects display only to enable accelerator key 
        behavior also see FocusControl.

        value can be:
        \li 0 - false (an & is an &)
        \li 1 - true  (an & indicates an accelerator)
        
        \param value Flag for & handling (0, 1 - indcates accelerator).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001C)
    ]
    HRESULT _stdcall ShowAccelChar([in] long value );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    /**
        \brief Get whether the background of the control is transparent.

        Retrieve whether the background of the control is transparent.

        Use Transparent to specify whether the background of the control is transparent.
        
        \p value will be either 0 (FALSE) or 1 (TRUE).

        \param value Receives the control's transparency.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001E)
    ]
    HRESULT _stdcall Transparent([out, retval] long * value );
    /**
        \brief Set whether the background of the control is transparent.

        Sets whether the background of the control is transparent.

        Use Transparent to specify whether the background of the control is transparent.
        
        \p value will be either 0 (FALSE) or 1 (TRUE).

        \param value The new value of the control's transparency.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001E)
    ]
    HRESULT _stdcall Transparent([in] long value );
    /**
        \brief Gets the layout used by the label text.

        This method gets the layout used by the label text.
        The layout value are:
        \li 0 - left justified
        \li 1 - right justified
        \li 2 - top of bottom
        \li 3 - bottom of button

        \param value An ordinal indicating the justification of the text.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001F)
    ]
    HRESULT _stdcall Layout([out, retval] long * value );
    /**
        \brief Sets the layout used by the label text.

        This method gets the layout used by the label text.
        The layout value are:
        \li 0 - left justified
        \li 1 - right justified
        \li 2 - top of bottom
        \li 3 - bottom of button

        \param value An ordinal indicating the justification of the text.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001F)
    ]
    HRESULT _stdcall Layout([in] long value );
    [
    propget, 
    id(0x00000020)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x00000020)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    /**
        \brief Get whether the button text wraps to fit the width of the control.

        Retrieve whether the text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Receives WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000021)
    ]
    HRESULT _stdcall WordWrap([out, retval] long * value );
    /**
        \brief Set whether the button text wraps to fit the width of the control.

        Specifies whether the text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Sets WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000021)
    ]
    HRESULT _stdcall WordWrap([in] long value );
  };

  [
    uuid(F34F969E-4BC2-4ADE-8648-A8F618FCC205), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlListBox
    \brief <b>UNSUPPORTED</b> - Displays a collection of items in a scrollable list.
    <b>UNSUPPORTED</b>

    Use to display a scrollable list of items that users can select, add, or
    delete. This control is a wrapper for the Windows listbox control.

    \since 0.9.1.1
  **/

   interface IExodusControlListBox: IExodusControl
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    /**
        \brief Gets the property indicating the type of list box - standard, owner draw, or virtual.

        This method gets the property indicating the type of list box - standard, 
        owner draw, or virtual.

        value can be:
        \li 0 - standard (fixed size, string items only)
        \li 1 - owner draw fixed (used to display fixed sized items w/images)
        \li 2 - owner draw variable size (use to display variable sized items w/images)
        \li 3 - virtual
        \li 4 - virtual owner draw

        \param value Receives the setting for the Style property (0, 1, 2, 3, 4).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Style([out, retval] long * value );
    /**
        \brief Sets the property indicating the type of list box - standard, owner draw, or virtual.

        This method sets the property indicating the type of list box - standard, 
        owner draw, or virtual.

        value can be:
        \li 0 - standard (fixed size, string items only)
        \li 1 - owner draw fixed (used to display fixed sized items w/images)
        \li 2 - owner draw variable size (use to display variable sized items w/images)
        \li 3 - virtual
        \li 4 - virtual owner draw

        \param value The property value for the Style property (0, 1, 2, 3, 4).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Style([in] long value );
    /**
        \brief Gets the property indicating user can focus items by typing in the list.
        
        This method gets the property indicating user can focus items by typing in
        the list.  value can be 0 (false - don't focus) or 1 (true - focus).

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoComplete([out, retval] long * value );
    /**
        \brief Sets the property indicating user can focus items by typing in the list.
        
        This method sets the property indicating user can focus items by typing in
        the list.  value can be 0 (false - don't focus) or 1 (true - focus).

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AutoComplete([in] long value );
    /**
        \brief Get how the control aligns within its container (parent control).

        Retreive how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Receives control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Align([out, retval] long * value );
    /**
        \brief Set how the control aligns within its container (parent control).

        Specify how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Sets control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Align([in] long value );
    /**
        \brief Get the cut of the inner bevel.

        Retreive the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value are:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000010)
    ]
    HRESULT _stdcall BevelInner([out, retval] long * value );
     /**
        \brief Sets the cut of the inner bevel.

        Specifies the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value are:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
		\p value should be one of 0, 1, 2, 3.
 
        \param value Sets control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000010)
    ]
    HRESULT _stdcall BevelInner([in] long value );
    /**
        \brief Get the control's bevel style.

        Retreive the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Receives control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000011)
    ]
    HRESULT _stdcall BevelKind([out, retval] long * value );
    /**
        \brief Sets the control's bevel style.

        Specify the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Sets control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000011)
    ]
    HRESULT _stdcall BevelKind([in] long value );
    /**
        \brief Get the cut of the outer bevel.

        Retrieve the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
		The outer bevel appears immediately inside the border and outside the inner bevel. 
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000012)
    ]
    HRESULT _stdcall BevelOuter([out, retval] long * value );
    /**
        \brief Set the cut of the outer bevel.

        Specifies the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
	    The outer bevel appears immediately inside the border and outside the inner bevel. 
        
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Sets control's bevel (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000012)
    ]
    HRESULT _stdcall BevelOuter([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000013)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Get whether the edit control has a single line border around the client area.

        Retreive whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Receives control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000014)
    ]
    HRESULT _stdcall BorderStyle([out, retval] long * value );
    /**
        \brief Set whether the edit control has a single line border around the client area.

        Specify whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Sets control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000014)
    ]
    HRESULT _stdcall BorderStyle([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    /**
        \brief Gets the number of columns in this list control.

        This method retrieves the number of columns in this list control. The
        default value is 0 - which indicates there's a single column.

        \param value Receives the number of columns in the list (0 means 1 column).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000016)
    ]
    HRESULT _stdcall Columns([out, retval] long * value );
    /**
        \brief Sets the number of columns in this list control.

        This method sets the number of columns in this list control. The default
        value is 0 - which indicates there's a single column.

        \param value Sets the number of columns in the list (0 means 1 column).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000016)
    ]
    HRESULT _stdcall Columns([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x00000017)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    /**
        \brief Get property indicating if user can select a range of items.

        This method gets the property indicating if user can select a range of 
        items using the standard Windows methodolgies (i.e. using shift or 
        control keys). This property works in conjunction with MultiSelect.  
        
        If both MultiSelect and ExtendedSelect are set to true then selecting 
        with shift or control allows selection of a range of values (or 
        non-contiguous values). 
        
        If ExtendedSelect was set to false and MultiSelect is true, the user can
        select multiple items (without using the shift or control keys) but not 
        in a single operation.

        If MultiSelect is false, ExtendedSelect has no effect.
        
        value can be:
        \li 0  - not enabled (false)
        \li 1  - enabled (true)

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001C)
    ]
    HRESULT _stdcall ExtendedSelect([out, retval] long * value );
    /**
        \brief Sets property indicating if user can select a range of items.

        This method sets the property indicating if user can select a range of 
        items using the standard Windows methodolgies (i.e. using shift or 
        control keys). This property works in conjunction with MultiSelect.  
        
        If both MultiSelect and ExtendedSelect are set to true then selecting 
        with shift or control allows selection of a range of values (or 
        non-contiguous values). 
        
        If ExtendedSelect was set to false and MultiSelect is true, the user can
        select multiple items (without using the shift or control keys) but not 
        in a single operation.

        If MultiSelect is false, ExtendedSelect has no effect.
        
        value can be:
        \li 0  - not enabled (false)
        \li 1  - enabled (true)

        \param value The new value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001C)
    ]
    HRESULT _stdcall ExtendedSelect([in] long value );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the current value of the IME mode for this control.

        This method gets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value Receives the current value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001E)
    ]
    HRESULT _stdcall ImeMode([out, retval] long * value );
    /**
        \brief Sets the current value of the IME mode for this control.

        This method sets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value The new value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001E)
    ]
    HRESULT _stdcall ImeMode([in] long value );
    /**
        \brief Gets the ImeName property of the control.

        Gets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determines which IME tool will be used to convert user 
        input.

        \param value Receives the name of the current IME instance being used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001F)
    ]
    HRESULT _stdcall ImeName([out, retval] BSTR * value );
    /**
        \brief Sets the ImeName property of the control.

        Sets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determine which IME tool will be used to convert user 
        input. If the supplied name isn't recognized the currently selected IME
        instance remains active and no error is thrown.

        \param value The new name of the IME instance to be used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001F)
    ]
    HRESULT _stdcall ImeName([in] BSTR value );
    /**
        \brief Gets the property determining how the list box displays partial items.

        This method gets the property determining how the list box displays 
        partial items.  This method has dependancies on the Style property.

        If IntegralHeight is true (1) and Style is set to owner draw fixed (1),
        the list box height will always be a multiple of ItemHeight and partial 
        items will NOT be displayed.

        If IntegralHeight is false (0), the list box height can be any value, but 
        the last item may not be fully displayed.

        If Style is set to owner draw variable (2), then setting IntegralHeight 
        has no effect.

        value can be:
        \li 0 (false)
        \li 1 (true)

        \param value Receives the property's value (0 or 1). 
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000020)
    ]
    HRESULT _stdcall IntegralHeight([out, retval] long * value );
    /**
        \brief Sets the property determining how the list box displays partial items.

        This method sets the property determining how the list box displays 
        partial items.  This method has dependancies on the Style property.

        If IntegralHeight is true (1) and Style is set to owner draw fixed (1),
        the list box height will always be a multiple of ItemHeight and partial 
        items will NOT be displayed.

        If IntegralHeight is false (0), the list box height can be any value, but 
        the last item may not be fully displayed.

        If Style is set to owner draw variable (2), then setting IntegralHeight 
        has no effect.

        value can be:
        \li 0 (false)
        \li 1 (true)

        \param value Receives the property's value (0 or 1). 
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000020)
    ]
    HRESULT _stdcall IntegralHeight([in] long value );
    /**
        \brief Gets the height of list box items in pixels.

        This method gets the height of the list box items in the drop down list 
        in pixels.

        \b Note: The value of this property is only used if, and only if, the 
        Style property is set to 3 or 4 (the owner-draw settings).

        \param value The height in pixels of the list box items.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000021)
    ]
    HRESULT _stdcall ItemHeight([out, retval] long * value );
    /**
        \brief Sets the height of list box list items in pixels.

        This method sets the height of the list box list items in pixels.
        
       \b Note: The value of this property is only used if, and only if, the 
        Style property is set to 3 or 4 (the owner-draw settings).

        \param value The height in pixels of the list box items.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000021)
    ]
    HRESULT _stdcall ItemHeight([in] long value );
    /**
        \brief Finds a list box item by index.

        This method finds a list box item by index.  Index checking is done and 
        if it is out of bounds, blank is returned.

        \param index The index of the desired item.
        \param value Receives the list box item (a string) at index or blank.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x00000022)
    ]
    HRESULT _stdcall Items([in] long index, [out, retval] BSTR * value );
    /**
        \brief Sets the value of a list box item by index.

        This method sets the value of a list box item by index.  index checking
        is done and and if no action is taken if the index is out of bounds 
        (i.e. new items cannot be added to the list box with this method).

        \param index The index of the item to add.
        \param value The new value of the list box item.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x00000022)
    ]
    HRESULT _stdcall Items([in] long index, [in] BSTR value );
    /**
        \brief Gets the property indicating a user can select multiple list items at once.

        This method gets the property indicating a user can select multiple list
        items at once.

        value can be:
        \li 0 - (false) no multi-select allowed
        \li 1 - (true) multi-select allowed

        \param Receives the value of the property (0 or 1).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x00000023)
    ]
    HRESULT _stdcall MultiSelect([out, retval] long * value );
    /**
        \brief Gets the property indicating a user can select multiple list items at once.

        This method gets the property indicating a user can select multiple list 
        items at once.

        value can be:
        \li 0 - (false) no multi-select allowed
        \li 1 - (true) multi-select allowed

        \param Receives the value of the property (0 or 1).
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propput, 
    id(0x00000023)
    ]
    HRESULT _stdcall MultiSelect([in] long value );
    [
    propget, 
    id(0x00000024)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its 
        parent's bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput,
    id(0x00000024)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000025)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000025)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000026)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput,
    id(0x00000026)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget, 
    id(0x00000027)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000027)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000028)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000028)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000029)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the width in pixels determing when horizontal scroll bars appear.

        This method gets the width in pixels determing when horizontal scroll 
        bars appear. When ScrollWidth is greater than the size of the list box's
        client area (see ClientWidth) a horizontal scroll bar is displayed.

        \param value Receives the size in pixels of this property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002A)
    ]
    HRESULT _stdcall ScrollWidth([out, retval] long * value );
    /**
        \brief Sets the width in pixels determing when horizontal scroll bars appear.

        This method sets the width in pixels determing when horizontal scroll 
        bars appear. When ScrollWidth is greater than the size of the list box's
        client area (see ClientWidth) a horizontal scroll bar is displayed.

        \param value Sets the size in pixels of this property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002A)
    ]
    HRESULT _stdcall ScrollWidth([in] long value );
    [
    propget, 
    id(0x0000002B)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000002B)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    /**
        \brief Gets the property indicating if list box items are sorted.

        This method retrieves the property indicating if the list box items 
        are sorted alphabetically.
        value can be:
        \li 0 - unsorted
        \li 1 - sorted

        \param value Receives the sorted property (0 unsorted, 1 sorted).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002C)
    ]
    HRESULT _stdcall Sorted([out, retval] long * value );
    /**
        \brief Sets property indicating if list box items are sorted.

        This method sets the property indicating if the list box items are 
        sorted alphabetically.
        value can be:
        \li 0 - unsorted
        \li 1 - sorted

        Note: Once items are sorted the original order is lost.

        \param value The new value for the sorted property (0 unsorted, 1 sorted).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002C)
    ]
    HRESULT _stdcall Sorted([in] long value );
    [
    propget, 
    id(0x0000002D)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000002D)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    /**
        \brief Get the displayed dialog base units for each tab character in item strings.

        Gets the displayed dialog base units for each tab character in item strings.
        
		\p value is in dialog base units and the default setting is 0. 0 
        indicates the width will be specificed by Windows (i.e. 2 dialog base
        units).
				
        \param value Receives the current TabWidth in dialog base units.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002E)
    ]
    HRESULT _stdcall TabWidth([out, retval] long * value );
    /**
        \brief Set the displayed dialog base units for each tab character in item strings.

        Sets the displayed dialog base units for each tab character in item strings.
        
		\p value is in dialog base units and the default setting is 0. 0 
        indicates the width will be specificed by Windows (i.e. 2 dialog base
        units).
				
        \param value The new value of TabWidth in dialog base units.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002E)
    ]
    HRESULT _stdcall TabWidth([in] long value );
    [
    propget, 
    id(0x0000002F)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000002F)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
  };

  [
    uuid(BA37BB99-F039-49B7-AB56-819E87B0472F), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlPanel
    \brief <b>UNSUPPORTED</b> - A generic panel control.

     <b>UNSUPPORTED</b>

     Panels have properties for providing a beveled border around the control,
     as well as methods to help manage the placement of child controls embedded
     in the panel.

     You can also use panels to group controls together, similar to the way you
     can use a group box, but with a beveled border (or no border) rather than
     the group box outline. Panels are typically used for groups of controls
     within a single form.

     Panels with no borders are useful as docking sites when writing applications
     that use drag-and-dock.

    \since 0.9.1.1
  **/

   interface IExodusControlPanel: IExodusControl
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get how the control aligns within its container (parent control).

        Retreive how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Receives control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Align([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set how the control aligns within its container (parent control).

        Specify how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Sets control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Align([in] long value );
    [
    propget, 
    id(0x0000000D)
    ]
    /**
        \brief Gets the property controlling how the text is aligned within the control.

        Gets the property controlling how the text is aligned within the control.

        Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Receives control's alignment (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([out, retval] long * value );
    [
    propput, 
    id(0x0000000D)
    ]
    /**
        \brief Set the property controlling how the text is aligned within the control.

        Specify how the text is aligned within the control.

		Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Sets control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([in] long value );
    [
    propget, 
    id(0x0000000E)
    ]
    /**
        \brief Get whether the control sizes itself automatically to accommodate its contents.

        Retreive whether the control sizes itself automatically to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoSize([out, retval] long * value );
    [
    propput, 
    id(0x0000000E)
    ]
    /**
        \brief Set whether the control sizes itself automatically to accommodate its contents.

        Specifies whether the control sizes itself automatically to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Gets control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoSize([in] long value );
    [
    propget, 
    id(0x0000000F)
    ]
    /**
        \brief Gets the cut of the inner bevel.

        Retreives the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelInner([out, retval] long * value );
    [
    propput, 
    id(0x0000000F)
    ]
    /**
        \brief Sets the cut of the inner bevel.

        Specifies the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
		\p value should be one of 0, 1, 2, 3.
 
        \param value Sets control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelInner([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
    /**
        \brief Get the cut of the outer bevel.

        Retrieve the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
		The outer bevel appears immediately inside the border and outside the inner bevel. 
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelOuter([out, retval] long * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set the cut of the outer bevel.

        Specifies the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
	    The outer bevel appears immediately inside the border and outside the inner bevel. 
        
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Sets control's bevel (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelOuter([in] long value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get the width of the inner and outer bevels.

        Retreive the width of the inner and outer bevels.

				Use BevelWidth to specify the width, in pixels, of the inner and outer bevels.
				 
				\p value should be in pixels.
 
        \param value Receives control's bevel width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelWidth([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Set the width of the inner and outer bevels.

        Specify the width of the inner and outer bevels.

				Use BevelWidth to specify the width, in pixels, of the inner and outer bevels.
				 
				\p value should be in pixels.
 
        \param value Sets control's bevel width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelWidth([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get the width of the control's border

        Retreive the width of the control's border

        Use BorderWidth to get or set the width of the control's border. Graphics or text drawn by the control is clipped to the area within the border.
				 
        \p value should be in pixels.
 
        \param value Receives control's border width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderWidth([out, retval] long * value );
    [
    propput, 
    id(0x00000013)
    ]
    /**
        \brief Set the width of the control's border

        Specify the width of the control's border

        Use BorderWidth to get or set the width of the control's border. Graphics or text drawn by the control is clipped to the area within the border.
				 
        \p value should be in pixels.
 
        \param value Sets control's border width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderWidth([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get whether the edit control has a single line border around the client area.

        Retreive whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Receives control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderStyle([out, retval] long * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set whether the edit control has a single line border around the client area.

        Specify whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Sets control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderStyle([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x00000017)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get whether the docking manager is used in drag-and-dock operations.

        Retreive whether the docking manager is used in drag-and-dock operations.

        Use UseDockManager to get or set whether a docking manager is used in drag-and-dock operations. 

        The docking manager handles the positioning of docked controls and any painting of docking zones around those controls. If you do not use a docking manager, docked controls are aligned to the windowed control based on the closest edge to where they are released. When you do not use a docking manager, there is no region for a user to grab to automatically undock a docked control.
		
        \p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's UseDockManager setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall UseDockManager([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set whether the docking manager is used in drag-and-dock operations.

        Specifies whether the docking manager is used in drag-and-dock operations.

		Use UseDockManager to get or set whether a docking manager is used in drag-and-dock operations. 
			
		The docking manager handles the positioning of docked controls and any painting of docking zones around those controls. If you do not use a docking manager, docked controls are aligned to the windowed control based on the closest edge to where they are released. When you do not use a docking manager, there is no region for a user to grab to automatically undock a docked control.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's UseDockManager setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall UseDockManager([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get whether the control can be the target of drag-and-dock operations.

        Retreive whether the control can be the target of drag-and-dock operations.

		Set DockSite to true to allow other controls to be docked to this windowed control.
				
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's DockSite setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DockSite([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set whether the control can be the target of drag-and-dock operations.

        Specifies whether the control can be the target of drag-and-dock operations.

		Set DockSite to true to allow other controls to be docked to this windowed control.
				
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's DockSite setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DockSite([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x0000001C)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x0000001C)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x0000001E)
    ]
    /**
        \brief Get how the panel repaints itself when it is resized.

        Retreive how the panel repaints itself when it is resized.

				FullRepaint controls how the panel responds when it is resized. When FullRepaint is true, the entire panel, including the beveled border repaints when the size changes. When FullRepaint is false, only the area inside the beveled border repaints.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's FullRepaint setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall FullRepaint([out, retval] long * value );
    [
    propput, 
    id(0x0000001E)
    ]
    /**
        \brief Set how the panel repaints itself when it is resized.

        Specifies how the panel repaints itself when it is resized.

				FullRepaint controls how the panel responds when it is resized. When FullRepaint is true, the entire panel, including the beveled border repaints when the size changes. When FullRepaint is false, only the area inside the beveled border repaints.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's FullRepaint setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall FullRepaint([in] long value );
    [
    propget, 
    id(0x0000001F)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000020)
    ]
    /**
        \brief Get whether a panel that is used as a toolbar is replaced by a toolbar supplied by an OLE server.

        Retreive whether a panel that is used as a toolbar is replaced by a toolbar supplied by an OLE server.

				Set the Locked property to false when the panel is used as a toolbar by a TOleContainer component. Any panels aligned to a TOleContainer are assumed to be replaceable toolbars unless the Locked property is true. Prevent the panel from being replaced by an OLE server's toolbar by setting the panel's Locked property to true.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's Locked setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Locked([out, retval] long * value );
    [
    propput, 
    id(0x00000020)
    ]
    /**
        \brief Set whether a panel that is used as a toolbar is replaced by a toolbar supplied by an OLE server.

        Specifies whether a panel that is used as a toolbar is replaced by a toolbar supplied by an OLE server.

				Set the Locked property to false when the panel is used as a toolbar by a TOleContainer component. Any panels aligned to a TOleContainer are assumed to be replaceable toolbars unless the Locked property is true. Prevent the panel from being replaced by an OLE server's toolbar by setting the panel's Locked property to true.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's Locked setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Locked([in] long value );
    [
    propget, 
    id(0x00000021)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000021)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    /**
        \brief Gets the property determining if control use's parents theme background.

        This method gets the property determining if control use's parents theme
        background. This method has no effect if XP Themes are disabled.

        value can be 0 (False) or 1 (True).

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000022)
    ]
    HRESULT _stdcall ParentBackground([out, retval] long * value );
    /**
        \brief Sets the property determining if control use's parents theme background.

        This method sets the property determining if control use's parents theme
        background. This method has no effect if XP Themes are disabled.

        value can be 0 (False) or 1 (True).

        \param value The new value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000022)
    ]
    HRESULT _stdcall ParentBackground([in] long value );
    [
    propget, 
    id(0x00000023)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000023)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000024)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput,
    id(0x00000024)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget, 
    id(0x00000025)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000025)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000026)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000026)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000027)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000028)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000028)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x00000029)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x00000029)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x0000002A)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000002A)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    [
    propget, 
    id(0x0000002B)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000002B)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
  };

  [
    uuid(F80CD345-A91C-40C8-89CD-AD5BE532B9C2), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlPopupMenu
    \brief <b>UNSUPPORTED</b> - Encapsulates the methods of a pop-up menu.

    <b>UNSUPPORTED</b>

    This is a placeholder for a discussion of popup menus         
    \since 0.9.1.1
  **/

   interface IExodusControlPopupMenu: IExodusControl
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the number of items in the popup menu.

        Retrieve the number of items in the popup menu.

        \param value The number of items in the popup menu.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ItemsCount([out, retval] long * value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get a specific menu item.

        Retrieve a specific IExodusControlMenuItem by index.
        
        \param index Zero based index of IExodusControlMenuItem to retrieve.
        \param value IExodusControlMenuItem retrieved.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Items([in] long index, [out, retval] IExodusControlMenuItem ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get where the pop-up menu appears when the user clicks the right mouse button.

        Retrieve where the pop-up menu appears when the user clicks the right mouse button.
        
        \p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
        
        \param value Receives the alignment of the popup menu.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief Set where the pop-up menu appears when the user clicks the right mouse button.

        Specifies where the pop-up menu appears when the user clicks the right mouse button.
        
        \p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
        
        \param value Sets the alignment of the popup menu.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get whether the accelerator keys for menu items can be reset automatically.

        Retrieve whether the accelerator keys for menu items can be reset automatically.

				Use AutoHotkeys to indicate whether the menu should automatically ensure that accelerator keys for its items are reset if necessary so that every menu item has an accelerator key and no two items have the same accelerator key. This ensures that menu items added dynamically at runtime can have accelerator keys that do not introduce conflicts. TMenu tries to preserve all existing accelerator key mappings when adjusting the accelerator keys, but may be forced to change some in order to ensure that every item has a unique accelerator.
				 
				Before the menu is displayed, the menu checks and adjusts the accelerator key mappings if AutoHotkeys is maAutomatic. When AutoHotkeys is maManual, the menu does not alter the top-level menu items, but may adjust the accelerator keys for submenus, depending on the AutoHotkeys property of the menu item that is the parent of the submenu.
				 
        \p value can be:
        \li 0 - Automatic
        \li 1 - Manual
        
        \param value Receives the value of AutoHotKeys.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoHotkeys([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief Set whether the accelerator keys for menu items can be reset automatically.

        Specifies whether the accelerator keys for menu items can be reset automatically.

				Use AutoHotkeys to indicate whether the menu should automatically ensure that accelerator keys for its items are reset if necessary so that every menu item has an accelerator key and no two items have the same accelerator key. This ensures that menu items added dynamically at runtime can have accelerator keys that do not introduce conflicts. TMenu tries to preserve all existing accelerator key mappings when adjusting the accelerator keys, but may be forced to change some in order to ensure that every item has a unique accelerator.
				 
				Before the menu is displayed, the menu checks and adjusts the accelerator key mappings if AutoHotkeys is maAutomatic. When AutoHotkeys is maManual, the menu does not alter the top-level menu items, but may adjust the accelerator keys for submenus, depending on the AutoHotkeys property of the menu item that is the parent of the submenu.
				 
        \p value can be:
        \li 0 - Automatic
        \li 1 - Manual
        
        \param value Sets the value of AutoHotKeys.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoHotkeys([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get whether redundant separator bars are automatically removed from the menu.

        Retrieve whether redundant separator bars are automatically removed from the menu.

		Use AutoLineReduction to indicate whether the menu should automatically ensure that the menu does not start or end with a separator bar and that two separator bars do not appear next to each other. This ensures that menus built dynamically at runtime do not have separator bars in undesired positions.
 
		Before the menu is displayed, it removes superfluous separator bars when AutoLineReduction is maAutomatic. When AutoLineReduction is maManual, the menu does not remove separators from the top-level menu items, but may remove superfluous separators from submenus, depending on the AutoLineReduction property of the menu item that is the parent of the submenu.				
        
        \p value can be:
        \li 0 - automatic
        \li 1 - manual
        
        \param value Receives the value of AutoLineReduction.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoLineReduction([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set whether redundant separator bars are automatically removed from the menu.

        Specifies whether redundant separator bars are automatically removed from the menu.

		Use AutoLineReduction to indicate whether the menu should automatically ensure that the menu does not start or end with a separator bar and that two separator bars do not appear next to each other. This ensures that menus built dynamically at runtime do not have separator bars in undesired positions.
 
		Before the menu is displayed, it removes superfluous separator bars when AutoLineReduction is maAutomatic. When AutoLineReduction is maManual, the menu does not remove separators from the top-level menu items, but may remove superfluous separators from submenus, depending on the AutoLineReduction property of the menu item that is the parent of the submenu.				
        
        \p value can be:
        \li 0 - automatic
        \li 1 - manual
        
        \param value Sets the value of AutoLineReduction.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoLineReduction([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get whether the pop-up menu appears automatically when the user clicks the right mouse button.

        Retrieve whether the pop-up menu appears automatically when the user clicks the right mouse button.

				Set AutoPopup to true to cause the pop-up menu to appear automatically when the user clicks the right mouse button on a control that has the pop-up menu specified as its PopupMenu property. The application key present on keyboards specifically designed for 32-bit Windows will also show this menu when pressed.
 
				Set AutoPopup to false if the appearance of the pop-up menu should be controlled programmatically. To display a pop-up menu when AutoPopup is false, use the Popup method.
        
        \p value is 1 (TRUE) or 0 (FALSE)
        
        \param value Receives the value of AutoPopup.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoPopup([out, retval] long * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set whether the pop-up menu appears automatically when the user clicks the right mouse button.

        Specifies whether the pop-up menu appears automatically when the user clicks the right mouse button.

				Set AutoPopup to true to cause the pop-up menu to appear automatically when the user clicks the right mouse button on a control that has the pop-up menu specified as its PopupMenu property. The application key present on keyboards specifically designed for 32-bit Windows will also show this menu when pressed.
 
				Set AutoPopup to false if the appearance of the pop-up menu should be controlled programmatically. To display a pop-up menu when AutoPopup is false, use the Popup method.
        
        \p value is 1 (TRUE) or 0 (FALSE)
        
        \param value Sets the value of AutoPopup.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall AutoPopup([in] long value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic.

        Retrieve numeric ID for control's context-sensitive help topic.

				If value is 0 (default), no help topic is provided for this control.        
				
        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords. If \p value is an empty string (zero length), no help topic is provided for this control.        
				
        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get whether the menu items are drawn by the application.

        Retrieve whether the menu items are drawn by the application.

		Set OwnerDraw to true to indicate that the application will draw menu items in an OnDrawItem event handler.
				
		\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives the state of ownerdraw.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall OwnerDraw([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set whether the menu items are drawn by the application.

        Specifies whether the menu items are drawn by the application.

		Set OwnerDraw to true to indicate that the application will draw menu items in an OnDrawItem event handler.
				
		\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets the state of ownerdraw.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall OwnerDraw([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x0000000D)
    ]
    /**
        \brief Get which mouse button activates the popup menu when it is associated with a toolbar button

        Retrieve which mouse button activates the popup menu when it is associated with a toolbar button

				Use TrackButton to specify which mouse button activates the popup menu when it is associated with a toolbar button.
				
				\p value can be:
                \li 0 - Right button
                \li 1 - Left button
				
        \param value Receives which mouse button activates the popup menu (0-right or 1-left).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TrackButton([out, retval] long * value );
    [
    propput, 
    id(0x0000000D)
    ]
    /**
        \brief Set which mouse button activates the popup menu when it is associated with a toolbar button

        Specifies which mouse button activates the popup menu when it is associated with a toolbar button

		Use TrackButton to specify which mouse button activates the popup menu when it is associated with a toolbar button.
				
		\p value can be:
        \li 0 - Right button
        \li 1 - Left button
				
        \param value Sets which mouse button activates the popup menu (0-right, 1-left).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TrackButton([in] long value );
  };

  [
    uuid(87FAD954-03E1-4657-B58D-9947087EAAEC), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlRadioButton
    \brief <b>UNSUPPORTED</b> - A wrapper for a system radio button. 

    <b>UNSUPPORTED</b>

    Radio buttons present a set of mutually exclusive options to the
    user- that is, only one radio button in a set can be selected at a time.
    When the user selects a radio button, the previously selected radio button
    becomes unselected.

    By default, all radio buttons that are directly contained in the same
    windowed control container, such as an IExodusControlPanel, are grouped.
    For example, two radio buttons on a IExodusControlForm can be checked at
    the same time only if they are contained in separate containers, such as
    two different panels.

    \since 0.9.1.1
  **/

   interface IExodusControlRadioButton: IExodusControl
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get the position of the caption relative to the radio button.

        Retrieve the position of the caption relative to the radio button.

		Use Alignment to determine whether the caption of the radio button should appear to the left of the radio button or to the right of the radio button.
				
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center

        \param value Receives the Alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set the position of the caption relative to the radio button.

        Specifies the position of the caption relative to the radio button.

		Use Alignment to determine whether the caption of the radio button should appear to the left of the radio button or to the right of the radio button.
				
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center

        \param value Sets the Alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([in] long value );
    [
    propget, 
    id(0x0000000D)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000000D)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    [
    propget, 
    id(0x0000000E)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000E)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
    [
    propget, 
    id(0x0000000F)
    ]
    /**
        \brief Get whether the option represented by the radio button is selected.

        Retrieve whether the option represented by the radio button is selected.

		Read Checked to determine whether the radio button is selected. Set Checked to true to select the radio button and deselect all other radio buttons in the same container. Set Checked to false to deselect the radio button, leaving no radio button in the group selected.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Receives the value of Checked.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Checked([out, retval] long * value );
    [
    propput, 
    id(0x0000000F)
    ]
    /**
        \brief Set whether the option represented by the radio button is selected.

        Specifies whether the option represented by the radio button is selected.

		Read Checked to determine whether the radio button is selected. Set Checked to true to select the radio button and deselect all other radio buttons in the same container. Set Checked to false to deselect the radio button, leaving no radio button in the group selected.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Sets the value of Checked.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Checked([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x00000011)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000013)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput,
    id(0x00000019)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x0000001C)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000001E)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000001E)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x0000001F)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000001F)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    [
    propget, 
    id(0x00000020)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x00000020)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    [
    propget, 
    id(0x00000021)
    ]
    /**
        \brief Get whether the button text wraps to fit the width of the control.

        Retrieve whether the button text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
	    \p value is 1 (TRUE) or 0 (FALSE)

        \param value Receives WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WordWrap([out, retval] long * value );
    [
    propput, 
    id(0x00000021)
    ]
    /**
        \brief Set whether the button text wraps to fit the width of the control.

        Specifies whether the button text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Sets WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WordWrap([in] long value );
  };

  [
    uuid(3997314D-4068-43E7-ACEB-150FF196069C), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlRichEdit
    \brief <b>UNSUPPORTED</b> - a wrapper for a Windows rich text edit control.

    <b>UNSUPPORTED</b>

    Rich text edit controls let the user enter text that includes variation in
    font attributes and paragraph formatting information.

    \since 0.9.1.1 
  **/

   interface IExodusControlRichEdit: IExodusControl
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    [
    propget, 
    id(0x0000000D)
    ]
    /**
        \brief Get how the control aligns within its container (parent control).

        Retreive how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Receives control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Align([out, retval] long * value );
    [
    propput, 
    id(0x0000000D)
    ]
    /**
        \brief Set how the control aligns within its container (parent control).

        Specify how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Sets control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Align([in] long value );
    [
    propget, 
    id(0x0000000E)
    ]
    /**
        \brief Gets the property controlling how the text is aligned within the control.

        Gets the property controlling how the text is aligned within the control.

        Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Receives control's alignment (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([out, retval] long * value );
    [
    propput, 
    id(0x0000000E)
    ]
    /**
        \brief Set the property controlling how the text is aligned within the control.

        Specify how the text is aligned within the control.

		Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Sets control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Alignment([in] long value );
    [
    propget, 
    id(0x0000000F)
    ]
    /**
        \brief Get the cut of the inner bevel.

        Retreive the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border.
				 
	    \c value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelInner([out, retval] long * value );
    [
    propput, 
    id(0x0000000F)
    ]
    /**
        \brief Set the cut of the inner bevel.

        Specifies the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border.
				 
        \c value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.

        \param value Sets control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelInner([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
   /**
        \brief Get the cut of the outer bevel.

        Retrieve the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
		The outer bevel appears immediately inside the border and outside the inner bevel. 
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelOuter([out, retval] long * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set the cut of the outer bevel.

        Specifies the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
	    The outer bevel appears immediately inside the border and outside the inner bevel. 
        
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Sets control's bevel (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelOuter([in] long value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get the control's bevel style.

        Retreive the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Receives control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelKind([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Sets the control's bevel style.

        Specify the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Sets control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelKind([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get the width of the inner and outer bevels.

        Retreive the width of the inner and outer bevels.

				Use BevelWidth to specify the width, in pixels, of the inner and outer bevels.
				 
				\p value should be in pixels.
 
        \param value Receives control's bevel width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelWidth([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set the width of the inner and outer bevels.

        Specify the width of the inner and outer bevels.

				Use BevelWidth to specify the width, in pixels, of the inner and outer bevels.
				 
				\p value should be in pixels.
 
        \param value Sets control's bevel width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BevelWidth([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000013)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get whether the edit control has a single line border around the client area.

        Retreive whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Receives control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderStyle([out, retval] long * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set whether the edit control has a single line border around the client area.

        Specify whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Sets control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderStyle([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the width of the control's border

        Retreive the width of the control's border

				Use BorderWidth to get or set the width of the control's border. Graphics or text drawn by the control is clipped to the area within the border.
				 
				\p value should be in pixels.
 
        \param value Receives control's border width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderWidth([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the width of the control's border

        Specify the width of the control's border

				Use BorderWidth to get or set the width of the control's border. Graphics or text drawn by the control is clipped to the area within the border.
				 
				\p value should be in pixels.
 
        \param value Sets control's border width.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BorderWidth([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x00000017)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x0000001C)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get whether the visual indication of the selected text remains when focus shifts to another control.

        Retreive whether the visual indication of the selected text remains when focus shifts to another control.

				Set HideSelection to false when it is important to provide visual feedback of the selected portion of the text even when the edit control does not have focus. Set HideSelection to true to make the visual indication of selection appear only when the edit control has focus. HideSelection does not affect the actual value of the selection, only the visual indication. Always setting HideSelection to false can make forms containing many edit controls look busy.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's HideSelection value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HideSelection([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set whether the visual indication of the selected text remains when focus shifts to another control.

        Specify whether the visual indication of the selected text remains when focus shifts to another control.

				Set HideSelection to false when it is important to provide visual feedback of the selected portion of the text even when the edit control does not have focus. Set HideSelection to true to make the visual indication of selection appear only when the edit control has focus. HideSelection does not affect the actual value of the selection, only the visual indication. Always setting HideSelection to false can make forms containing many edit controls look busy.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's HideSelection value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HideSelection([in] long value );
    [
    propget, 
    id(0x0000001E)
    ]
    /**
        \brief Get whether the scroll bars disappear when not needed.

        Retreive whether the scroll bars disappear when not needed

				Use HideScrollBars to determine whether the scroll bars disappear from the edit control when the text appears completely within the edit window. Set HideScrollBars to false to prevent the scroll bars from flashing on and off when the contents of the rich edit control change.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's HideScrollBars value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HideScrollBars([out, retval] long * value );
    [
    propput, 
    id(0x0000001E)
    ]
    /**
        \brief Set whether the scroll bars disappear when not needed.

        Specify whether the scroll bars disappear when not needed

				Use HideScrollBars to determine whether the scroll bars disappear from the edit control when the text appears completely within the edit window. Set HideScrollBars to false to prevent the scroll bars from flashing on and off when the contents of the rich edit control change.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's HideScrollBars value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HideScrollBars([in] long value );
    [
    propget, 
    id(0x0000001F)
    ]
    /**
        \brief Gets the current value of the IME mode for this control.

        This method gets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value Receives the current value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ImeMode([out, retval] long * value );
    [
    propput, 
    id(0x0000001F)
    ]
    /**
        \brief Sets the current value of the IME mode for this control.

        This method sets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value The new value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ImeMode([in] long value );
    [
    propget, 
    id(0x00000020)
    ]
    /**
        \brief Gets the ImeName property of the control.

        Gets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determines which IME tool will be used to convert user 
        input.

        \param value Receives the name of the current IME instance being used.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ImeName([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000020)
    ]
    /**
        \brief Sets the ImeName property of the control.

        Sets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determine which IME tool will be used to convert user 
        input. If the supplied name isn't recognized the currently selected IME
        instance remains active and no error is thrown.

        \param value The new name of the IME instance to be used.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ImeName([in] BSTR value );
    [
    propget, 
    id(0x00000021)
    ]
    /**
        \brief Get how many lines of text there are in the control.

        Retreive how many lines of text there are in the control.

        \param value Receives the number of lines of text.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall LinesCount([out, retval] long * value );
    [
    propget, 
    id(0x00000022)
    ]
    /**
        \brief Get the individual lines of text in the rich text edit control.

        Retreive the individual lines of text in the rich text edit control.

				\param index Zero based index of which line to retreive.
				\param value Returned String.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Lines([in] long index, [out, retval] BSTR * value );
    [
    propput, 
    id(0x00000022)
    ]
    /**
        \brief Set the individual lines of text in the rich text edit control.

        Specify the individual lines of text in the rich text edit control.

				\param index Zero based index of which line to set.
				\param value String to set.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Lines([in] long index, [in] BSTR value );
    [
    propget, 
    id(0x00000023)
    ]
    /**
        \brief Get the maximum number of characters the user can enter into the edit control.

        Retreive the maximum number of characters the user can enter into the edit control.

		Use MaxLength to limit the number of characters that can be entered into the edit control. A value of 0 indicates that there is no application-defined limit on the length.
				
		\param value Receives the Maximum Length.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall MaxLength([out, retval] long * value );
    [
    propput, 
    id(0x00000023)
    ]
    /**
        \brief Set the maximum number of characters the user can enter into the edit control.

        Specifies the maximum number of characters the user can enter into the edit control.

		Use MaxLength to limit the number of characters that can be entered into the edit control. A value of 0 indicates that there is no application-defined limit on the length.
				
		\param value Set the MaximumLength.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall MaxLength([in] long value );
    [
    propget, 
    id(0x00000024)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000024)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000025)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000025)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000026)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput,
    id(0x00000026)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget, 
    id(0x00000027)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000027)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000028)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000028)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000029)
    ]
    /**
        \brief Get whether the rich edit control treats the text as plain text or rich text when streaming to or from a file.

        Retreive whether the rich edit control treats the text as plain text or rich text when streaming to or from a file.

				To write the rich text in the control to a plain text file, set PlainText to true before streaming the text to a file. To ignore the rich text information encoded in a file, set PlainText to true before streaming the text to the control. To stream in the rich text attributes encoded in a file, or save the encoding of the rich text attributes to a file, set PlainText to false.
				
				\p value is 1 (TRUE) or 0 (FALSE)
				
				\param value Receives Plain Text value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PlainText([out, retval] long * value );
    [
    propput, 
    id(0x00000029)
    ]
    /**
        \brief Set whether the rich edit control treats the text as plain text or rich text when streaming to or from a file.

        Specifies whether the rich edit control treats the text as plain text or rich text when streaming to or from a file.

				To write the rich text in the control to a plain text file, set PlainText to true before streaming the text to a file. To ignore the rich text information encoded in a file, set PlainText to true before streaming the text to the control. To stream in the rich text attributes encoded in a file, or save the encoding of the rich text attributes to a file, set PlainText to false.
				
				\p value is 1 (TRUE) or 0 (FALSE)
				
				\param value Sets Plain Text value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PlainText([in] long value );
    [
    propget, 
    id(0x0000002A)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000002B)
    ]
    /**
        \brief Get whether the user can change the text of the edit control.

        Retreive whether the user can change the text of the edit control.

		To restrict the edit control to display only, set the ReadOnly property to true. Set ReadOnly to false to allow the contents of the edit control to be edited.
 
		Setting ReadOnly to true ensures that the text is not altered, while still allowing the user to select text. The selected text can then be manipulated by the application, or copied to the Clipboard.
		
		\p value is 1 (TRUE) or 0 (FALSE)
		
		\param value Receives Read Only value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ReadOnly([out, retval] long * value );
    [
    propput, 
    id(0x0000002B)
    ]
    /**
        \brief Set whether the user can change the text of the edit control.

        Specifies whether the user can change the text of the edit control.

		To restrict the edit control to display only, set the ReadOnly property to true. Set ReadOnly to false to allow the contents of the edit control to be edited.
 
		Setting ReadOnly to true ensures that the text is not altered, while still allowing the user to select text. The selected text can then be manipulated by the application, or copied to the Clipboard.

		\p value is 1 (TRUE) or 0 (FALSE)
				
		\param value Sets Read Only value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ReadOnly([in] long value );
    [
    propget, 
    id(0x0000002C)
    ]
    /**
        \brief Get whether the control has scroll bars.

        Gets property controlling whether the control has scroll bars.

		Use ScrollBars to give a multiline edit control horizontal or vertical scroll bars.
				
		\p value can be:
        \li 0 - none
        \li 1 - horizonal scroll bars
        \li 2 - vertical scroll bars
        \li 3 - both scroll bars
				
		\param value Receives Scroll Bars value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ScrollBars([out, retval] long * value );
    [
    propput, 
    id(0x0000002C)
    ]
    /**
        \brief Set whether the control has scroll bars.

        Sets property controlling whether the control has scroll bars.

        Use ScrollBars to give a multiline edit control horizontal or vertical scroll bars.
				
        \p value can be:
        \li 0 - none
        \li 1 - horizonal scroll bars
        \li 2 - vertical scroll bars
        \li 3 - both scroll bars
				
        \param value Receives Scroll Bars value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ScrollBars([in] long value );
    [
    propget, 
    id(0x0000002D)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000002D)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000002E)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000002E)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x0000002F)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000002F)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    [
    propget, 
    id(0x00000030)
    ]
    /**
        \brief Get whether the user can insert tab characters into the text.

        Retreives property controlling whether the user can insert tab characters into the text.

        Set WantTabs to true to allow users to enter tab characters into the text. Set WantTabs to false if you want the tab character to select the next control on the form instead.
				
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives WantTabs value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WantTabs([out, retval] long * value );
    [
    propput, 
    id(0x00000030)
    ]
    /**
        \brief Set whether the user can insert tab characters into the text.

        Specifies whether the user can insert tab characters into the text.

        Set WantTabs to true to allow users to enter tab characters into the text. Set WantTabs to false if you want the tab character to select the next control on the form instead.
				
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets WantTabs value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WantTabs([in] long value );
    [
    propget, 
    id(0x00000031)
    ]
    /**
        \brief Get whether the user can insert return characters into the text.

        Retreive whether the user can insert return characters into the text.

        Set WantReturns to true to allow users to enter return characters into the text. Set WantReturns to false to allow the form to handle return characters instead. 
				
        For example, in a form with a default button (such as an OK button) and a memo control, if WantReturns is false, pressing Enter chooses the default button. If WantReturns is true, pressing Enter inserts a return character in the text.
 		Note: If WantReturns is false, users can still enter return characters into the text by pressing Ctrl+Enter.
 
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives WantReturns value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WantReturns([out, retval] long * value );
    [
    propput, 
    id(0x00000031)
    ]
    /**
        \brief Set whether the user can insert return characters into the text.

        Specifies whether the user can insert return characters into the text.

        Set WantReturns to true to allow users to enter return characters into the text. Set WantReturns to false to allow the form to handle return characters instead. 
				
        For example, in a form with a default button (such as an OK button) and a memo control, if WantReturns is false, pressing Enter chooses the default button. If WantReturns is true, pressing Enter inserts a return character in the text.
 				Note: If WantReturns is false, users can still enter return characters into the text by pressing Ctrl+Enter.
 
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets WantReturns value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WantReturns([in] long value );
    [
    propget, 
    id(0x00000032)
    ]
    /**
        \brief Get whether the edit control inserts soft carriage returns so text wraps at the right margin.

        Retreive whether the edit control inserts soft carriage returns so text wraps at the right margin.

		Set WordWrap to true to make the edit control wrap text at the right margin so it fits in the client area. The wrapping is cosmetic only. The text does not include any return characters that were not explicitly entered. Set WordWrap to false to have the edit control show a separate line only where return characters were explicitly entered into the text.
 
		\p value is 1 (TRUE) or 0 (FALSE)
				
		\param value Receives WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WordWrap([out, retval] long * value );
    [
    propput, 
    id(0x00000032)
    ]
    /**
        \brief Set whether the edit control inserts soft carriage returns so text wraps at the right margin.

        Specifies whether the edit control inserts soft carriage returns so text wraps at the right margin.

		Set WordWrap to true to make the edit control wrap text at the right margin so it fits in the client area. The wrapping is cosmetic only. The text does not include any return characters that were not explicitly entered. Set WordWrap to false to have the edit control show a separate line only where return characters were explicitly entered into the text.
 
		\p value is 1 (TRUE) or 0 (FALSE)
				
		\param value Sets WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall WordWrap([in] long value );
  };

  [
    uuid(0D41733E-3505-46FB-B199-C6046E1C84C7), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**
    \interface IExodusControlButton
    \brief <b>UNSUPPORTED</b> - A push button control.

    <b>UNSUPPORTED</b>

    A standard push button on a form. To use a button that displays a bitmap
    instead of a label, use IExodusControlBitBtn.

    \since 0.9.1.1   
  **/

   interface IExodusControlButton: IExodusControl
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );    
    /**
        \brief Gets the value of the button's cancel property.

        This method returns the value of the button's cancel property. When set
        to true, pressing the escape key will cause the onClick event handler to 
        execute.

        \param value The current value of the button's cancel property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Cancel([out, retval] long * value );
    /**
        \brief Sets the value of the button's cancel property.

        This method sets the value of the button's cancel property.  When set
        to true, pressing the escape key will cause the onClick event handler to 
        execute.

        \param value The new value of the button's cancel property.
        \return S_OK if successful, or an error value otherwise.

    **/
    [
    propput, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Cancel([in] long value );
    [
    propget, 
    id(0x0000000E)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000E)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
    /**
        \brief Gets the value of the button's default property.

        This method gets the value of the button's default property.  When set
        to true, pressing the enter key will cause the onClick event handler to 
        execute.

        \param value The current value of the button's default property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Default([out, retval] long * value );
    /**
        \brief Sets the value of the button's default property.

        This method sets the value of the button's default property.  When set
        to true, pressing the enter key will cause the onClick event handler to 
        execute.

        \param value The new value of the button's default property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Default([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000013)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the ModalResult property, affects how/if the parent (modal) form closes on button click.
        
        The ModalResult property will determines how the modal parent form will 
        react when the button is clicked. When the button is clicked, the 
        ModalResult property of its parent form is set with the value of the 
        button's property.

        Possible values of ModalResult include:
        \li mrNone      (0)         The default value before user exits.
        \li mrOk        (idOK)      The user exited using the OK button.
        \li mrCancel    (idCancel)  The user exited with the CANCEL button.
        \li mrAbort     (idAbort)   The user exited with the ABORT button.
        \li mrRetry     (idRetry)   The user exited with the RETRY button.
        \li mrIgnore    (idIgnore)  The user exited with the IGNORE button.
        \li mrYes       (idYes)     The user exited with the YES button.
        \li mrNo        (idNo)      The user exited with the NO button.
        \li mrAll       (mrNo+1)    The user exited with the ALL button.
        \li mrNoToAll   (mrAll+1)   The user exited with the NO TO ALL button.
        \li mrYesToAll  (mrNoToAll+1) The user exited with the YES TO ALL button.
       
        \param value The current value of ModalResult.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000015)
    ]
    HRESULT _stdcall ModalResult([out, retval] long * value );
    /**
        \brief Sets if a button closes it's (modal) parent form and how.

        Setting the ModalResult property will cause the modal parent form to close 
        when the button is clicked. When the button is clicked, the ModalResult 
        property of its parent form is set with the value of the button's property.
        Possible values of ModalResult include:
        \li mrNone      (0)         The default value before user exits.
        \li mrOk        (idOK)      The user exited using the OK button.
        \li mrCancel    (idCancel)  The user exited with the CANCEL button.
        \li mrAbort     (idAbort)   The user exited with the ABORT button.
        \li mrRetry     (idRetry)   The user exited with the RETRY button.
        \li mrIgnore    (idIgnore)  The user exited with the IGNORE button.
        \li mrYes       (idYes)     The user exited with the YES button.
        \li mrNo        (idNo)      The user exited with the NO button.
        \li mrAll       (mrNo+1)    The user exited with the ALL button.
        \li mrNoToAll   (mrAll+1)   The user exited with the NO TO ALL button.
        \li mrYesToAll  (mrNoToAll+1) The user exited with the YES TO ALL button.
       
        \param value The new value of ModalResult.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000015)
    ]
    HRESULT _stdcall ModalResult([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x0000001C)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000001C)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    /**
        \brief Get whether the button text wraps to fit the width of the control.

        Retrieve whether the button text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Receives WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001E)
    ]
    HRESULT _stdcall WordWrap([out, retval] long * value );
    /**
        \brief Set whether the button text wraps to fit the width of the control.

        Specifies whether the button text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Sets WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001E)
    ]
    HRESULT _stdcall WordWrap([in] long value );
  };

  [
    uuid(2954B16B-64BA-4441-A476-918CCCCA9B46), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusControlBitBtn
    \brief <b>UNSUPPORTED</b> - A button control allowing an image to placed on the button.
   
    <b>UNSUPPORTED</b>
    <p>A button control allowing an image to placed on the button.</p>
  **/

   interface IExodusControlBitBtn: IDispatch
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Gets the value of the button's cancel property.

        This method returns the value of the button's cancel property. When set
        to true, pressing the escape key will cause the onClick event handler to 
        execute.

        \param value The current value of the button's cancel property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Cancel([out, retval] long * value );
    /**
        \brief Sets the value of the button's cancel property.

        This method sets the value of the button's cancel property.  When set
        to true, pressing the escape key will cause the onClick event handler to 
        execute.

        \param value The new value of the button's cancel property.
        \return S_OK if successful, or an error value otherwise.

    **/
    [
    propput, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Cancel([in] long value );
    [
    propget, 
    id(0x0000000E)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000E)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
    /**
        \brief Gets the value of the button's default property.

        This method gets the value of the button's default property.  When set
        to true, pressing the enter key will cause the onClick event handler to 
        execute.

        \param value The current value of the button's default property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Default([out, retval] long * value );
    /**
        \brief Sets the value of the button's default property.

        This method sets the value of the button's default property.  When set
        to true, pressing the enter key will cause the onClick event handler to 
        execute.

        \param value The new value of the button's default property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Default([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000013)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the ModalResult property, affects how/if the parent (modal) form closes on button click.
        
        The ModalResult property will determines how the modal parent form will 
        react when the button is clicked. When the button is clicked, the 
        ModalResult property of its parent form is set with the value of the 
        button's property.

        Possible values of ModalResult include:
        \li mrNone      (0)         The default value before user exits.
        \li mrOk        (idOK)      The user exited using the OK button.
        \li mrCancel    (idCancel)  The user exited with the CANCEL button.
        \li mrAbort     (idAbort)   The user exited with the ABORT button.
        \li mrRetry     (idRetry)   The user exited with the RETRY button.
        \li mrIgnore    (idIgnore)  The user exited with the IGNORE button.
        \li mrYes       (idYes)     The user exited with the YES button.
        \li mrNo        (idNo)      The user exited with the NO button.
        \li mrAll       (mrNo+1)    The user exited with the ALL button.
        \li mrNoToAll   (mrAll+1)   The user exited with the NO TO ALL button.
        \li mrYesToAll  (mrNoToAll+1) The user exited with the YES TO ALL button.
       
        \param value The current value of ModalResult.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000015)
    ]
    HRESULT _stdcall ModalResult([out, retval] long * value );
    /**
        \brief Sets if a button closes it's (modal) parent form and how.

        Setting the ModalResult property will cause the modal parent form to close 
        when the button is clicked. When the button is clicked, the ModalResult 
        property of its parent form is set with the value of the button's property.
        Possible values of ModalResult include:
        \li mrNone      (0)         The default value before user exits.
        \li mrOk        (idOK)      The user exited using the OK button.
        \li mrCancel    (idCancel)  The user exited with the CANCEL button.
        \li mrAbort     (idAbort)   The user exited with the ABORT button.
        \li mrRetry     (idRetry)   The user exited with the RETRY button.
        \li mrIgnore    (idIgnore)  The user exited with the IGNORE button.
        \li mrYes       (idYes)     The user exited with the YES button.
        \li mrNo        (idNo)      The user exited with the NO button.
        \li mrAll       (mrNo+1)    The user exited with the ALL button.
        \li mrNoToAll   (mrAll+1)   The user exited with the NO TO ALL button.
        \li mrYesToAll  (mrNoToAll+1) The user exited with the YES TO ALL button.
       
        \param value The new value of ModalResult.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000015)
    ]
    HRESULT _stdcall ModalResult([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001A)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x0000001C)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000001C)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    /**
        \brief Get whether the button text wraps to fit the width of the control.

        Retrieve whether the button text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Receives WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001E)
    ]
    HRESULT _stdcall WordWrap([out, retval] long * value );
    /**
        \brief Set whether the button text wraps to fit the width of the control.

        Specifies whether the button text wraps to fit the width of the control.

		Set WordWrap to true to allow the label to display multiple line of text. When WordWrap is true, text that is too wide for the control wraps at the right margin.
				
		\p value is 1 (TRUE) or 0 (FALSE)

        \param value Sets WordWrap value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001E)
    ]
    HRESULT _stdcall WordWrap([in] long value );
    /**
        \brief Returns they kind of button this is.

        The button's kind property gives an indication as to the appearance
        of the bit button and it's response when the user clicks the button.
        Some of the possible values are: 
        \li bkCustom
        \li bkOK
        \li bkCancel
        \li bkHelp
        \li bkYes
        \li bkNo
        \li bkClose
        \li bkAbort
        \li bkRetry
        \li bkIgnore
        \li bkAll

        \param value The current value of the kind property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001F)
    ]
    HRESULT _stdcall Kind([out, retval] long * value );
    /**
        \brief Sets they kind of button this is.

        The button's kind property gives an indication as to the appearance
        of the bit button and it's response when the user clicks the button.
        Some of the possible values are: 
        \li bkCustom
        \li bkOK
        \li bkCancel
        \li bkHelp
        \li bkYes
        \li bkNo
        \li bkClose
        \li bkAbort
        \li bkRetry
        \li bkIgnore
        \li bkAll

        \param value The new value of the kind property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001F)
    ]
    HRESULT _stdcall Kind([in] long value );
    /**
        \brief Gets the layout used by the button image.

        This method gets the layout used by the button image. 
        The layout value are:
        \li 0 - left justified
        \li 1 - right justified
        \li 2 - top of bottom
        \li 3 - bottom of button

        \param value An ordinal indicating the justification of the image.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000020)
    ]
    HRESULT _stdcall Layout([out, retval] long * value );
    /**
        \brief Sets the layout used by the button image.

        This method sets the layout used by the button image. 
        The layout value are:
        \li 0 - left justified
        \li 1 - right justified
        \li 2 - top of bottom
        \li 3 - bottom of button

        \param value Sets the new alignment (0-left, 1-right, 2-top, 3-bottom).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000020)
    ]
    HRESULT _stdcall Layout([in] long value );
    /**
        \brief Gets the margins of the button control.

        Gets the margins of the button control.

        \param value The current value of the margin in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000021)
    ]
    HRESULT _stdcall Margin([out, retval] long * value );
    /**
        \brief Sets the margins of the button control.

        Sets the margins of the button control.

        \param value The new value of the margin in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000021)
    ]
    HRESULT _stdcall Margin([in] long value );
    /**
        \brief Gets the number images contained in the button's bitmap.

        Gets the number images contained in the button's bitmap, by default 
        this value is set to 1.

        \param value Receives the number of images in the button's bitmap.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000022)
    ]
    HRESULT _stdcall NumGlyphs([out, retval] long * value );
    /**
        \brief Sets the number images contained in the button's bitmap.

        Sets the number images contained in the button's bitmap, by default 
        this value is set to 1.

        \param value The number of images in the button's bitmap.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000022)
    ]
    HRESULT _stdcall NumGlyphs([in] long value );
    /**
        \brief Gets the visual style property of the control.

        This method gets the visual style property of the control.
        The property determines the look and feel of the control:
        \li 0 - Autodetect
        \li 1 - Windows 3.1 (old school 16-bit style)
        \li 2 - New Modern look and feel

        \param value The value of the style property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000023)
    ]
    HRESULT _stdcall Style([out, retval] long * value );
    /**
        \brief Sets the visual style property of the control.

        This method sets the visual style property of the control.
        The property determines the look and feel of the control:
        \li 0 - Autodetect
        \li 1 - Windows 3.1 (old school 16-bit style)
        \li 2 - New Modern look and feel

        \param value The new value of the style property (one of: 0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000023)
    ]
    HRESULT _stdcall Style([in] long value );
    /**
        \brief Gets the number of pixels between the image and the caption.

        The number of pixels between the image and the caption. Use -1 to make
        the button center the text between the image and the edge of the button.

        \param value The current value of the Spacing in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000024)
    ]
    HRESULT _stdcall Spacing([out, retval] long * value );
    /**
        \brief Sets the number of pixels between the image and the caption.

        The number of pixels between the image and the caption. Use -1 to make
        the button center the text between the image and the edge of the button.

        \param value The new value of the Spacing in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000024)
    ]
    HRESULT _stdcall Spacing([in] long value );
  };

  [
    uuid(0C3AE024-51A4-453F-91CB-B0EEBA175AED), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusControlMainMenu
    \brief <b>UNSUPPORTED</b> - Encapsulates the main application menu.
   
    <b>UNSUPPORTED</b> - 
    <p>Encapsulates the main application menu.</p>
  **/

   interface IExodusControlMainMenu: IDispatch
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    /**
        \brief Gets a count of the items in the menu.

        This method gets a count of the items in the menu.

        \param value Receives the count of items in the menu.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000003)
    ]
    HRESULT _stdcall ItemsCount([out, retval] long * value );
    /**
        \brief Gets a menu item at the given index.

        This method gets a menu item at the given index. Bounds checking is
        performed on the index and if out of bounds nil is returned.

        \param index The index of the item to be retrieved.
        \param value The desired IExodusControlMenuItem or nil if index is out of range.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000004)
    ]
    HRESULT _stdcall Items([in] long index, [out, retval] IExodusControlMenuItem ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets property determining if accelerator keys for menu items can be automatically reset.

        This method gets the property determining if the accelerator keys for
        menu items can be automatically reset.  This method resets accelerator
        keys to avoid conflicts with items added at runtime.  Accelerator keys
        for existing items will be preserved unless there is an unresolvable 
        conflict with a newly added item.

        value can be:
        \li 0 - accelerator keys adjusted automatically
        \li 1 - accelerator keys adjusted manually

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000005)
    ]
    HRESULT _stdcall AutoHotkeys([out, retval] long * value );
    /**
        \brief Sets property determining if accelerator keys for menu items can be automatically reset.

        This method sets the property determining if the accelerator keys for
        menu items can be automatically reset.  This method resets accelerator
        keys to avoid conflicts with items added at runtime.  Accelerator keys
        for existing items will be preserved unless there is an unresolvable 
        conflict with a newly added item.

        value can be:
        \li 0 - accelerator keys adjusted automatically
        \li 1 - accelerator keys adjusted manually

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000005)
    ]
    HRESULT _stdcall AutoHotkeys([in] long value );
    /**
        \brief Get whether redundant separator bars are automatically removed from the menu.

        Retrieve whether redundant separator bars are automatically removed from the menu.

		Use AutoLineReduction to indicate whether the menu should automatically ensure that the menu does not start or end with a separator bar and that two separator bars do not appear next to each other. This ensures that menus built dynamically at runtime do not have separator bars in undesired positions.
 
		Before the menu is displayed, it removes superfluous separator bars when AutoLineReduction is maAutomatic. When AutoLineReduction is maManual, the menu does not remove separators from the top-level menu items, but may remove superfluous separators from submenus, depending on the AutoLineReduction property of the menu item that is the parent of the submenu.				
        
        \p value can be:
        \li 0 - automatic
        \li 1 - manual
        
        \param value Receives the value of AutoLineReduction.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000006)
    ]
    HRESULT _stdcall AutoLineReduction([out, retval] long * value );
    /**
        \brief Set whether redundant separator bars are automatically removed from the menu.

        Specifies whether redundant separator bars are automatically removed from the menu.

		Use AutoLineReduction to indicate whether the menu should automatically ensure that the menu does not start or end with a separator bar and that two separator bars do not appear next to each other. This ensures that menus built dynamically at runtime do not have separator bars in undesired positions.
 
		Before the menu is displayed, it removes superfluous separator bars when AutoLineReduction is maAutomatic. When AutoLineReduction is maManual, the menu does not remove separators from the top-level menu items, but may remove superfluous separators from submenus, depending on the AutoLineReduction property of the menu item that is the parent of the submenu.				
        
        \p value can be:
        \li 0 - automatic
        \li 1 - manual
        
        \param value Sets the value of AutoLineReduction.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000006)
    ]
    HRESULT _stdcall AutoLineReduction([in] long value );
    /**
        \brief Gets the property that determines if the menus of secondary forms merge with the main menu.

        This method gets the property determining if the main menus of secondary
        forms merge with the main menu of the main form in non-MDI applications 
        at runtime.  The way menus merge depends on the value of the GroupIndex 
        property of the individual items.

        value can be:
        \li 0 - False (secondary form menus will NOT be merged)
        \li 1 - True  (secondary form menus WILL be merged)

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000007)
    ]
    HRESULT _stdcall AutoMerge([out, retval] long * value );
    /**
        \brief Sets the property that determines if the menus of secondary forms merge with the main menu.

        This method sets the property determining if the main menus of secondary
        forms merge with the main menu of the main form in non-MDI applications 
        at runtime.  The way menus merge depends on the value of the GroupIndex 
        property of the individual items.

        value can be:
        \li 0 - False (secondary form menus will NOT be merged)
        \li 1 - True  (secondary form menus WILL be merged)

        \param value The new value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000007)
    ]
    HRESULT _stdcall AutoMerge([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Gets property determining whether the menu items are drawn by the application.

        Retrieve whether the menu items are drawn by the application.

		Set OwnerDraw to true (1) to indicate that the application will draw menu items
        in an OnDrawItem event handler.
				
		\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives the state of ownerdraw.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000009)
    ]
    HRESULT _stdcall OwnerDraw([out, retval] long * value );
    /**
        \brief Sets the property determining whether menu items are drawn by the application.

        This property specifies whether the menu items are drawn by the application.

		Set OwnerDraw to true to indicate that the application will draw menu items in
        an OnDrawItem event handler.
				
		\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets the state of ownerdraw.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000009)
    ]
    HRESULT _stdcall OwnerDraw([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
  };

  [
    uuid(62B921DE-13F1-4F63-BCA6-30EE3C66D454), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusControlMemo
    \brief <b>UNSUPPORTED</b> - Encapsulates the Memo field control.
    
    <b>UNSUPPORTED</b>
    
    <p>Encapsulates the Memo field control.</p>
  **/

   interface IExodusControlMemo: IDispatch
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Can user tab to control?
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    /**
        \brief Get how the control aligns within its container (parent control).

        Retreive how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Receives control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Align([out, retval] long * value );
    /**
        \brief Set how the control aligns within its container (parent control).

        Specify how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Sets control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000D)
    ]
    HRESULT _stdcall Align([in] long value );
    /**
        \brief Gets the property controlling how the text is aligned within the control.

        Gets the property controlling how the text is aligned within the control.

        Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Receives control's alignment (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000E)
    ]
    HRESULT _stdcall Alignment([out, retval] long * value );
    /**
        \brief Set the property controlling how the text is aligned within the control.

        Specify how the text is aligned within the control.

		Use Alignment to change the way the text is formatted by the control.
				 
		\p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - center
 
        \param value Sets control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000E)
    ]
    HRESULT _stdcall Alignment([in] long value );
    /**
        \brief Get the cut of the inner bevel.

        Retreive the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. 
        
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000F)
    ]
    HRESULT _stdcall BevelInner([out, retval] long * value );
     /**
        \brief Sets the cut of the inner bevel.

        Specifies the cut of the inner bevel.

		Use BevelInner to specify whether the inner bevel has a raised, lowered, or flat look. 

		The inner bevel appears immediately inside the outer bevel. If there is no outer bevel, the inner bevel appears immediately inside the border. value are:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
		\p value should be one of 0, 1, 2, 3.
 
        \param value Sets control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000F)
    ]
    HRESULT _stdcall BevelInner([in] long value );
    /**
        \brief Get the control's bevel style.

        Retreive the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Receives control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000010)
    ]
    HRESULT _stdcall BevelKind([out, retval] long * value );
    /**
        \brief Sets the control's bevel style.

        Specify the control's bevel style.

		Use BevelKind to modify the appearance of a bevel. BevelKind influences how sharply the bevel stands out.
 
		BevelKind, in combination with BevelWidth and the cut of the bevel specified by BevelInner or BevelOuter , can create a variety of effects. 
        \p value can be:
        \li bkNone  (0)     No bevel
        \li bkTile  (1)     Sharply defined bevel
        \li bkSoft  (2)     Softer contrasts than bkTile
        \li bkFlat  (3)     Broad flat bevel
				 
        \param value Sets control's bevel style (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000010)
    ]
    HRESULT _stdcall BevelKind([in] long value );
    /**
        \brief Get the cut of the outer bevel.

        Retrieve the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
		The outer bevel appears immediately inside the border and outside the inner bevel. 
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Receives control's bevel.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000011)
    ]
    HRESULT _stdcall BevelOuter([out, retval] long * value );
    /**
        \brief Set the cut of the outer bevel.

        Specifies the cut of the outer bevel.

		Use BevelInner to specify whether the outer bevel has a raised, lowered, or flat look.
 
	    The outer bevel appears immediately inside the border and outside the inner bevel. 
        
        value can be:
        \li bvNone    (0)   No bevel
        \li bvLowered (1)   Lowered bevel
        \li bvRaised  (2)   Raised bevel
        \li bvSpace   (3)   Appears as a space if Kind is not bkTile else it's raised.
				 
        \p value should be one of 0, 1, 2, 3.
				 
        \param value Sets control's bevel (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000011)
    ]
    HRESULT _stdcall BevelOuter([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Get whether the edit control has a single line border around the client area.

        Retreive whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Receives control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000013)
    ]
    HRESULT _stdcall BorderStyle([out, retval] long * value );
    /**
        \brief Set whether the edit control has a single line border around the client area.

        Specify whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Sets control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000013)
    ]
    HRESULT _stdcall BorderStyle([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x00000015)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Get whether the visual indication of the selected text remains when focus shifts to another control.

        Retreive whether the visual indication of the selected text remains when focus shifts to another control.

				Set HideSelection to false when it is important to provide visual feedback of the selected portion of the text even when the edit control does not have focus. Set HideSelection to true to make the visual indication of selection appear only when the edit control has focus. HideSelection does not affect the actual value of the selection, only the visual indication. Always setting HideSelection to false can make forms containing many edit controls look busy.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's HideSelection value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001B)
    ]
    HRESULT _stdcall HideSelection([out, retval] long * value );
    /**
        \brief Set whether the visual indication of the selected text remains when focus shifts to another control.

        Specify whether the visual indication of the selected text remains when focus shifts to another control.

				Set HideSelection to false when it is important to provide visual feedback of the selected portion of the text even when the edit control does not have focus. Set HideSelection to true to make the visual indication of selection appear only when the edit control has focus. HideSelection does not affect the actual value of the selection, only the visual indication. Always setting HideSelection to false can make forms containing many edit controls look busy.
				 
				\p value should be 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's HideSelection value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001B)
    ]
    HRESULT _stdcall HideSelection([in] long value );
    /**
        \brief Gets the current value of the IME mode for this control.

        This method gets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value Receives the current value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001C)
    ]
    HRESULT _stdcall ImeMode([out, retval] long * value );
    /**
        \brief Sets the current value of the IME mode for this control.

        This method sets the current value of the IME mode for this control. IME
        is (Input Method Editor), Microsoft's solution to entering double-byte
        character sets with a standard keyboard (such as Chinese, Japanese, and 
        Korean).  This setting appears to control if IME is available for this control
        as well as how keyboard input is interepted.

        \c value can be:
        \li 0  - disabled (user cannot enable IME)
        \li 1  - close
        \li 2  - open
        \li 3  - don't care
        \li 4  - Single byte alphanumerics (SBC) (only for Korean/Japanese IME)
        \li 5  - Alpha double byte alphanumerics (DBC) (only for Korean/Japanese IME)
        \li 6  - Hiragana DBC (only Japanese IME)
        \li 7  - Katakana SBC (only Japanese IME)
        \li 8  - Katakana DBC (only Japanese IME)
        \li 9  - Chinese
        \li 10 - Hangul SBC (only Korean IME)
        \li 11 - Hangul DBC (only Korean IME)

        \param value The new value of the ImeMode property.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001C)
    ]
    HRESULT _stdcall ImeMode([in] long value );
    /**
        \brief Gets the ImeName property of the control.

        Gets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determines which IME tool will be used to convert user 
        input.

        \param value Receives the name of the current IME instance being used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001D)
    ]
    HRESULT _stdcall ImeName([out, retval] BSTR * value );
    /**
        \brief Sets the ImeName property of the control.

        Sets the ImeName property of the control. IME is Input method Editor,
        Microsoft's solution to entering double-byte character sets with a 
        standard keyboard (such as Chinese, Japanese, and Korean). 
        
        This property determine which IME tool will be used to convert user 
        input. If the supplied name isn't recognized the currently selected IME
        instance remains active and no error is thrown.

        \param value The new name of the IME instance to be used.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001D)
    ]
    HRESULT _stdcall ImeName([in] BSTR value );
    /**
        \brief Counts the number of lines in the memo control.

        This method counts the number of lines in the memo control.

        \param value Receives the count of lines in the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001E)
    ]
    HRESULT _stdcall LinesCount([out, retval] long * value );
    /**
        \brief Gets the string from the specified line of the memo control.

        This method gets the string from the specified line of the memo control.
        Index checking is performed and value remains unchanged if the index is
        out of bounds.

        \param index The zero based index of the desired line to get.
        \param value Receives the string at the specified line.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001F)
    ]
    HRESULT _stdcall Lines([in] long index, [out, retval] BSTR * value );
    /**
        \brief Sets the string at the specified line of the memo control.

        This method sets the string at the specified line of the memo control.
        Index checking is performed and if the index is out of bounds no change
        is made.

        \param index The zero based index of the desired line to change.
        \param value The new value of the specified line.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001F)
    ]
    HRESULT _stdcall Lines([in] long index, [in] BSTR value );
    /**
        \brief Gets the maximum number of characters the memo control can contain.

        This method gets the maximum number of characters the memo control can 
        contain.

        \param value Receives the maximum number of characters the control can contain.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000020)
    ]
    HRESULT _stdcall MaxLength([out, retval] long * value );
    /**
        \brief Sets the maximum number of characters the memo control can contain.

        This method sets the maximum number of characters the memo control can 
        contain.

        \param value The new value for the maximum number of characters the control can contain.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000020)
    ]
    HRESULT _stdcall MaxLength([in] long value );
    /**
        \brief Gets property that determines if characters typed in control will map to OEM.

        This method gets the property that determines if characters typed in control
        are converted from ANSI to OEM back to ANSI. The conversion is to ensure 
        entered characters can be unambiguously mapped to an OEM character.

        Note: If your application supports Unicode this functionality may not be needed.

        value can be:
        \li 0 - no conversion
        \li 1 - perform conversion of text from ANSI to OEM back to ANSI

        \param value The current value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000021)
    ]
    HRESULT _stdcall OEMConvert([out, retval] long * value );
    /**
        \brief Sets property that determines if characters typed in control will map to OEM.

        This method gets the property that determines if characters typed in control
        are converted from ANSI to OEM back to ANSI. The conversion is to ensure 
        entered characters can be unambiguously mapped to an OEM character.

        Note: If your application supports Unicode this functionality may not be needed.

        value can be:
        \li 0 - no conversion
        \li 1 - perform conversion of text from ANSI to OEM back to ANSI

        \param value The current value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/ 
    [
    propput, 
    id(0x00000021)
    ]
    HRESULT _stdcall OEMConvert([in] long value );
    [
    propget, 
    id(0x00000022)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000022)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000023)
    ]
    /**
        \brief Get where a control's color information come from

        Retreive property indicating whether or not control should use its parent's
        color.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentColor(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentColor([out, retval] long * value );
    [
    propput, 
    id(0x00000023)
    ]
    /**
        \brief Set whether or not control should use its parent's color.

        Specify where a control should get color information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's color. If \p value is
        false control uses its own color information.

        Flag is true by default so all controls on a IExodusControlForm share
        the same color. If control's color changes, this property is automatically
        set to false.

        \param value Should control use parent color?
        \return S_OK if successful, or an error value otherwise.
        \see Color
    **/
    HRESULT _stdcall ParentColor([in] long value );
    [
    propget, 
    id(0x00000024)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([out, retval] long * value );
    [
    propput,
    id(0x00000024)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows or NT 4.0 and later, unsupported
    **/
    HRESULT _stdcall ParentCtl3D([in] long value );
    [
    propget, 
    id(0x00000025)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000025)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000026)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000026)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000027)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Get whether the user can change the text of the edit control.

        Retreive whether the user can change the text of the edit control.

		To restrict the edit control to display only, set the ReadOnly property to true. Set ReadOnly to false to allow the contents of the edit control to be edited.
 
		Setting ReadOnly to true ensures that the text is not altered, while still allowing the user to select text. The selected text can then be manipulated by the application, or copied to the Clipboard.
		
		\p value is 1 (TRUE) or 0 (FALSE)
		
		\param value Receives Read Only value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000028)
    ]
    HRESULT _stdcall ReadOnly([out, retval] long * value );
    /**
        \brief Set whether the user can change the text of the edit control.

        Specifies whether the user can change the text of the edit control.

		To restrict the edit control to display only, set the ReadOnly property to true. Set ReadOnly to false to allow the contents of the edit control to be edited.
 
		Setting ReadOnly to true ensures that the text is not altered, while still allowing the user to select text. The selected text can then be manipulated by the application, or copied to the Clipboard.

		\p value is 1 (TRUE) or 0 (FALSE)
				
		\param value Sets Read Only value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000028)
    ]
    HRESULT _stdcall ReadOnly([in] long value );
    /**
        \brief Get whether the control has scroll bars.

        Gets property controlling whether the control has scroll bars.

		Use ScrollBars to give a multiline edit control horizontal or vertical scroll bars.
				
		\p value can be:
        \li 0 - none
        \li 1 - horizonal scroll bars
        \li 2 - vertical scroll bars
        \li 3 - both scroll bars
				
		\param value Receives Scroll Bars value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000029)
    ]
    HRESULT _stdcall ScrollBars([out, retval] long * value );
    /**
        \brief Set whether the control has scroll bars.

        Sets property controlling whether the control has scroll bars.

        Use ScrollBars to give a multiline edit control horizontal or vertical scroll bars.
				
        \p value can be:
        \li 0 - none
        \li 1 - horizonal scroll bars
        \li 2 - vertical scroll bars
        \li 3 - both scroll bars
				
        \param value Receives Scroll Bars value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000029)
    ]
    HRESULT _stdcall ScrollBars([in] long value );
    [
    propget, 
    id(0x0000002A)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000002A)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000002B)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x0000002B)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x0000002C)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000002C)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    /**
        \brief Get whether the user can insert return characters into the text.

        Retreive whether the user can insert return characters into the text.

        Set WantReturns to true to allow users to enter return characters into the text. Set WantReturns to false to allow the form to handle return characters instead. 
				
        For example, in a form with a default button (such as an OK button) and a memo control, if WantReturns is false, pressing Enter chooses the default button. If WantReturns is true, pressing Enter inserts a return character in the text.
 		Note: If WantReturns is false, users can still enter return characters into the text by pressing Ctrl+Enter.
 
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives WantReturns value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002D)
    ]
    HRESULT _stdcall WantReturns([out, retval] long * value );
    /**
        \brief Set whether the user can insert return characters into the text.

        Specifies whether the user can insert return characters into the text.

        Set WantReturns to true to allow users to enter return characters into the text. Set WantReturns to false to allow the form to handle return characters instead. 
				
        For example, in a form with a default button (such as an OK button) and a memo control, if WantReturns is false, pressing Enter chooses the default button. If WantReturns is true, pressing Enter inserts a return character in the text.
 				Note: If WantReturns is false, users can still enter return characters into the text by pressing Ctrl+Enter.
 
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets WantReturns value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002D)
    ]
    HRESULT _stdcall WantReturns([in] long value );
    /**
        \brief Get whether the user can insert tab characters into the text.

        Retreives property controlling whether the user can insert tab characters into the text.

        Set WantTabs to true to allow users to enter tab characters into the text. Set WantTabs to false if you want the tab character to select the next control on the form instead.
				
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives WantTabs value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002E)
    ]
    HRESULT _stdcall WantTabs([out, retval] long * value );
    /**
        \brief Set whether the user can insert tab characters into the text.

        Specifies whether the user can insert tab characters into the text.

        Set WantTabs to true to allow users to enter tab characters into the text. Set WantTabs to false if you want the tab character to select the next control on the form instead.
				
        \p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets WantTabs value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002E)
    ]
    HRESULT _stdcall WantTabs([in] long value );
    /**
        \brief Get whether the memo control inserts soft returns to wrap text.

        Gets the property indicating if the memo control inserts soft carriage 
        returns to wrap text at the right margin.  The inserted carriage returns
        DO NOT modify the user entered text only the display of the text. If set
        to false (0), the control shows a separate line only where the user entered
        return characters.

		\p value can be:
        \li 0 - false (no soft returns)
        \li 1 - true  (soft returns displayed)

        \param value Receives WordWrap value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002F)
    ]
    HRESULT _stdcall WordWrap([out, retval] long * value );
    /**
        \brief Sets whether the memo control inserts soft returns to wrap text.

        Sets the property indicating if the memo control inserts soft carriage 
        returns to wrap text at the right margin.  The inserted carriage returns
        DO NOT modify the user entered text only the display of the text. If set
        to false (0), the control shows a separate line only where the user entered
        return characters.

		\p value can be:
        \li 0 - false (no soft returns)
        \li 1 - true  (soft returns displayed)

        \param value Receives WordWrap value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002F)
    ]
    HRESULT _stdcall WordWrap([in] long value );
  };

  [
    uuid(AF41AC90-38C4-46FB-9A45-D7C26ECB2E1C), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusControlPageControl
    \brief <b>UNSUPPORTED</b> - A set of pages used to make a multiple page dialog box.
   
    <b>UNSUPPORTED</b>
    Use IExodusControlPageControl to create a multiple page dialog or tabbed notebook.
  **/

   interface IExodusControlPageControl: IDispatch
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked property.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    [
    propget, 
    id(0x0000000C)
    ]
    /**
        \brief Get how the control aligns within its container (parent control).

        Retreive how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Receives control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Align([out, retval] long * value );
    [
    propput, 
    id(0x0000000C)
    ]
    /**
        \brief Set how the control aligns within its container (parent control).

        Specify how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.

        \p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Sets control's alignment.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Align([in] long value );
    [
    propget, 
    id(0x0000000D)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000000D)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    [
    propget, 
    id(0x0000000E)
    ]
    /**
        \brief Get whether the control can be the target of drag-and-dock operations.

        Retreive whether the control can be the target of drag-and-dock operations.

				Set DockSite to true to allow other controls to be docked to this windowed control.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's DockSite setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DockSite([out, retval] long * value );
    [
    propput, 
    id(0x0000000E)
    ]
    /**
        \brief Set whether the control can be the target of drag-and-dock operations.

        Specifies whether the control can be the target of drag-and-dock operations.

				Set DockSite to true to allow other controls to be docked to this windowed control.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's DockSite setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DockSite([in] long value );
    [
    propget, 
    id(0x0000000F)
    ]
    /**
        \brief Get cursor used during dragging operations.

        Retrieve an integer enumeration of the cursor used during dragging operations.

        \p value will be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([out, retval] long * value );
    [
    propput, 
    id(0x0000000F)
    ]
    /**
        \brief Set the cursor used during drag operations

        Specify an integer enumeration of the cursor used during dragging operations.

        \p value must be an integer enumeration of a cursor.

        todo: define this cursor enumeration somewhere.

        \param value cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragCursor([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get whether labels on the tab under the mouse are automatically highlighted.

        Retrieves whether labels on the tab under the mouse are automatically highlighted.

				Set HotTrack to true to provide visual feedback to the user about which tab would be selected if the mouse button is clicked. HotTrack is especially useful when the tab control contains multiple rows of tabs.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's HotTrack setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HotTrack([out, retval] long * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set whether labels on the tab under the mouse are automatically highlighted.

        Specifies whether labels on the tab under the mouse are automatically highlighted.

				Set HotTrack to true to provide visual feedback to the user about which tab would be selected if the mouse button is clicked. HotTrack is especially useful when the tab control contains multiple rows of tabs.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's HotTrack setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HotTrack([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get whether the tabs can appear on more than one row.

        Retrieves whether the tabs can appear on more than one row.

				Use MultiLine to determine how the tabs are displayed. If MultiLine is true, the tabs are displayed on more than one row when the number of tabs exceeds the number that fits across the top of the tab control. How many rows is determined by how many tabs are in the tab control. If MultiLine is false, the tabs are displayed on one row only, and the user must scroll the displayed scroll arrows to view all the tabs.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's MultiLine setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall MultiLine([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set whether the tabs can appear on more than one row.

        Specifies whether the tabs can appear on more than one row.

				Use MultiLine to determine how the tabs are displayed. If MultiLine is true, the tabs are displayed on more than one row when the number of tabs exceeds the number that fits across the top of the tab control. How many rows is determined by how many tabs are in the tab control. If MultiLine is false, the tabs are displayed on one row only, and the user must scroll the displayed scroll arrows to view all the tabs.
				
				\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's MultiLine setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall MultiLine([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get whether the menu items are drawn by the application.

        Retrieve whether the menu items are drawn by the application.

		Set OwnerDraw to true to indicate that the application will draw menu items in an OnDrawItem event handler.
				
		\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives the state of ownerdraw.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall OwnerDraw([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Set whether the menu items are drawn by the application.

        Specifies whether the menu items are drawn by the application.

		Set OwnerDraw to true to indicate that the application will draw menu items in an OnDrawItem event handler.
				
		\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets the state of ownerdraw.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall OwnerDraw([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get whether rows of tabs stretch to fill the width of the control.

        Retrieve whether rows of tabs stretch to fill the width of the control.

				Use RaggedRight to get or set whether rows of tabs stretch to fill the width of the control.
				
				\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives the RaggedRight state.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall RaggedRight([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Swhether rows of tabs stretch to fill the width of the control.

        Specifies whether rows of tabs stretch to fill the width of the control.

				Use RaggedRight to get or set whether rows of tabs stretch to fill the width of the control.
				
				\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets the RaggedRight state.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall RaggedRight([in] long value );
    [
    propget, 
    id(0x0000001C)
    ]
    /**
        \brief Get how the rows of tabs are scrolled in a multi-line tab control.

        Retrieve how the rows of tabs are scrolled in a multi-line tab control.

				Set ScrollOpposite to determine where previous rows of tabs in a multi-line tab control move when the user selects a tab in another row.
				
				\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Receives the ScrollOpposite state.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ScrollOpposite([out, retval] long * value );
    [
    propput, 
    id(0x0000001C)
    ]
    /**
        \brief Set how the rows of tabs are scrolled in a multi-line tab control.

        Specifies how the rows of tabs are scrolled in a multi-line tab control.

				Set ScrollOpposite to determine where previous rows of tabs in a multi-line tab control move when the user selects a tab in another row.
				
				\p value is 1 (TRUE) or 0 (FALSE)
				
        \param value Sets the ScrollOpposite state.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ScrollOpposite([in] long value );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000001E)
    ]
    /**
        \brief Get the style of the tab control.

        Retrieve the style of the tab control.

		Use Style to determine the appearance of the tabs. These can appear as notebook tabs or as buttons.
				
		\p value can be:
        \li 0 - tabs
        \li 1 - buttons
        \li 2 - flat buttons

        \param value The appears of the tabs (0 tabs, 1 buttons, 2 flat buttons).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Style([out, retval] long * value );
    [
    propput, 
    id(0x0000001E)
    ]
    /**
        \brief Set the style of the tab control.

        Specifies the style of the tab control.

		Use Style to determine the appearance of the tabs. These can appear as notebook tabs or as buttons.
				
		\p value can be:
        \li 0 - tabs
        \li 1 - buttons
        \li 2 - flat buttons
				
        \param value Sets the tab control style.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Style([in] long value );
    [
    propget, 
    id(0x0000001F)
    ]
    /**
        \brief Get the height, in pixels, of the tabs in the tab control.

        Retrieve the height, in pixels, of the tabs in the tab control.

		Set TabHeight to control the height of the tabs that appear above the client region of the tab control. The TabHeight property is the vertical size in pixels of the individual tabs. If TabHeight is set to 0, the tabs automatically size themselves to fit their text.
				
		\p value is in pixels.
				
        \param value Receives the Tab Height in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabHeight([out, retval] long * value );
    [
    propput, 
    id(0x0000001F)
    ]
    /**
        \brief Set the height, in pixels, of the tabs in the tab control.

        Specifies the height, in pixels, of the tabs in the tab control.

				Set TabHeight to control the height of the tabs that appear above the client region of the tab control. The TabHeight property is the vertical size in pixels of the individual tabs. If TabHeight is set to 0, the tabs automatically size themselves to fit their text.
				
				\p value is in pixels.
				
        \param value Sets the Tab Height in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabHeight([in] long value );
    [
    propget, 
    id(0x00000020)
    ]
    /**
        \brief Get the Identity of the selected tab on a tab control.

        Retrieve the Identity of the selected tab on a tab control.

				Read TabIndex to determine which tab was selected by the user. TabIndex is the index of the tab in the list of labels maintained by the Tabs property. The first (leftmost) tab has a TabIndex of 0, the next has 1, and so on. If no tabs are selected, TabIndex has a value of -1.
				
				Set TabIndex to programmatically change the selected tab in the tab control.
				
        \param value Receives the current TabIndex.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabIndex([out, retval] long * value );
    [
    propput, 
    id(0x00000020)
    ]
    /**
        \brief Set the Identity of the selected tab on a tab control.

        Specifies the Identity of the selected tab on a tab control.

				Read TabIndex to determine which tab was selected by the user. TabIndex is the index of the tab in the list of labels maintained by the Tabs property. The first (leftmost) tab has a TabIndex of 0, the next has 1, and so on. If no tabs are selected, TabIndex has a value of -1.
				
				Set TabIndex to programmatically change the selected tab in the tab control.
				
        \param value Sets the current TabIndex.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabIndex([in] long value );
    [
    propget, 
    id(0x00000021)
    ]
    /**
        \brief Get position if control in parent's tab order.

        Retrieve the position of the control in the parent's tab order.

        See TabOrder(long value) for additional information.
                 
        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabOrder([out, retval] long * value );
    [
    propput, 
    id(0x00000021)
    ]
    /**
        \brief Set position if control in parent's tab order.

        Specify the position of the control in the parent's tab order.

        TabOrder is the order in which child windows are visited when the user
        presses the Tab key. The control with the TabOrder value of 0 is the
        control that has the focus when the form first appears.

        Each control has a unique tab-order value within its parent.
        If you change the TabOrder value of one control to be the
        same as the value of a second control, the TabOrder value for all
        the other controls changes. For example, suppose a control is sixth
        in the tab order. If you change the control's TabOrder
        value to 3 (making the control fourth in the tab order),
        the control that was originally fourth in the tab order now
        becomes fifth, and the control that was fifth becomes sixth.

        Assigning TabOrder a value greater than the number of controls
        contained in the parent control moves the control to the end of the
        tab order. The control does not take on the assigned value of
        TabOrder, but instead is given the number that assures the control
        is the last in the tab order.

        \note
        TabOrder is meaningful only if TabStop is true and if the control has
        a parent. (The TabOrder property of an IExodusControlForm is not used
        unless the form is the child of another form.) A control with a
        TabOrder of -1 has no parent, and therefore cannot be reached
        by pressing the Tab key. To remove a parented control from the Tab
        order, set its TabStop to false.

        \param value The control's position in the tab order.
        \return S_OK if successful, or an error value otherwise.
        \see TabStop
    **/
    HRESULT _stdcall TabOrder([in] long value );
    [
    propget, 
    id(0x00000022)
    ]
    /**
        \brief Get where tabs appear in the control.

        Retrieve property indicating where tabs appear in the control.

		\p value can be:
        \li 0 - top
        \li 1 - bottom
        \li 2 - left
        \li 3 - right
				
        \param value Receives the current TabPosition (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabPosition([out, retval] long * value );
    [
    propput, 
    id(0x00000022)
    ]
    /**
        \brief Set where tabs appear in the control.

        Sets the property indicating where tabs appear in the control.

		\p value can be:
        \li 0 - top
        \li 1 - bottom
        \li 2 - left
        \li 3 - right
				
        \param value The new value for TabPosition (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabPosition([in] long value );
    [
    propget, 
    id(0x00000023)
    ]
    /**
        \brief Get whether or not the user can tab to the control.

        Retrieve a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key.

        \param value Receives the TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([out, retval] long * value );
    [
    propput, 
    id(0x00000023)
    ]
    /**
        \brief Set whether or not the user can tab to the control.

        Specify a property indicating whether or not the tab key accesses the control.

        if \p value is 0 (FALSE) the control is not in the tab order and the
        user cannot use the tab key to move to the control. If \p value is
        1 (TRUE) the control is in the tab order and the user can navigate to it
        using the tab key. All other values are ignored and TabStop is not changed.

        \param value The new TabStop property value (0 or 1).
        \return S_OK if successful, or an error value otherwise.
        \see TabOrder
    **/
    HRESULT _stdcall TabStop([in] long value );
    [
    propget, 
    id(0x00000024)
    ]
    /**
        \brief Get the horizontal size, in pixels, of the tabs in the tab control.

        Retrieve the horizontal size, in pixels, of the tabs in the tab control.
        
        TabWidth specifies a fixed width for all tabs. If TabWidth is 0, then each tab is automatically made wide enough to show its caption.

		\p value is in pixels.
				
        \param value Receives the current TabWidth in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabWidth([out, retval] long * value );
    [
    propput, 
    id(0x00000024)
    ]
    /**
        \brief Set the horizontal size, in pixels, of the tabs in the tab control.

        Specifies the horizontal size, in pixels, of the tabs in the tab control.
        
        TabWidth specifies a fixed width for all tabs. If TabWidth is 0, then each tab is automatically made wide enough to show its caption.

				\p value is in pixels.
				
        \param value Sets the current TabWidth in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall TabWidth([in] long value );
    [
    propget, 
    id(0x00000025)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x00000025)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
  };

  [
    uuid(0706359E-DD10-4D98-862B-7417E5E79DE8), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusControlSpeedButton
    \brief <b>UNSUPPORTED</b> - An interface to a SpeedButton control.
   
    <b>UNSUPPORTED</b>

    An interface to a SpeedButton control.
    A SpeedButton is a button that is used to execute commands or set modes.
    
  **/

   interface IExodusControlSpeedButton: IDispatch
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    /**
        \brief Get whether all speed buttons in the group that contains this speed button can be unselected at the same time.

        Retrieve whether all speed buttons in the group that contains this speed button can be unselected at the same time.

        Use AllowAllUp with a group of buttons that belong to the same group. Speed buttons belong to the same group if they have the same value on their GroupIndex property. Buttons in the same group behave in a mutually exclusive fashion: when one button in the group is selected (down), all other buttons in the group become unselected (up). 
        
				If AllowAllUp is true, all of the speed buttons in a group can be unselected at the same time. Clicking the single selected button in the group will deselect that button without selecting another. If AllowAllUp is false, exactly one of the buttons in the group must be selected at any time. Clicking a down button won't return the button to its up state.
 
				Changing the value of the AllowAllUp property for one speed button in a group changes the AllowAllUp value for all buttons in the group.

        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Receives current value.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    [
    propget, 
    id(0x0000000C)
    ]
    HRESULT _stdcall AllowAllUp([out, retval] long * value );
    /**
        \brief Set whether all speed buttons in the group that contains this speed button can be unselected at the same time.

        Specify whether all speed buttons in the group that contains this speed button can be unselected at the same time.
        
        Use AllowAllUp with a group of buttons that belong to the same group. Speed buttons belong to the same group if they have the same value on their GroupIndex property. Buttons in the same group behave in a mutually exclusive fashion: when one button in the group is selected (down), all other buttons in the group become unselected (up). 
        
				If AllowAllUp is true, all of the speed buttons in a group can be unselected at the same time. Clicking the single selected button in the group will deselect that button without selecting another. If AllowAllUp is false, exactly one of the buttons in the group must be selected at any time. Clicking a down button won't return the button to its up state.
 
				Changing the value of the AllowAllUp property for one speed button in a group changes the AllowAllUp value for all buttons in the group.
				
				Note: If GroupIndex is zero, AllowAllUp has no effect.
 

        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Receives current value.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    [
    propput, 
    id(0x0000000C)
    ]
    HRESULT _stdcall AllowAllUp([in] long value );
    [
    propget, 
    id(0x0000000D)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000000D)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Get the GroupIndex value.

        Retrieve the GroupIndex value for this control.
        
        When GroupIndex is 0, the button behaves independently of all other buttons on the form. When the user clicks such a speed button, the button appears pressed (in its clicked state) and then returns to its normal up state when the user releases the mouse button.
 
				When GroupIndex is greater than 0, the button remains selected (in its down state) when clicked by the user. When the user clicks a selected button, it returns to the up state, unless Caption is false. Setting the GroupIndex property of a single speed button to a value greater than 0 causes the button to behave as a two-state button when Caption is true.
 
				Speed buttons with the same GroupIndex property value (other than 0), work together as a group. When the user clicks one of these buttons, it remains selected until the user clicks another speed button belonging to the same group. Speed buttons used in this way can present mutually exclusive choices to the user.

        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Receives GroupIndex value.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000E)
    ]
    HRESULT _stdcall GroupIndex([out, retval] long * value );
    /**
        \brief Set the GroupIndex value.

        Specify the GroupIndex value for this control.  The GroupIndex value allows speed buttons to work together as a group.
        
        When GroupIndex is 0, the button behaves independently of all other buttons on the form. When the user clicks such a speed button, the button appears pressed (in its clicked state) and then returns to its normal up state when the user releases the mouse button.
 
				When GroupIndex is greater than 0, the button remains selected (in its down state) when clicked by the user. When the user clicks a selected button, it returns to the up state, unless Caption is false. Setting the GroupIndex property of a single speed button to a value greater than 0 causes the button to behave as a two-state button when Caption is true.
 
				Speed buttons with the same GroupIndex property value (other than 0), work together as a group. When the user clicks one of these buttons, it remains selected until the user clicks another speed button belonging to the same group. Speed buttons used in this way can present mutually exclusive choices to the user.

        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value GroupIndex value to set.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000E)
    ]
    HRESULT _stdcall GroupIndex([in] long value );
    /**
        \brief Get the selected state of the SpeedButton.

				Read Down to determine whether a speed button is selected. The Down property only applies if the GroupIndex property of the button is nonzero.
				 
				When GroupIndex is greater than 0, set Down to true to select a button. When the user clicks on a button in the unselected (up) state, the button is selected and Down is set to true. When the user clicks on a button in the selected (down) state, if Caption is true, the button becomes unselected and Down is set to false.
				 
				At design time, specify which button in a group is the initially selected button by setting the Down property of the selected button to true.

				Note:  When GroupIndex is 0, buttons do not remain in the selected state when clicked.
 

        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value GroupIndex value to set.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Down([out, retval] long * value );
    /**
        \brief Set the selected state of the SpeedButton.

				Set Down to specify whether a speed button is selected. The Down property only applies if the GroupIndex property of the button is nonzero.
				 
				When GroupIndex is greater than 0, set Down to true to select a button. When the user clicks on a button in the unselected (up) state, the button is selected and Down is set to true. When the user clicks on a button in the selected (down) state, if Caption is true, the button becomes unselected and Down is set to false.
				 
				At design time, specify which button in a group is the initially selected button by setting the Down property of the selected button to true.

				Note:  When GroupIndex is 0, buttons do not remain in the selected state when clicked.
 

        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value GroupIndex value to set.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000F)
    ]
    HRESULT _stdcall Down([in] long value );
    [
    propget, 
    id(0x00000010)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000010)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x00000012)
    ]
    /**
        \brief Get whether the button has a 3D border that provides a raised or lowered look.

        Retrieve whether the button has a 3D border that provides a raised or lowered look.

        Set Flat to true to remove the raised border when the button is unselected and the lowered border when the button is clicked or selected. When Flat is true, use separate bitmaps for the different button states to provide visual feedback to the user about the button state.
        
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value 1 (TRUE) for flat, 0 (FALSE) for 3D.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Flat([out, retval] long * value );
    [
    propput, 
    id(0x00000012)
    ]
    /**
        \brief Set whether the button has a 3D border that provides a raised or lowered look.

        Specify whether the button has a 3D border that provides a raised or lowered look.

        Set Flat to true to remove the raised border when the button is unselected and the lowered border when the button is clicked or selected. When Flat is true, use separate bitmaps for the different button states to provide visual feedback to the user about the button state.
        
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value 1 (TRUE) for flat, 0 (FALSE) for 3D.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Flat([in] long value );
    [
    propget, 
    id(0x00000013)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get where the image or text appears on the speed button.

        Retrieve where the image or text appears on the speed button.

        Use Layout to determine the position of the bitmap specified by the Glyph property or the text specified by the Caption property.
        
        \p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - top
        \li 3 - bottom

        \param value Receives the control's current layout (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Layout([out, retval] long * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set where the image or text appears on the speed button.

        Specify where the image or text appears on the speed button.

        Use Layout to determine the position of the bitmap specified by the Glyph property or the text specified by the Caption property.
        
        \p value can be:
        \li 0 - left
        \li 1 - right
        \li 2 - top
        \li 3 - bottom

        \param value Sets the control's current layout (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Layout([in] long value );
    [
    propget, 
    id(0x00000015)
    ]
    /**
        \brief Get the number of pixels between the edge of the button and the image or caption drawn on its surface.

        Retrieve the number of pixels between the edge of the button and the image or caption drawn on its surface.

        Use Margin to specify the indentation of the image specified by the Glyph property or the text specified by the Caption property. The edges that Margin separates depends on the Layout property. If Layout is blGlyphLeft, the margin appears between the left edge of the image or caption and the left edge of the button. If Layout is blGlyphRight, the margin separates the right edges. If Layout is blGlyphTop, the margin separates the top edges, and if Layout is blGlyphBottom, the margin separates the bottom edges.
 
				If \p value Margin is –1, the image or text is centered on the button.


        \param value Receives the control's current margin.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Margin([out, retval] long * value );
    [
    propput, 
    id(0x00000015)
    ]
    /**
        \brief Set the number of pixels between the edge of the button and the image or caption drawn on its surface.

        Specify the number of pixels between the edge of the button and the image or caption drawn on its surface.

        Use Margin to specify the indentation of the image specified by the Glyph property or the text specified by the Caption property. The edges that Margin separates depends on the Layout property. If Layout is blGlyphLeft, the margin appears between the left edge of the image or caption and the left edge of the button. If Layout is blGlyphRight, the margin separates the right edges. If Layout is blGlyphTop, the margin separates the top edges, and if Layout is blGlyphBottom, the margin separates the bottom edges.
 
				If \p value Margin is –1, the image or text is centered on the button.


        \param value Sets the control's current margin.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Margin([in] long value );
    [
    propget, 
    id(0x00000016)
    ]
    /**
        \brief Get the number of images included in the Glyph property.

        Retrieve the number of images included in the Glyph property.

        Use NumGlyphs to the number of images provided by the bitmap assigned to the Glyph property. All images must be the same size and next to each other in a row. The Glyph property can provide up to four images.

        \param value Receives the control's current number of glyphs.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall NumGlyphs([out, retval] long * value );
    [
    propput, 
    id(0x00000016)
    ]
    /**
        \brief Set the number of images included in the Glyph property.

        Specify the number of images included in the Glyph property.

        Use NumGlyphs to the number of images provided by the bitmap assigned to the Glyph property. All images must be the same size and next to each other in a row. The Glyph property can provide up to four images.

        \param value Sets the control's current number of glyphs.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall NumGlyphs([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000018)
    ]
    /**
        \brief Get where a control looks to find out if its Help Hint should be shown.

        Retrieve where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        \p value will either be 0 (FALSE) or 1 (TRUE). See ParentShowHint(long value)
        for additional information.

        \param value Should control use parent's ShowHint property?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000018)
    ]
    /**
        \brief Set where a control looks to find out if its Help Hint should be shown.

        Specify where a control looks to find out if its Help Hint should be
        shown, either from its parent or itself.

        Use ParentShowHint to ensure that all the controls on a form either
        uniformly show their Help Hints or uniformly do not show them.
        If ParentShowHint is true, the control uses the ShowHint value of its
        parent. If ParentShowHint is false, the control uses the value of its
        own ShowHint.

        To provide Help Hints for only selected controls on a form,
        set the ShowHint for those controls that should have Help Hints to true,
        and ParentShowHint becomes false automatically.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and ParentShowHelp's value does not change.

        \param value Whether control should use parent's ShowHint.
        \return S_OK if successful, or an error value otherwise.
        \see Hint, ShowHint
    **/
    HRESULT _stdcall ParentShowHint([in] long value );
    [
    propget, 
    id(0x00000019)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000019)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000001B)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x0000001B)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    [
    propget, 
    id(0x0000001C)
    ]
    /**
        \brief Get where the image and text appear on a speed button.

        Retrieve where the image and text appear on a speed button.

        Use Spacing to the number of pixels that should appear between the image specified in the Glyph property and the text specified in the Caption property. 

				If Spacing is a positive number, its value is the number of pixels between the image and text. If Spacing is 0, the image and text appear flush with each other. If Spacing is -1, the text appears centered between the image and the button edge.

        \param value Receives the control's current spacing.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Spacing([out, retval] long * value );
    [
    propput, 
    id(0x0000001C)
    ]
    /**
        \brief Set where the image and text appear on a speed button.

        Specifies where the image and text appear on a speed button.

        Use Spacing to the number of pixels that should appear between the image specified in the Glyph property and the text specified in the Caption property. 

				If Spacing is a positive number, its value is the number of pixels between the image and text. If Spacing is 0, the image and text appear flush with each other. If Spacing is -1, the text appears centered between the image and the button edge.

        \param value Sets the control's current spacing.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Spacing([in] long value );
    [
    propget, 
    id(0x0000001D)
    ]
    /**
        \brief Get whether the background of the control is transparent.

        Retrieve whether the background of the control is transparent.

        Use Transparent to specify whether the background of the control is transparent.
        
        \p value will be either 0 (FALSE) or 1 (TRUE).

        \param value Receives the control's transparency.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Transparent([out, retval] long * value );
    [
    propput, 
    id(0x0000001D)
    ]
    /**
        \brief Set whether the background of the control is transparent.

        Sets whether the background of the control is transparent.

        Use Transparent to specify whether the background of the control is transparent.
        
        \p value will be either 0 (FALSE) or 1 (TRUE).

        \param value The new value of the control's transparency.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Transparent([in] long value );
    [
    propget, 
    id(0x0000001E)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x0000001E)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
  };

  [
    uuid(7949D67E-E287-4643-90DA-E6FE7EDEFA97), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusToolbar
    \brief Inteface to the Exodus Toolbar.
    
    This is the interface to the Exodus toolbar.
  **/

   interface IExodusToolbar: IDispatch
  {
    /**
        \brief How many buttons are on the toolbar.

        Called to get the count of the number of IExodusToolbarButton buttons are on the toolbar.

        \param value Number of buttons on the toolbar
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x000000C9)
    ]
    HRESULT _stdcall Count([out, retval] long * value );
    /**
        \brief Get a handle to a specific button on the toolbar.

        Get a handle to a specific button on the toolbar.

        \param index Zero based index of the desired button.
        \param Button Handle to the IExodusToolBarButton.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall GetButton([in] long index, [out, retval] IExodusToolbarButton ** Button /*Warning: unable to validate structure name: */ );
    /**
        \brief Add a new button to the Toolbar.

        Add a new button to the Toolbar.

        \param imageID ID of the for the toolbar button.
        \param button Handle to the IExodusToolbarButton to be added.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall AddButton([in] BSTR imageID, [out, retval] IExodusToolbarButton ** button /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(2F60EC05-634D-44B2-BECB-059169BA1459), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusControlForm
    \brief <b>UNSUPPORTED</b>
  
    <b>UNSUPPORTED</b> 
  **/

   interface IExodusControlForm: IDispatch
  {
    [
    propget, 
    id(0x00000001)
    ]
    /**
        \brief Get the name of the control

        Retrieve the name of the control.

        \c value must have at least 250 bytes allocated. Comparisions of
        control names should be case insensitive.

        \param value The control's name
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000001)
    ]
    /**
        \brief Set the name of the control.

        Specify a unique name for the control.

        \note
        This is the name code uses when referencing
        the control and changing it at runtime could cause exceptions to
        be thrown. Be very carefull when modifying the names of existing controls.

        \c value must be a valid Pascal identifier. Only the
        first 250 characters are significant and comparisions between
        names are case insensitive.

        \param value The new name of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Name([in] BSTR value );
    [
    propget, 
    id(0x00000002)
    ]
    /**
        \brief Get the Tag from the control.

        Retrieve user data associated with a control.

        See Tag(long value) for additional information.

        \param value The current user data for this control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([out, retval] long * value );
    [
    propput, 
    id(0x00000002)
    ]
    /**
        \brief Set the Tag for the control.

        Specify user data associated with a control.

        Each control has a 32 bit value associated with it that
        may contain any data. Typical uses are as a pointer to additonal
        information or for bitmasked properties.

        \param value The new user data for this control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Tag([in] long value );
    [
    propget, 
    id(0x00000003)
    ]
    /**
        \brief Get the horizontal coordinate of the left edge of a control relative to its parent.

        Retrieve the horizontal coordinate of the left edge of a control relative to its parent.

        \p value is in pixels.
        See Left(long value) for additional information.

        \param value Left side poition of control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Left([out, retval] long * value );
    [
    propput, 
    id(0x00000003)
    ]
    /**
        \brief Set the horizontal coordinate of the left edge of a control relative to its parent.

        Specify the horizontal coordinate of the left edge of a control relative to its parent.

        If the control is contained in another control, the Left (and Top)
        are relative to the parent control. \n
        For IExodusControlForm, Left is relative to the screen.

        \p value is in pixels.

        \param value New horizontal position of LHS of control.
        \return S_OK if successful, or an error value otherwise.
        \see Top
    **/
    HRESULT _stdcall Left([in] long value );
    [
    propget, 
    id(0x00000004)
    ]
    /**
        \brief Get the verticle coordinate of the top edge of a control relative to its parent.

        Retrieve the verticle coordinate of the top edge of a control relative to its parent.

        \p value is in pixels.
        See Top(long value) for additional information.

        \param value Coordinate of the top of the control
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Top([out, retval] long * value );
    [
    propput, 
    id(0x00000004)
    ]
    /**
        \brief Set the verticle coordinate of the top edge of a control relative to its parent.

        Specify the verticle coordinate of the top edge of a control relative to its parent.

        If the control is contained in another control, top (and left) coordinates
        are relative to the parent control. \n
        For IExodusControlForm, top is relative to the screen.

        \p value is in pixels.

        \param value New verticle position of top of control.
        \return S_OK if successful, or an error value otherwise.
        \see Left
    **/
    HRESULT _stdcall Top([in] long value );
    [
    propget, 
    id(0x00000005)
    ]
    /**
        \brief Get the horizontal size of the control.

        Retrieve the horizontal size of the control.

        \p value is in pixels.

        \param value The horizontal size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([out, retval] long * value );
    [
    propput, 
    id(0x00000005)
    ]
    /**
        \brief  Set the horizontal size of the control.

         Specify the horizontal size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting width has no effect.

        \param value The new width
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Width([in] long value );
    [
    propget, 
    id(0x00000006)
    ]
    /**
        \brief Get the verticle size of the control.

        Retrieve the verticle size of the control.

        \p value is in pixels.

        \param value The verticle size of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([out, retval] long * value );
    [
    propput, 
    id(0x00000006)
    ]
    /**
        \brief  Set the verticle size of the control.

         Specify the verticle size of the control.

         \p value is in pixels.
         \note For IExodusPageControl controls setting height has no effect.

        \param value The new height
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Height([in] long value );
    [
    propget, 
    id(0x00000007)
    ]
    /**
        \brief Get the cursor displayed when mouse is within bounds of control.

        Retrieve an enumeration of the cursor shown when the mouse is within
        the bounds of the control.

        \p value will be an integer enumeration into the currently loaded
        cursor set.\n
        See Cursor(long value) for additional information

        \param value The cursor enumeration
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([out, retval] long * value );
    [
    propput, 
    id(0x00000007)
    ]
    /**
        \brief Set the cursor displayed when mouse is within bounds of control.

        Specify the cursor displayed when the mouse pointer is within the control's
        boundries.

        \p value is one of the enumerated cursor types defined by the currently
        loaded cursor set.

        todo: what is this enumeration? at least provide default and wait.

        \param value The new cursor.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Cursor([in] long value );
    [
    propget, 
    id(0x00000008)
    ]
    /**
        \brief Get the text string that can appear when the user moves the mouse over the control.

        Retrieve the text string that can appear when the user moves the mouse
        over the control.

        \p value should be have space for at least 256 characters allocated.
        See Hint(BSTR value) for additional information.

        \param value The string that will show if hinting is enabled.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Hint([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000008)
    ]
    /**
        \brief Set the text string that can appear when the users moves the mouse over the control

        Specify the text string that can appear when the users moves the mouse over the control.

        Use hint to provide a string of help text as a Help Hint.
        A Help Hint is a box containing help text that appears for a control
        when the user moves the mouse pointer over the control and pauses
        momentarily.

        To set up Help Hints:\n
        Specify the hint of each control for which a Help Hint should appear.\n
        Set ShowHint of each appropriate control to true, or set
        ParentShowHint of all controls to true and set ShowHint
        of the IExodusControlForm to true.

        \param value The new hint.
        \return S_OK if successful, or an error value otherwise.
        \see ShowHint, ParentShowHint
    **/
    HRESULT _stdcall Hint([in] BSTR value );
    [
    propget, 
    id(0x00000009)
    ]
    /**
        \brief Get whether the control's context sensitive help topic is identified by context ID or by keyword.

        Retrieve whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        \p value will be either 0 (keyword) or 1 (context ID). See HelpType(long value)
        for additional information.

        \param value keyword or context ID indicator.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall HelpType([out, retval] long * value );
    [
    propput, 
    id(0x00000009)
    ]
    /**
        \brief Set whether the control's context sensitive help topic is identified by context ID or by keyword.

        Specify whether the control's context sensitive help topic is
        identified by context ID or by keyword.

        Set HelpType to specify whether the control identifies its context-sensitive
        help topic by ID or keyword. When HelpType is context ID (1), HelpContext
        identifies the control's help topic. When HelpType is keyword (0),
        HelpKeyword identifies the control's help topic.

        \p value must either be 0 (keyword) or 1 (context ID). All other values are
        ignored and HelpType does not change.
        
        \param value context ID or keyword indicator.
        \return S_OK if successful, or an error value otherwise.
        \see HelpContext, HelpKeyword
    **/
    HRESULT _stdcall HelpType([in] long value );
    [
    propget, 
    id(0x0000000A)
    ]
    /**
        \brief Get keyword for control's context-sensitive help topic

        Retrieve keyword for control's context-sensitive help topic.

        If \p value is an empty string (default), no help topic is
        provided for this control.

        \param value The help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([out, retval] BSTR * value );
    [
    propput, 
    id(0x0000000A)
    ]
    /**
        \brief Set keyword for control's context-sensitive help topic

        Specify keyword for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use topic keyords.
        If \p value is an empty string (zero length), no help topic is
        provided for this control.

        \param value The new help topic keyword.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpKeyword([in] BSTR value );
    [
    propget, 
    id(0x0000000B)
    ]
    /**
        \brief Get numeric ID for control's context-sensitive help topic

        Retrieve numeric ID for control's context-sensitive help topic.

        If \p value is 0 (default), no help topic is provided for this control.

        \param value The help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([out, retval] long * value );
    [
    propput, 
    id(0x0000000B)
    ]
    /**
        \brief Set numeric ID for control's context-sensitive help topic

        Specify numeric ID for control's context-sensitive help topic.

        Supports context-sensitive help for applications that use numeric topic IDs.
        If \p value is 0, no help topic is provided for this control.

        \param value The new help numeric topic ID.
        \return S_OK if successful, or an error value otherwise.
        \see HelpType
    **/
    HRESULT _stdcall HelpContext([in] long value );
    /**
        \brief Get how the control aligns within its container (parent control).

        Retreive how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Receives control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000C)
    ]
    HRESULT _stdcall Align([out, retval] long * value );
    /**
        \brief Set how the control aligns within its container (parent control).

        Specify how the control aligns within its container (parent control).

		Use Align to align a control to the top, bottom, left, or right of a form or panel and have it remain there even if the size of the form, panel, or component that contains the control changes. When the parent is resized, an aligned control also resizes so that it continues to span the top, bottom, left, or right edge of the parent.
				 
		Note: To cause a control to maintain a specified relationship with an edge of its parent, but not necessarily lie along one edge of the parent, use the Anchors property instead.
				
		\p value can be:
        \li 0 - no alignment - control remains where placed on the form
        \li 1 - top
        \li 2 - bottom
        \li 3 - left
        \li 4 - right
        \li 5 - client - fills the entire client area
        \li 6 - custom
 
        \param value Sets control's alignment (0-6).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000C)
    ]
    HRESULT _stdcall Align([in] long value );
    /**
        \brief Gets the property specifing if the form is translucent.
        
        This method gets the property specifing if the form is translucent. When
        set to true (1), use AlphaBlendValue to specify the degree of translucency.

        value can be:
        \li 0 - false (form is opaque)
        \li 1 - true  (form is translucent)

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000D)
    ]
    HRESULT _stdcall AlphaBlend([out, retval] long * value );
    /**
        \brief Sets the property specifing if the form is translucent.
        
        This method sets the property specifing if the form is translucent. When
        set to true (1), use AlphaBlendValue to specify the degree of translucency.

        value can be:
        \li 0 - false (form is opaque)
        \li 1 - true  (form is translucent)

        \param value The new value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000D)
    ]
    HRESULT _stdcall AlphaBlend([in] long value );
    /**
        \brief Gets the degree of translucency of a translucent form.

        This method gets the degree of translucency of a translucent form (see 
        AlphaBlend).  The value can be between 0 (clear) and 255 (opague). This
        property has no effect if AlphaBlend is set to 0 (false).

        \param value Receives the degree of translucency (0-255).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AlphaBlendValue([out, retval] long * value );
    /**
        \brief Sets the degree of translucency of a translucent form.

        This method sets the degree of translucency of a translucent form (see 
        AlphaBlend).  The value can be between 0 (clear) and 255 (opague). This
        property has no effect if AlphaBlend is set to 0 (false).

        \param value Receives the degree of translucency (0-255).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000E)
    ]
    HRESULT _stdcall AlphaBlendValue([in] long value );
    /**
        \brief Gets the property indicating if scrollbars will automatically appear.

        This method gets the property indicating if scrollbars will automatically
        appear when the display area is too small to display all controls.

        value can be:
        \li 0 - (false) - scrollbars aren't automatically added
        \li 1 - (true)  - scrollbars are automatically added

        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000000F)
    ]
    HRESULT _stdcall AutoScroll([out, retval] long * value );
    /**
        \brief Sets the property indicating if scrollbars will automatically appear.

        This method sets the property indicating if scrollbars will automatically
        appear when the display area is too small to display all controls.

        value can be:
        \li 0 - (false) - scrollbars aren't automatically added
        \li 1 - (true)  - scrollbars are automatically added

        \param value The new value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000000F)
    ]
    HRESULT _stdcall AutoScroll([in] long value );
    /**
        \brief Get whether the control sizes itself automatically to accommodate its contents.

        Gets property specifing whether the control sizes itself automatically 
        to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Gets control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000010)
    ]
    HRESULT _stdcall AutoSize([out, retval] long * value );
    /**
        \brief Set whether the control sizes itself automatically to accommodate its contents.

        Specifies whether the control sizes itself automatically to accommodate its contents.

		Use AutoSize to specify whether the control sizes itself automatically. When AutoSize is true, the control resizes automatically when its contents change.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Gets control's autosize setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000010)
    ]
    HRESULT _stdcall AutoSize([in] long value );
    [
    propget, 
    id(0x00000011)
    ]
    /**
        \brief Get the bi-directional mode for the control.

        Retrieve the enabled state of the bi-directional mode for this control.

        See BiDiMode(long value) for additional information.\n
        \p value will be either 0 (FALSE) or 1 (TRUE)

        \param value Is bi-direction mode enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall BiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x00000011)
    ]
    /**
        \brief Set the bi-directional mode for the control.

        Specify whether or not bi-directional mode should be enabled.

        Use BiDiMode to enable the control to adjust its appearance and behavior
        automatically when the application runs in a locale that reads from
        right to left instead of left to right.\n
        The bi-directional mode controls the reading order for the text,
        the placement of the vertical scroll bar, and whether the alignment
        is changed.

        \p value must be either 0 (FALSE, do not use) or 1 (TRUE, use). Any other value is ignored
        and bi-directional mode is not changed.

        \param value Enable/disable bi-directional mode
        \return S_OK if successful, or an error value otherwise.

    **/
    HRESULT _stdcall BiDiMode([in] long value );
    /**
        \brief Get whether the edit control has a single line border around the client area.

        Retreive whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Receives control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000012)
    ]
    HRESULT _stdcall BorderStyle([out, retval] long * value );
    /**
        \brief Set whether the edit control has a single line border around the client area.

        Specify whether the edit control has a single line border around the client area.

		Use BorderStyle to affect the sharpness with which the client area of the edit control stands out.
				 
		\p value can be:
        \li 0 - no border
        \li 1 - single line border
 
        \param value Sets control's border style (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000012)
    ]
    HRESULT _stdcall BorderStyle([in] long value );
    /**
        \brief Get the width of the control's border

        Retreive the width of the control's border

		Use BorderWidth to get or set the width of the control's border. Graphics or text drawn by the control is clipped to the area within the border.
				 
		\p value should be in pixels.
 
        \param value Receives control's border width.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000013)
    ]
    HRESULT _stdcall BorderWidth([out, retval] long * value );
    /**
        \brief Set the width of the control's border

        Specify the width of the control's border

        Use BorderWidth to get or set the width of the control's border. Graphics or text drawn by the control is clipped to the area within the border.
				 
        \p value should be in pixels.
        \param value Sets control's border width.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000013)
    ]
    HRESULT _stdcall BorderWidth([in] long value );
    [
    propget, 
    id(0x00000014)
    ]
    /**
        \brief Get the text string that identifies the control to the user.

        Retrieve the text string that identifies the control to the user.

        See Caption(BSTR value) for a detailed explanation.

        \p value must have space for at least 256 characters allocated.

        \param value The control's caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([out, retval] BSTR * value );
    [
    propput, 
    id(0x00000014)
    ]
    /**
        \brief Set the text string that identifies the control to the user.

        Specify the text string that identifies the control to the user.

        To specify an accelerator character, include an ampersand (\&) before
        the character. To display an ampersand character in the caption,
        use two ampersands (\&\&).

        \note
        Controls that display text set either a caption or text property.
        Which property is used depends on the type of control.
        In general, caption is used for text that appears as a window title
        or label, while text is used for text that appears as the content
        of a control.

        \p value may be any length but should be less than 256 characters.

        \param value The new caption.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Caption([in] BSTR value );
    /**
        \brief Gets the height in pixels of the form's client area.

        This method gets the height in pixels of the form's client area.  The 
        client area is the usable area inside the form's border, excluding the 
        title bar, scroll bars, and so on.
       
       \param value Receives the height of the form's client area in pixels.
       \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000015)
    ]
    HRESULT _stdcall ClientHeight([out, retval] long * value );
    /**
        \brief Sets the height in pixels of the form's client area.

        This method sets the height in pixels of the form's client area.  The 
        client area is the usable area inside the form's border, excluding the 
        title bar, scroll bars, and so on.
       
       \param value The new height of the form's client area in pixels.
       \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000015)
    ]
    HRESULT _stdcall ClientHeight([in] long value );
    /**
        \brief Gets the width in pixels of the form's client area.

        This method gets the width in pixels of the form's client area.

        \param value Receives the width of the form's client area in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000016)
    ]
    HRESULT _stdcall ClientWidth([out, retval] long * value );
    /**
        \brief Sets the width in pixels of the form's client area.

        This method sets the width in pixels of the form's client area.

        \param value The new width of the form's client area in pixels.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000016)
    ]
    HRESULT _stdcall ClientWidth([in] long value );
    [
    propget, 
    id(0x00000017)
    ]
    /**
        \brief Get the control's color.

        Retrieve the control's current color.

        See Color(long value) for a detailed explanation.\n

        \param value The control's current color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([out, retval] long * value );
    [
    propput, 
    id(0x00000017)
    ]
    /**
        \brief Set the control's color.

        Specify the control's color.

        Color is specified as a specific 4-byte hexadecimal number, the low three
        bytes represent RGB color intensities for blue, green, and red, respectively.
        The value \p 0x00FF0000 represents full-intensity, pure blue, \p 0x0000FF00 is
        pure green, and \p 0x000000FF is pure red. \p 0x00000000 is black and \p 0x00FFFFFF
        is white.

        If the highest-order byte is zero, the color obtained is the closest
        matching color in the system palette. If the highest-order byte is one
        (<code>0x01</code>), the color obtained is the closest matching color in the currently
        realized palette. If the highest-order byte is two (<code>0x02</code>), the value is
        matched with the nearest color in the logical palette of the current device
        context.

        If \p value cannot be parsed correctly the control's color will remain unchanged.

        \param value The control's new color.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Color([in] long value );
    /**
        \brief Gets property indicating if a color on the form appears transparent.
        
        This method gets the property indicating if a color on the form appears
        transparent (see TransparentColorValue).
        Note: To make the entire form transparent, or to make it translucent rather than transparent, use the AlphaBlend and AlphaBlendvalue properties.
 
        Note: TransparentColor does not work on all systems. The application must be running under Windows 2000 or better, with adequate hardware.

        value can be:
        \li 0 - color is opague.
        \li 1 - color is transparent.
       
        \param value Receives the value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000018)
    ]
    HRESULT _stdcall TransparentColor([out, retval] long * value );
    /**
        \brief Sets property indicating if a color on the form appears transparent.
        
        This method sets the property indicating if a color on the form appears
        transparent (see TransparentColorValue).
        Note: To make the entire form transparent, or to make it translucent rather than transparent, use the AlphaBlend and AlphaBlendValue properties.
 
        Note: TransparentColor does not work on all systems. The application must be running under Windows 2000 or better, with adequate hardware.

        value can be:
        \li 0 - color is opague.
        \li 1 - color is transparent.
       
        \param value The new value of the property (0 or 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000018)
    ]
    HRESULT _stdcall TransparentColor([in] long value );
    /**
        \brief Gets the color on the form that appears transparent when TransparentColor is true (1).

        This method gets the color on the form that appears transparent when 
        TransparentColor is true (1).

        Note: If the application uses a 16-bit color palette, TransparentColorValue 
        must be a basic color.

        \param value Receives the current value of TransparentColorValue.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000019)
    ]
    HRESULT _stdcall TransparentColorValue([out, retval] long * value );
    /**
        \brief Sets the color on the form that appears transparent when TransparentColor is true (1).

        This method sets the color on the form that appears transparent when 
        TransparentColor is true (1).

        Note: If the application uses a 16-bit color palette, TransparentColorValue 
        must be a basic color.

        \param value Receives the current value of TransparentColorValue.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000019)
    ]
    HRESULT _stdcall TransparentColorValue([in] long value );
    [
    propget, 
    id(0x0000001A)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([out, retval] long * value );
    [
    propput,
    id(0x0000001A)
    ]
    /**
        \brief Has no effect on 32-bit versions of Windows and on NT 4.0 and later, unsupported.
    **/
    HRESULT _stdcall Ctl3D([in] long value );
    /**
        \brief Get whether the docking manager is used in drag-and-dock operations.

        Retreive whether the docking manager is used in drag-and-dock operations.

        Use UseDockManager to get or set whether a docking manager is used in drag-and-dock operations. 

        The docking manager handles the positioning of docked controls and any painting of docking zones around those controls. If you do not use a docking manager, docked controls are aligned to the windowed control based on the closest edge to where they are released. When you do not use a docking manager, there is no region for a user to grab to automatically undock a docked control.
		
        \p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's UseDockManager setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001B)
    ]
    HRESULT _stdcall UseDockManager([out, retval] long * value );
    /**
        \brief Set whether the docking manager is used in drag-and-dock operations.

        Specifies whether the docking manager is used in drag-and-dock operations.

		Use UseDockManager to get or set whether a docking manager is used in drag-and-dock operations. 
			
		The docking manager handles the positioning of docked controls and any painting of docking zones around those controls. If you do not use a docking manager, docked controls are aligned to the windowed control based on the closest edge to where they are released. When you do not use a docking manager, there is no region for a user to grab to automatically undock a docked control.
				 
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's UseDockManager setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001B)
    ]
    HRESULT _stdcall UseDockManager([in] long value );
    /**
        \brief Gets property specifing which monitor is used to display the form.


        This method gets property specifing which monitor is used to display 
        the form.

        value can be:
        \li 0 - No attempt is made position the form on a monitor.
        \li 1 - The form is positioned on the system's first monitor.
        \li 2 - The form appears on the same monitor as the application's main form.
        \li 3 - The form appears on the same monitor as the currently active form.

        \param value Receives the value of the property (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001C)
    ]
    HRESULT _stdcall DefaultMonitor([out, retval] long * value );
    /**
        \brief Sets property specifing which monitor is used to display the form.


        This method sets property specifing which monitor is used to display 
        the form.

        value can be:
        \li 0 - No attempt is made position the form on a monitor.
        \li 1 - The form is positioned on the system's first monitor.
        \li 2 - The form appears on the same monitor as the application's main form.
        \li 3 - The form appears on the same monitor as the currently active form.

        \param value The new value of the property (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001C)
    ]
    HRESULT _stdcall DefaultMonitor([in] long value );
    /**
        \brief Get whether the control can be the target of drag-and-dock operations.

        Retreive whether the control can be the target of drag-and-dock operations.

		Set DockSite to true to allow other controls to be docked to this windowed control.
				
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Receives control's DockSite setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000001D)
    ]
    HRESULT _stdcall DockSite([out, retval] long * value );
    /**
        \brief Set whether the control can be the target of drag-and-dock operations.

        Specifies whether the control can be the target of drag-and-dock operations.

		Set DockSite to true to allow other controls to be docked to this windowed control.
				
		\p value is 1 (TRUE) or 0 (FALSE)
 
        \param value Sets control's DockSite setting.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000001D)
    ]
    HRESULT _stdcall DockSite([in] long value );
    [
    propget, 
    id(0x0000001E)
    ]
    /**
        \brief Get whether control can be dragged normally or for docking

        Retrieve whether control can be dragged normally or for docking

        \p value will be either 0 (normal) or 1 (docking).\n
        See DragKind(long value) for additional information.

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([out, retval] long * value );
    [
    propput, 
    id(0x0000001E)
    ]
    /**
        \brief Set whether control can be dragged normally or for docking

        Specify whether the control participates in drag-and-drop operations, or drag-and-dock.

        \p value must be either  0 (dropping) or 1 (docking). All other values
        are ignored and DragKind will not change.\n

        \param value Normal or dock dragging?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragKind([in] long value );
    [
    propget, 
    id(0x0000001F)
    ]
    /**
        \brief Get whether or not drag operations are enabled.

        Retrieve a property indicating whether or not drag operations are enabled.

        \p value will either be 0 (disabled) or 1 (enabled).\n
        See DragMode(long value) for additional information.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([out, retval] long * value );
    [
    propput, 
    id(0x0000001F)
    ]
    /**
        \brief Set whether or not drag operations are enabled.

        Specify whether or not drag operations are enabled.

        Disable the drag-and-drop or drag-and-dock capability at runtime by
        setting the \p value to 0. Enable automatic dragging by setting
        \p value to 1.

        \param value Are dragging operations enabled?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall DragMode([in] long value );
    [
    propget, 
    id(0x00000020)
    ]
    /**
        \brief Get the enabled state of a control.

        Retrieve whether the control responds to mouse and keyboard input.

        \p value will either be 0 (FALSE, disabled) or 1 (TRUE, enabled).\n
        See Enabled(long value) for additional information.
        
        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([out, retval] long * value );
    [
    propput, 
    id(0x00000020)
    ]
    /**
        \brief Set the enabled state of a control.

        Specify whether the control responds to mouse and keyboard input.

        A disabled control appears dimmed and ignores mouse and keyboard events.
        \p value may be either 0 (FALSE, disable) or 1 (TRUE, enable). All other
        values are ignored and will not change the enabled state of the control.

        \param value The enabled state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Enabled([in] long value );
    [
    propget, 
    id(0x00000021)
    ]
    /**
        \brief Is the control using its parent's font?

        Retreive property indicating whether or not control should use its parent's
        font.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentFont(long value) for additonal information. 

        \param value Using parent's font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([out, retval] long * value );
    [
    propput, 
    id(0x00000021)
    ]
    /**
        \brief Set whether or not control should use its parent's font.

        Specify where a control should get font information.

        \p value must be either 0 (FALSE) or 1 (TRUE). Any other value
        is ignored and property does not change.

        If \p value is true the control uses its parent's font. If \p value is
        false control uses its own font information.

        Flag is true by default so all controls on a IExoudsControlForm share
        the same font. If control's font changes, this property is automatically
        set to false.

        \param value Should control use parent font?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentFont([in] long value );
    [
    propget, 
    id(0x00000022)
    ]
    /**
        \brief Get the font of the control.

        Retrieve the font associated with the control.

        Use the IExodusControlFont to modify the font.\n
        \p value will never be null

        todo: Who is responsible for cleanup of the Delphi font object

        \param value The control's font
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Font([out, retval] IExodusControlFont ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the property determining the style of the form.

        This method gets the property determining the style of the form.
        value can be:
        \li 0 - The form is not a MDI parent or MDI child window.
        \li 1 - The form is a MDI child window.
        \li 2 - The form is a MDI parent window.
        \li 3 - The form remains on top of the desktop and other forms in the project (except forms with this FormStyle setting).

        \param value Receives the value of the property (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000023)
    ]
    HRESULT _stdcall FormStyle([out, retval] long * value );
    /**
        \brief Sets the property determining the style of the form.

        This method sets the property determining the style of the form.
        value can be:
        \li 0 - The form is not a MDI parent or MDI child window.
        \li 1 - The form is a MDI child window.
        \li 2 - The form is a MDI parent window.
        \li 3 - The form remains on top of the desktop and other forms in the project (except forms with this FormStyle setting).

        \param value The new value of the property (0, 1, 2, 3).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000023)
    ]
    HRESULT _stdcall FormStyle([in] long value );
    /**
        \brief Gets the name of the file used to display the application's help.

        This gets the name of the file used to display the application's help.

        \param value Receives the name of the application's help file.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000024)
    ]
    HRESULT _stdcall HelpFile([out, retval] BSTR * value );
    /**
        \brief Sets the name of the file used to display the application's help.

        This sets the name of the file used to display the application's help.

        \param value Receives the name of the application's help file.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000024)
    ]
    HRESULT _stdcall HelpFile([in] BSTR value );
    /**
        \brief Gets the property indicating the form should receive keyboard events before the active control.

        This method gets the property indicating the form should receive keyboard
        events before the active control.  If this propety is true (1), keyboard 
        events occur on the form before the active control, if false the form does 
        not receive events.  value can be true (1) and false (0).
        
        \b Note: Navigation keys and buttons with focus and the Default property set 
        to true do not generate keyboard events.

        \param value Receives the value of the property (0, 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000025)
    ]
    HRESULT _stdcall KeyPreview([out, retval] long * value );
    /**
        \brief Sets the property indicating the form should receive keyboard events before the active control.

        This method sets the property indicating the form should receive keyboard
        events before the active control.  If this propety is true (1), keyboard 
        events occur on the form before the active control, if false the form does 
        not receive events.  value can be true (1) and false (0).
        
        \b Note: Navigation keys and buttons with focus and the Default property set 
        to true do not generate keyboard events.

        \param value The new value of the property (0, 1).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000025)
    ]
    HRESULT _stdcall KeyPreview([in] long value );
    /**
        \brief Gets the form's main menu.

        This method gets the form's main menu.

        \param value Receives the form's menu.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000026)
    ]
    HRESULT _stdcall Menu([out, retval] IExodusControlMainMenu ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the property indicating when OnCreate and OnDestroy events occur.

        This method gets the property indicating when OnCreate and OnDestroy 
        events occur.  When OldCreateOrder is false (0 - the default) the OnCreate 
        event occurs after all constructors are finished and the OnDestroy event 
        occurs before any destructors are called.
       
       \param value Receives the value of the property (0 - false, 1 - true).
       \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000027)
    ]
    HRESULT _stdcall OldCreateOrder([out, retval] long * value );
    /**
        \brief Sets the property indicating when OnCreate and OnDestroy events occur.

        This method sets the property indicating when OnCreate and OnDestroy 
        events occur.  When OldCreateOrder is false (0 - the default) the OnCreate 
        event occurs after all constructors are finished and the OnDestroy event 
        occurs before any destructors are called.
       
       \param value Receives the value of the property (0 - false, 1 - true).
       \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000027)
    ]
    HRESULT _stdcall OldCreateOrder([in] long value );
    /**
        \brief Gets the number of items in the form's menu.

        This method gets the number of items in the form's menu.

        \param value Receives the item count of the form's menu.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000028)
    ]
    HRESULT _stdcall ObjectMenuItemCount([out, retval] long * value );
    /**
        \brief Gets a form's menu item by index.

        This method gets a form's menu item by index. The index is subjected
        to bounds checking and nil is returned if the index is out of bounds.

        \param index The index of the desired menu item.
        \param value Receives the menu item or nil if not found.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000029)
    ]
    HRESULT _stdcall ObjectMenuItem([in] long index, [out, retval] IExodusControlMenuItem ** value /*Warning: unable to validate structure name: */ );
    [
    propget, 
    id(0x0000002A)
    ]
    /**
        \brief Get whether or not to use parent's bi-directional mode.

        Retrieves a property indicating whether the control should uses its parent's
        bi-directional mode.

        \p value will be either 0 (FALSE) or 1 (TRUE).

        See ParentBiDiMode(long value) for additional information.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([out, retval] long * value );
    [
    propput, 
    id(0x0000002A)
    ]
    /**
        \brief Set whether or not to use parent's bi-directional mode.

        Specifies whether the control uses its parent's bi-directional mode.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are
        ignored and the Use parent bi-directional mode property does not change.

        When \p value is true, the control's bi-directional mode is always equal
        to that of its parent. If \p value is false, the control uses its own
        bi-directional mode (See BiDiMode(long value)).

        Set \p value to true for all controls in a IExodusControlForm in order
        to ensure that the entire user interface adjusts to Middle Eastern
        locales in a uniform manner.

        When the value of the control's bi-directional mode changes,
        the Use parent bi-directional mode property becomes false automatically.

        \param value Should control use its parent's bi-directional mode?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ParentBiDiMode([in] long value );
    /**
        \brief Gets the property describing the proportion of the font on the user's system.

        This method gets the property describing the proportion of the font on 
        the user's system. This property can be adjusted at runtime to change 
        how the form is scaled to the active screen resolution.

        To use this the Scaled property must be set to true.

        \param value Receives the value of the property in pixels per inch.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002B)
    ]
    HRESULT _stdcall PixelsPerInch([out, retval] long * value );
    /**
        \brief Sets the property describing the proportion of the font on the user's system.

        This method sets the property describing the proportion of the font on 
        the user's system. This property can be adjusted at runtime to change 
        how the form is scaled to the active screen resolution.

        To use this the Scaled property must be set to true.

        \param value The new value of the property in pixels per inch.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002B)
    ]
    HRESULT _stdcall PixelsPerInch([in] long value );
    [
    propget, 
    id(0x0000002C)
    ]
    /**
        \brief Get the control's popup menu

        Retrieve the popup menu for the control.

        Plugins may access any pre-defined popup menu but may not create their own
        and assign them to controls. \n
        \p value will never be null but may be empty.

        todo: looks like empty menus mean no popup actually exists for the control.
        though one is returned here, manipulating does not mean it
        will show up in Exodus. Also who is
        responsible for destroying this object in Exodus?

        \param value The possibly empty popup menu for this control.  
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall PopupMenu([out, retval] IExodusControlPopupMenu ** value /*Warning: unable to validate structure name: */ );
    /**
        \brief Gets the property affecting the forms size and position.

        This method gets the property affecting the forms size and position.
        value can be:
        \li 0 - Form position and size appears as in design mode.
        \li 1 - Form appears as determined by the OS. Each time the application is run, the form moves down & to the right (does not apply to pop-up or child windows).
        \li 2 - Form retains design time size, but the OS chooses the position. Each time the application is run, the form moves down & to the right (does not apply to pop-up or child windows).
        \li 3 - Form appears in design time position, but OS chooses the size.
        \li 4 - Form retains the design time size, but is positioned in the center of the screen. In multi-monitor systemsthe form can be moved so it fits on one monitor.
        \li 5 - Form retains the design time size, but is positioned in the center of the screen (no adjustments made for multi-monitor systems).
        \li 6 - Form retains the design time size, but is positioned near the center of the application's main form (no adjustments are made for multi-monitor systems).  This should only be used with secondary forms, if used ona main form it acts like 4.
        \li 7 - Form retains the design time size, but is positioned in the center of the form specified by the Owner property. If Owner does not specify a form, acts like 6.

        \param value Receives the value of the property (0-7).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002D)
    ]
    HRESULT _stdcall Position([out, retval] long * value );
    /**
        \brief Sets the property affecting the forms size and position.

        This method sets the property affecting the forms size and position.
        value can be:
        \li 0 - Form position and size appears as in design mode.
        \li 1 - Form appears as determined by the OS. Each time the application is run, the form moves down & to the right (does not apply to pop-up or child windows).
        \li 2 - Form retains the design time size, but the OS chooses the position. Each time the application is run, the form moves down & to the right (does not apply to pop-up or child windows).
        \li 3 - Form appears in design time position, but OS chooses the size.
        \li 4 - Form retains the design time size, but is positioned in the center of the screen. In multi-monitor systemsthe form can be moved so it fits on one monitor.
        \li 5 - Form retains the design time size, but is positioned in the center of the screen (no adjustments made for multi-monitor systems).
        \li 6 - Form retains the design time size, but is positioned near the center of the application's main form (no adjustments are made for multi-monitor systems).  This should only be used with secondary forms, if used ona main form it acts like 4.
        \li 7 - Form retains the design time size, but is positioned in the center of the form specified by the Owner property. If Owner does not specify a form, acts like 6.

        \param value Receives the value of the property (0-7).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002D)
    ]
    HRESULT _stdcall Position([in] long value );
    /**
        \brief Gets the property representing the proportions of a printed form.

        This method gets the property representing the proportions of a printed 
        form.
        value can be:
        \li 0 - No scaling done. The printed form and it's onscreen appearance may be distored.
        \li 1 - The form is printed approximately the size appearing on screen.
        \li 2 - The form is printed using the screen proportions but in a size fitting the printed page.

        \param value Receives the value of the property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002E)
    ]
    HRESULT _stdcall PrintScale([out, retval] long * value );
    /**
        \brief Sets the property representing the proportions of a printed form.

        This method sets the property representing the proportions of a printed 
        form.
        value can be:
        \li 0 - No scaling done. The printed form and it's onscreen appearance may be distored.
        \li 1 - The form is printed approximately the size appearing on screen.
        \li 2 - The form is printed using the screen proportions but in a size fitting the printed page.

        \param value Receives the value of the property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002E)
    ]
    HRESULT _stdcall PrintScale([in] long value );
    /**
        \brief Gets the property indicating if the form is sized using PixelsPerInch.

        This method gets the property indicating if the form is sized using 
        PixelsPerInch.  If true (1), the form will resize itself to differences
        between the font used at design time and the current system font at 
        runtime.

        \param value Receives the value of the property (0 - false, 1 - true).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x0000002F)
    ]
    HRESULT _stdcall Scaled([out, retval] long * value );
    /**
        \brief Sets the property indicating if the form is sized using PixelsPerInch.

        This method sets the property indicating if the form is sized using 
        PixelsPerInch.  If true (1), the form will resize itself to differences
        between the font used at design time and the current system font at 
        runtime.

        \param value The new value of the property (0 - false, 1 - true).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x0000002F)
    ]
    HRESULT _stdcall Scaled([in] long value );
    /**
        \brief Gets the property specifing if a form snaps to the edge of the screen.

        This method gets the property specifing if a form snaps to the edge of 
        the screen when the form is moved at runtime.  To determine how close the 
        form needs be before snapping to the screen edges use SnapBuffer.

        \param value Receives the value of the property (0 - false, 1 - true).
        \return S_OK if successful, or an error value otherwise.
    **/ 
    [
    propget, 
    id(0x00000030)
    ]
    HRESULT _stdcall ScreenSnap([out, retval] long * value );
    /**
        \brief Sets the property specifing if a form snaps to the edge of the screen.

        This method sets the property specifing if a form snaps to the edge of 
        the screen when the form is moved at runtime.  To determine how close the 
        form needs be before snapping to the screen edges use SnapBuffer.

        \param value The new value of the property (0 - false, 1 - true).
        \return S_OK if successful, or an error value otherwise.
    **/ 
    [
    propput, 
    id(0x00000030)
    ]
    HRESULT _stdcall ScreenSnap([in] long value );
    [
    propget, 
    id(0x00000031)
    ]
    /**
        \brief Get whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Retrieve whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        \p value will either be 0 (FALSE) or 1(TRUE). See ShowHint(long value)
        for additional information.

        \param value Should Help Hint be displayed?
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall ShowHint([out, retval] long * value );
    [
    propput, 
    id(0x00000031)
    ]
    /**
        \brief Set whether the control displays a Help Hint when the mouse pointer rests momentarily on the control.

        Specify whether the control displays a Help Hint when the mouse
        pointer rests momentarily on the control.

        To enable Help Hint for a particular control, either the control's own
        ShowHint must be true, or the control's ParentShowHint must be true and
        its parent's ShowHint property must be true.

        For example, imagine an IExodusControlCheckBox within an IExodusControlPanel.
        If the ShowHint of the panel is true and the ParentShowHint of the
        check box is true, but the ShowHint of the check box is false,
        the check box still displays its Help Hint.

        Changing the ShowHint value automatically sets the ParentShowHint to false.

        \p value must be either 0 (FALSE) or 1 (TRUE). All other values are ignored
        and the Help Hint state will not change.
        \param value
        \return S_OK if successful, or an error value otherwise.

        \see Hint, ParentShowHint
    **/
    HRESULT _stdcall ShowHint([in] long value );
    /**
        \brief Gets the value specifing the distance for screen snap.

        This method gets the value specifing the distance for screen snap in 
        pixels (see ScreenSnap).

        \param value Receives the value of the property (in pixels).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000032)
    ]
    HRESULT _stdcall SnapBuffer([out, retval] long * value );
    /**
        \brief Sets the value specifing the distance for screen snap.

        This method sets the value specifing the distance for screen snap in 
        pixels (see ScreenSnap).

        \param value The new value of the property (in pixels).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000032)
    ]
    HRESULT _stdcall SnapBuffer([in] long value );
    [
    propget, 
    id(0x00000033)
    ]
    /**
        \brief Get whether or not the control appears on screen.

        Retrieve whether or not the control appears on screen.

        \p value will either be 0 (FALSE, invisible) or 1 (TRUE, visible).

        \param value The visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([out, retval] long * value );
    [
    propput, 
    id(0x00000033)
    ]
    /**
        \brief Set whether or not the control appears on screen.

        Specify whether or not the control appears on screen.

        \p value must either be 0 (FALSE, invisible) or 1 (TRUE, visible). Any
        other value will be ignored and visiblity will not change.

        \param value The new visibility state of the control.
        \return S_OK if successful, or an error value otherwise.
    **/
    HRESULT _stdcall Visible([in] long value );
    /**
        \brief Gets property indicating how the window appears (maximized, minimized, normal).

        This method gets the property indicating how the window appears (maximized, minimized, normal).
        value can be:
        \li 0 - normal
        \li 1 - minimized
        \li 2 - maximized

        \param value Receives the value of the property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000034)
    ]
    HRESULT _stdcall WindowState([out, retval] long * value );
    /**
        \brief Sets property indicating how the window appears (maximized, minimized, normal).

        This method sets the property indicating how the window appears (maximized, minimized, normal).
        value can be:
        \li 0 - normal
        \li 1 - minimized
        \li 2 - maximized

        \param value The new value of the property (0, 1, 2).
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propput, 
    id(0x00000034)
    ]
    HRESULT _stdcall WindowState([in] long value );
    /**
        \brief Gets the count of items in the form's menu.

        This method gets the count of items inthe form'smenu.

        \param value Receives the count of items in the form's menu.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000035)
    ]
    HRESULT _stdcall WindowMenuCount([out, retval] long * value );
    /**
        \brief Gets an item from the form's menu.

        This method gets an item by index from the form's menu.  Bounds checking
        is performed and nil is returned if index is out of range.

        \param index The index of the menu item to return.
        \param value The desired menu item or nil if not found or index out of range.
        \return S_OK if successful, or an error value otherwise.
    **/
    [
    propget, 
    id(0x00000036)
    ]
    HRESULT _stdcall WindowMenu([in] long index, [out, retval] IExodusControlMenuItem ** value /*Warning: unable to validate structure name: */ );
  };

  [
    uuid(35542007-5701-4190-AB28-D25EB186CC19), 
    version(1.0), 
    dual, 
    oleautomation
  ]
  /**\interface IExodusLogger
    \brief Inteface for logging of messages.
    
    This Interface defines methods for both adding and retrieving messages from a persistent store.
    Items are logged and retrieved via JID and optionally date.
  **/

   interface IExodusLogger: IDispatch
  {
    /**
        \brief <b>UNSUPPORTED</b> - Logs a message to persistent store.

        <b>UNSUPPORTED</b>

        This method will store a message to the persistent store.

        \param Msg The message to be logged to the persistent store.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000C9)
    ]
    HRESULT _stdcall LogMessage([in] IExodusLogMsg * Msg /*Warning: unable to validate structure name: */ );
    /**
        \brief Display full log for a specific JID.

        This method instructs the plugin to display the message log for a specific JID.

        \param jid The JID to display the message history for.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CA)
    ]
    HRESULT _stdcall Show([in] BSTR jid );
    /**
        \brief Delete log for a specific JID.

        This method instructs the plugin to delete the message log for a specific JID.

        \param jid The JID to delete the message history for.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CB)
    ]
    HRESULT _stdcall Clear([in] BSTR jid );
    /**
        \brief Delete all stored message logs

        This method instructs the plugin to delete all stored message logs.

        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CC)
    ]
    HRESULT _stdcall Purge( void );
    /**
        \brief <b>UNSUPPORTED</b> - Get a list of days for which there are stored message logs.

        <b>UNSUPPORTED</b> - If the plugin supports message log retrieval by date, 
        this method queries the plugin for what days of a specific 
        month and year have message logs for a specific JID.
        The plugin calls back an IExodusListener to supply the dates.

        \param jid The JID for which to retrieve dates.
        \param month The month for which to retrieve dates.
        \param year The year for which to retrieve dates.
        \param listener The IExodusLogListener for the plugin to call back to supply the dates.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CD)
    ]
    HRESULT _stdcall GetDays([in] BSTR jid, [in] long month, [in] long year, [in] IExodusLogListener * listener /*Warning: unable to validate structure name: */ );
    /**
        \brief <b>UNSUPPORTED</b> - Get the message log for a specific date.

        <b>UNSUPPORTED</b> - If the plugin supports message log retrieval by date, 
        this method queries the plugin for the message log for
        a specific date and  for a specific JID.
        The plugin calls back an IExodusListener to supply the message log.

        \param jid The JID for which to retrieve the message log.
        \param chunkSize The maximum amount of data to be retrieved at one time
        \param day The day for which to retrieve the message log.
        \param month The month for which to retrieve the message log.
        \param year The year for which to retrieve the message log.
        \param cancel Cancel message log retrieval for this date and Jid.
        \param listener The IExodusLogListener for the plugin to call back to supply the message log.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    id(0x000000CE)
    ]
    HRESULT _stdcall GetMessages([in] BSTR jid, [in] long chunkSize, [in] long day, [in] long month, [in] long year, [in] VARIANT_BOOL cancel, [in] IExodusLogListener * listener /*Warning: unable to validate structure name: */ );
    /**
        \brief <b>UNSUPPORTED</b> - Is message log retrieval by date supported.

        <b>UNSUPPORTED</b> - Queres the plugin to determine if the plugin supports
        retrieving message logs by specific date.

        \param value True if message log retrieval by date is supported, False if not.
        \return \c HRESULT either \c S_OK or an error value.
    **/
    [
    propget, 
    id(0x000000CF)
    ]
    HRESULT _stdcall IsDateEnabled([out, retval] VARIANT_BOOL * value );
  };

};
